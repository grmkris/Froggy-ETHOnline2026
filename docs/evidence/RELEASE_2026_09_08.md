# Combined release — 8 September 2026

The owner requested deployment of all current changes for live testing. The release combines X research routing and service-version recovery, bounded Graph errors, URL purchase approvals and persistence, native-browser paid-page handling, and trading research services.

## Source and validation

The uploaded source is a fixed snapshot of all tracked and non-ignored files captured from the shared checkout, based on `b7025b3fc5f3fa0df58c6a24f29a8df7883eb14e`. Verification ran in `.froggy/release-0908`; no source edits were required during verification. The source manifest SHA-256 is `7957bca7f41d17ac958cc5a47e7b8c6827bcf3e2067c1db795c371250856c168`. Deployment used a local Railway upload; this operation did not commit or push the concurrent workspace changes.

- `bun run check:fast`: passed in the shared checkout.
- `bun run check`: passed on the release snapshot, including type-aware lint, types, boundaries, unit tests and dead-code detection.
- `bun run build`: both build tasks passed.
- Disposable Postgres: all migrations applied; all 21 persistence and ledger checks passed with 110 assertions. The test container was removed.
- Browser suite: 111 passed, one native-Chrome startup failure and one subsequent serial case not run. The unchanged paid-page scenario then passed with a shorter temporary directory, and all four purchase scenarios passed together in a follow-up. All 113 distinct scenarios have passing runs; the original full command was not a clean pass. The shared-machine total test budget was 90 seconds, with assertion deadlines unchanged. The existing appearance case passed.

The temporary disk was full during initial setup. Dependencies and browser temporary files were moved to the workspace disk. The browser failure displayed `Chrome process closed the pipe`; no product assertions were weakened to obtain passing results.

## Production

- Project: `froggy` (`d6f4178e-fc21-4827-8347-20b1cec2aba4`).
- Environment: production (`44c2247f-e0a2-43f8-9b46-586a29126157`).
- Service: app (`393648df-65e9-4491-87f3-1b896c736b9f`).
- Deployment: `7f96df7f-04b6-4fe6-af25-6e6aaec05cfa`.
- URL: https://app-production-58dd.up.railway.app.

Railway reported `SUCCESS`; the production container started at 19:19:35 UTC. Its existing pre-deploy migration command completed before the new app took traffic.

Live smoke checks passed:

- `/health`: HTTP 200, `status: ok`; database, Graph, Hedera, model, Privy and Telegram report live mode.
- `/demo/x402`: HTTP 200 with the new Pond Observatory page.
- `/demo/x402/report`: HTTP 402 with a `payment-required` header; no payment was submitted.
- `/api/purchases` without credentials: HTTP 401.
- Fresh Chromium session at the app URL: HTTP 200, the real sign-in page rendered, and no page or console errors. Screenshot saved locally at `.froggy/release-live-home.png`.

These checks establish a healthy deployed app and unpaid quote discovery. They do not establish a successful live paid search, purchase settlement or upstream provider delivery.

X credentials are configured. Birdeye, Uniswap, trading RPC endpoints and explicit trading prices were unset at release preflight; live trading research remains unavailable until those are configured. No paid search, supplier payment, wallet policy change or trade was performed as part of release verification.
