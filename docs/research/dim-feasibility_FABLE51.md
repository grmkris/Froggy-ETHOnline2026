# feasibility

## Summary

Bottom line: the browser bet is real and works on this machine today; the custody story is where the plan is wrong; the local prior-art repos the plan says to "steal from" do not exist on this box.

Bun.WebView (VERIFIED locally). Bun 1.3.14 is installed; `typeof Bun.WebView === "function"`. I spawned it with `backend: {type:"chrome", path: "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"}` and a persistent `dataStore: {directory}`: it launched Chrome 152.0.7977.76, navigated, answered raw CDP (`Browser.getVersion`, `Target.createTarget` + `Target.attachToTarget`, `Input.dispatchMouseEvent`, `Accessibility.getFullAXTree`), and `Page.startScreencast` delivered JPEG frames via `addEventListener("Page.screencastFrame")` within 1.5 s. Profile dir persisted (4.1 MB). Caveats from the docs and the test: the API is experimental (introduced in 1.3.12); `headless` only supports `true`, so the human never sees a native window (screencast + Input.dispatch is the only human path, which is what ARCHITECTURE.md already assumes, but passkeys/OAuth/captchas inside headless Chrome are a risk); one operation per type per view (`ERR_INVALID_STATE`); open views do not keep the process alive; without an explicit `path` the auto-detect on this Mac picked a stale HeadlessChrome/108 binary, so always pass the path; close()/browser-death unhandled-rejection bugs on the chrome backend were only closed 2026-09-01 (oven-sh/bun #40991/#40992). Fallback if it bites: puppeteer-core/Playwright attached to the same Chrome uses the identical CDP calls, so the screencast/input code is portable.

Privy (VERIFIED from docs.privy.io). Policy engine: allow/deny rules with conditions on `ethereum_transaction` (`to`, `value`, `chain_id`), decoded calldata, typed-data messages, plus recipient/contract/chain allow/deny lists; default deny. Per-tx cap is a `value lte` condition. Daily cap exists only as "stateful policies": rolling windows 3,600–259,200 s, `sum` only, native units, max 10 aggregations per app, and ONLY on `eth_signTransaction` / `eth_signUserOperation`. That means a daily cap works for on-chain sends but NOT for x402 payments (EIP-3009 typed data) — the host must keep its own ledger for that. x402 support is `useX402Fetch` (react-auth ≥3.7.0) and `createX402Client` (`@privy-io/node` + `@x402/fetch`) with `maxValue`, on Base, Base Sepolia, Solana; facilitators Pay AI/Corbits/Coinbase; no Hedera. Delegation: `useSigners().addSigners({address, signers:[{signerId, policyIds}]})` client-side, then `privy.wallets().ethereum().sendTransaction(walletId, {caip2, params})` from the server with an authorization key; revoke = remove signer or change policy. Telegram: OAuth widget login and `linkWithOAuth({provider:'telegram'})`; seamless login is Mini-App-only; server-side `privy.users().create({linked_accounts:[{type:'telegram', telegram_user_id}]})` and `getByTelegramUserID` exist, and Privy ships a "Building a Telegram trading bot" recipe that creates the wallet at /start with the bot's authorization key as an additional signer plus `override_policy_ids` — that is the pairing pattern, ready made. Free tier: 499 MAU, 50k signatures/month, $1M volume. Current npm: @privy-io/react-auth 3.40.0, @privy-io/node 0.34.0.

The custody hole. `@x402/hedera` 2.25.0 (published today, 2026-09-04) requires `createClientHederaSigner(accountId, PrivateKey.fromString(...))` — a raw Hiero key. Privy cannot hand out a key, Hedera is not in Privy's chain-support list, and Privy `rawSign` would need a custom Hiero signer and would blind the policy engine. So "Privy policy is the leash on the Hedera x402 payment" is not true as written. Resolution that keeps all three prizes: the Hedera 402 (required by the Hedera track, settled through Blocky402 testnet facilitator `https://api.testnet.blocky402.com`, no API key, `hedera:testnet`, HBAR `0.0.0` or USDC `0.0.429274` with token association, 100 req/min/IP) is paid from an agent-owned Hedera testnet account with host-enforced caps; the Privy-policied spend is the Graph x402 gateway query on Base Sepolia. The Graph gateway now accepts x402 at `https://testnet.gateway.thegraph.com/api/x402/subgraphs/id/{id}` in USDC on Base Sepolia (`0x036CbD53842c5426634e7929541eC2318f3dCF7e`), which is exactly what Privy's x402 client speaks and what the Graph AI track names ("let your agent pay per query autonomously with x402").

The Graph. Subgraph MCP is hosted at `https://subgraphs.mcp.thegraph.com/sse`, Bearer Gateway API key from Studio, 100k free queries/month; tools: schema, query, discovery by keyword/contract, 30-day volume. Messari standardized lending subgraphs are live on the network (PaulieB14/graph-lending-mcp registry: 90 deployments, 65 live as of 2026-03-06, e.g. aave-v3-ethereum/base/arbitrum LIVE). Agent0 ERC-8004 subgraphs are live on 9 networks including Base Sepolia (not Hedera). graphprotocol/subgraphs-skills is for writing subgraphs, not querying.

Hedera extras. hedera-dev PoC (created 2026-07-13, 5 commits, 2 stars): `packages/service` with 4 payment-gated routes and `packages/agent`; it uses x402.org/facilitator on testnet, not Blocky402 — copy the middleware shape only. HCS message = $0.0008 USD since Jan 2026 (free on testnet). hedera-agent-kit 3.8.2, repo active (pushed 2026-09-03).

WebMCP. Chrome origin trial 149–156 (ends 2026-11-16); Chrome 150 moved to `document.modelContext` (navigator alias). Local Chrome 152 is inside the window, but no mainstream agent consumes it and anthropics/claude-code #30645 (WebMCP in the Claude Chrome extension) was closed "not planned". Only community bridges (WebMCP Bridge extension → Claude Code) exist. Cut it.

Local repos. `find`/`mdfind` for invok, harness, humanhook, boter, starters and the mini-app-host-platform spec: none exist under ~ (only unrelated poker "harness" dirs and ~/bob-harness = metadao-futarchy). The "steal patterns from Invok" plan has nothing to steal from on this machine.

Model. The claude-api skill in this environment says default `claude-opus-5` via `@anthropic-ai/sdk` (0.123.0), adaptive thinking; Vercel AI SDK `ai` 7.0.92 + `@ai-sdk/anthropic` 4.0.49 are fine for `streamText`. Avoid Fable 5.1 for the loop (forced `tool_choice` returns 400).

Critical path and risk: the Chrome slice (screencast → arbitration → snapshot/click → agent tools) is one person's full week; the single biggest technical risk is the two-custody split plus the "watch the 402 unlock in the shared Chrome" moment, which the in-page browser cannot perform because the host pays — the service must return a paid receipt token the agent then navigates to.

## Claims

### [high] conf 0.95: Bun 1.3.14 on this machine ships Bun.WebView with a working Chrome backend: navigate, raw CDP (Target.createTarget/attachToTarget, Input.dispatchMouseEvent, Accessibility.getFullAXTree), Page.startScreencast frames and a persistent dataStore profile all worked against Chrome 152.0.7977.76.

Evidence: VERIFIED locally: `bun --version` = 1.3.14; `typeof Bun.WebView` = function; scratchpad script wv2.ts returned {ver:'Chrome/152.0.7977.76', newTarget, attach sessionId, input:'ok', a11y:15 nodes}; wv.ts got 1 screencast frame in 1.5s. Docs: experimental since 1.3.12, `headless` only true, one op per type per view, `dataStore:{directory}`, `cdp()` chrome-only. Caveat: auto-detect without `path` launched a stale HeadlessChrome/108 binary.

Sources:
- https://bun.com/docs/runtime/webview
- https://bun.com/blog/bun-v1.3.12
- https://github.com/oven-sh/bun/issues/40991

> VERDICT SURVIVES: Independently re-verified on this machine (2026-09-04): `bun --version` = 1.3.14; `typeof Bun.WebView` = "function". Re-ran scratchpad/wv2.ts (explicit path to /Applications/Google Chrome.app, dataStore:{directory:"./profile-test"}): Browser.getVersion = Chrome/152.0.7977.76, Target.createTarget returned a targetId, Target.attachToTarget returned a sessionId, Input.dispatchMouseEvent ok, Accessibility.getFullAXTree returned 15 nodes, and ./profile-test was populated with a Chrome profile (Default/, GPUPersistentCache, etc.). Re-ran scratchpad/wv.ts with BUN_CHROME_PATH set: 1 screencast frame in ~3.4s against Chrome/152.0.7977.76. Auto-detect without `path`/BUN_CHROME_PATH indeed launched HeadlessChrome/108.0.5334.0 (stale binary, likely Playwright cache) — caveat confirmed. `Google Chrome --version` = 152.0.7977.76. Sources: bun.com/docs/runtime/webview confirms "experimental", headless "Only true is implemented; false throws", "One operation of each kind may be in flight at a time", dataStore "ephemeral" | {directory}, cdp() "(Chrome only)", and the Chrome search order (path, BUN_CHROME_PATH, $PATH, standard locations, Playwright cache). bun.com/blog/bun-v1.3.12 (April 9, 2026) confirms WebView shipped in 1.3.12 with WebKit+Chrome backends, cdp(), and dataStore; the blog itself does not use the word "experimental" (the docs page does). Discrepancies: (1) GitHub issue #40991 is NOT about stale-binary auto-detection — it is "Bun.WebView.close() raises an unhandled rejection the caller cannot catch (chrome backend)", opened Aug 31 2026 by sroussey, closed, repro on Bun 1.4.0 + Chrome 151; the stale-108 caveat is a local observation only. (2) That issue shows Bun 1.4.0 exists, so 1.3.14 is not the latest release. (3) "Persistent" dataStore was verified only as an on-disk profile directory being created/populated, not as cookies surviving a second launch. (4) Docs list no specific CDP methods; the specific domains worked empirically only.
> Corrected: Bun 1.3.14 on this machine (not the latest Bun; 1.4.0 exists) exposes Bun.WebView (experimental since v1.3.12, April 2026). With an explicit backend path to Google Chrome 152.0.7977.76 (or BUN_CHROME_PATH), the Chrome backend worked end-to-end: navigate, evaluate, raw CDP (Browser.getVersion, Target.getTargets/createTarget/attachToTarget, Input.dispatchMouseEvent, Accessibility.getFullAXTree returned 15 nodes), Page.startScreencast (1 frame in ~1.5–3.4s), and dataStore:{directory} created a populated Chrome profile on disk (cross-launch cookie persistence not separately tested). Per docs: headless must be true, one in-flight operation per kind per view, cdp() is Chrome-only, and Chrome is located via path > BUN_CHROME_PATH > $PATH > standard installs > Playwright cache. Caveats: auto-detect without a path launches a stale HeadlessChrome/108 binary on this machine (local observation, not from any cited source); GitHub issue #40991 (closed, Aug 2026) documents an unrelated but relevant bug where WebView.close() on the Chrome backend produces an uncatchable unhandledRejection.
> Sources: https://bun.com/docs/runtime/webview, https://bun.com/blog/bun-v1.3.12, https://github.com/oven-sh/bun/issues/40991, /private/tmp/claude-501/-Users-jonas-Documents-web3-agentic-wallet/ace6e5a2-eb35-4d30-b09a-7db65fc3c119/scratchpad/wv.ts, /private/tmp/claude-501/-Users-jonas-Documents-web3-agentic-wallet/ace6e5a2-eb35-4d30-b09a-7db65fc3c119/scratchpad/wv2.ts

> VERDICT SURVIVES: Core claim survives: I re-ran the scratchpad script (wv2.ts) and got the same result (Chrome/152.0.7977.76, createTarget/attach ids, input ok, 15 a11y nodes, 7.2s wall time), so Bun.WebView + Chrome backend + raw cdp() genuinely works here. But several sub-claims are overstated or cherry-picked, verified with new scripts wv3.ts/wv4.ts in the scratchpad:

1. "Target.attachToTarget worked" is cosmetic. Sending Runtime.evaluate with the returned sessionId (as a param or as a 3rd arg) still evaluated in the ORIGINAL page (returned https://example.com/, not the newly created example.org target). Bun's cdp() is bound to the view's own page session and ignores sessionId, so multi-tab/popup driving over CDP is not actually available. The original test only checked that the calls returned ids, not that they were usable.

2. "persistent dataStore profile worked" was only demonstrated as "a directory got created". Real test: localStorage survived a restart (k=v), but a cookie set with max-age=86400 did NOT survive (empty after restart, even with view.close() plus 3s sleep). For a wallet/dApp-login flow that relies on session cookies this matters. Also docs: Chrome dataStore directory is process-wide, first view wins.

3. close() on the Chrome backend produces an uncatchable unhandledRejection "Error: WebView closed" (reproduced; returns undefined, not a promise) — exactly GitHub issue #40991. The original scripts hid this behind process.exit(0).

4. Concurrency: two cdp() calls in flight throw ERR_INVALID_STATE ("a cdp() is already pending") — reproduced. A design that acks screencast frames inside an event listener while the agent issues Input/Accessibility commands will collide unless every cdp() goes through a single serialized queue. Screencast evidence is thin too: 1 frame in 1.5s on a static page is not proof of usable live video.

5. evaluate() is expression-only: statements separated by ';' fail with "Unexpected token ';'" (worked with the comma operator).

6. Docs still mark the API experimental and "may change"; Chrome backend is headless-only (UA reported HeadlessChrome even with no headless flag) so there is no native window for a demo — only screencast; Chrome backend can't run in Workers; startup was ~7s. Web search budget was exhausted so I could not check for Aug–Sep 2026 Bun releases or newer issues; the docs fetched today still say experimental and list no screencast API.

Net: the backend works for a single-tab, serialized, headless automation loop, which is enough for a 9-day hackathon, but the "raw CDP incl. target attach" and "persistent profile" parts should not be relied on as stated, and Playwright/puppeteer-core remain the safer fallback for multi-target or cookie-persistent flows.
> Corrected: Bun 1.3.14 on this Mac can drive Chrome 152 headless via the experimental Bun.WebView Chrome backend: navigate, evaluate (single expressions only), raw cdp() calls on the view's own page (Input.*, Accessibility.getFullAXTree, Page.startScreencast events) all work, with ~7s startup. Limits verified locally: cdp() is strictly one-in-flight per view (ERR_INVALID_STATE otherwise — serialize screencast acks and agent commands through one queue); Target.attachToTarget returns a sessionId but cdp() cannot route commands to it, so only the primary tab is drivable; dataStore persistence keeps localStorage across restarts but cookies were lost; close() on the Chrome backend emits an uncatchable "WebView closed" unhandledRejection (issue #40991) that must be swallowed with a process-level handler; headless-only, main-thread-only, API marked experimental/may change — pin the Bun version and keep puppeteer-core/Playwright as a fallback for multi-tab or cookie-persistent flows.
> Sources: https://bun.com/docs/runtime/webview, https://github.com/oven-sh/bun/issues/40991, https://bun.com/blog/bun-v1.3.12, /private/tmp/claude-501/-Users-jonas-Documents-web3-agentic-wallet/ace6e5a2-eb35-4d30-b09a-7db65fc3c119/scratchpad/wv2.ts, /private/tmp/claude-501/-Users-jonas-Documents-web3-agentic-wallet/ace6e5a2-eb35-4d30-b09a-7db65fc3c119/scratchpad/wv3.ts, /private/tmp/claude-501/-Users-jonas-Documents-web3-agentic-wallet/ace6e5a2-eb35-4d30-b09a-7db65fc3c119/scratchpad/wv4.ts

### [medium] conf 0.9: The agent Chrome will be headless-only (Bun.WebView implements headless:true only); the human sees it solely through the screencast and CDP Input, so passkey/OAuth/captcha flows on real sites are a demo risk.

Evidence: Bun docs: 'headless: Only true is implemented'. Local UA string reported HeadlessChrome/152.0.0.0 even with the full Google Chrome binary.

Sources:
- https://bun.com/docs/runtime/webview

### [high] conf 0.92: Privy policies give per-tx value caps, recipient/contract/chain allowlists and default-deny; rolling aggregate caps exist (1h–72h windows, sum only, native units, max 10 per app) but apply ONLY to eth_signTransaction and eth_signUserOperation — not to x402 typed-data payments.

Evidence: docs.privy.io policies overview lists conditions/operators and 'historical aggregates'; stateful-policies page: 'Only two RPC methods are supported: eth_signTransaction, eth_signUserOperation', rolling windows 3600–259200s, sum only, native units.

Sources:
- https://docs.privy.io/controls/policies/overview
- https://docs.privy.io/controls/policies/stateful-policies.md

> VERDICT SURVIVES: Both cited sources were fetched (Sep 2026) and support the claim. Overview page (docs.privy.io/controls/policies/overview): conditions on ethereum_transaction fields "to", "value", "chain_id", ethereum_calldata "function_name", operators eq/neq/lt/lte/gt/gte/in/in_condition_set/etc.; "If no rules resolve, the policy will default to 'DENY'"; a "reference" field source for "an Aggregation for stateful policies" that can "evaluate against historical data such as cumulative transaction values over a time window", "Only supported for Ethereum methods". Policies in general do cover eth_signTypedData_v4 (via ethereum_typed_data_domain/ethereum_typed_data_message field sources) — so stateless per-signature rules CAN be applied to x402 typed-data, but aggregates cannot. Stateful-policies page: "Aggregations can be configured for the following RPC methods: eth_signTransaction (Ethereum) and eth_signUserOperation (Ethereum)"; "Minimum value is 3600 (1 hour), and maximum is 259200 (72 hours)"; aggregation_function "Currently, only 'sum' is supported"; "Each app can have a maximum of 10 aggregations"; values in native units (e.g. 10 ETH in wei '0x8AC7230489E80000', "1000 USDC (1000e6)"). Two caveats: (1) the evidence quote "Only two RPC methods are supported" is a paraphrase, not verbatim; (2) the docs never mention x402 or eth_signTypedData_v4 in the aggregation context — the x402 exclusion is an inference from the exhaustive two-method list (x402 EVM payments are EIP-3009 typed-data signatures), which is sound but not stated by Privy. Also notable for the team: docs warn aggregation values are updated only after a request is signed, so concurrent requests can all pass before any is recorded (race window). WebSearch budget was exhausted, so no additional replacement sources were sought; the cited primary sources were reachable.
> Corrected: Privy policies support per-transaction value caps (ethereum_transaction "value"), recipient allowlists ("to"), contract/function allowlists (ethereum_calldata "function_name"), chain restrictions ("chain_id"), and default-deny when no rule matches; stateless rules also apply to eth_signTypedData_v4 via typed-data domain/message field sources. Stateful (rolling aggregate) caps exist — rolling windows of 3600–259200 s (1h–72h), 'sum' as the only aggregation function, values in native units (wei / token base units), max 10 aggregations per app — and per Privy's docs aggregations "can be configured for the following RPC methods: eth_signTransaction and eth_signUserOperation" only. Because x402 EVM payments are EIP-3009 typed-data signatures (eth_signTypedData_v4), rolling aggregate caps do not cover them (an inference; the docs do not mention x402 explicitly). Note also that aggregates are updated only after signing, so concurrent requests can exceed the cap.
> Sources: https://docs.privy.io/controls/policies/overview, https://docs.privy.io/controls/policies/stateful-policies, https://docs.privy.io/controls/policies/stateful-policies.md

> VERDICT SURVIVES: Re-checked the live Privy docs (Sept 2026). The stateful-policies page still says aggregations are available only for eth_signTransaction and eth_signUserOperation (Ethereum only), windows 3600–259200s, only 'sum', max 10 aggregations per app. The policies overview lists eth_signTypedData_v4 as a policy-able method with ethereum_typed_data_domain / ethereum_typed_data_message field sources and default DENY. Privy's own x402 recipe confirms x402 payments are signed as EIP-712 typed data and that "transaction policies don't apply to signed typed data"; it recommends typed-data recipient screening plus a client-side `maxValue` cap. So the core assertion survives. Two inaccuracies / cherry-picks found: (1) "native units" is wrong/misleading — aggregates are in raw smallest units (wei, or token decimals) and can be computed over decoded ERC-20 calldata amounts (e.g. sum of USDC transfer.amount), not just native ETH value; (2) the claim omits that even where aggregates apply they are explicitly "disaster prevention" — values are updated AFTER signing, so concurrent requests can all pass before any is recorded; Privy itself recommends app-level rate limiting. Strongest counter-argument: for a 9-day hackathon the gap is nearly irrelevant — per-payment caps on x402 are available both in policy (typed-data message.value comparisons, domain.verifyingContract/chainId, message.to allowlist) and client-side (`maxValue` in the x402 fetch wrapper), and a rolling USDC cap over typed-data payments is a few lines of app-side accounting that would be needed anyway because Privy's aggregates are racy. Alternative routing (submitting the EIP-3009 authorization yourself via eth_signTransaction to get aggregates) defeats x402's facilitator-pays-gas model and isn't worth it. Web search budget was exhausted this session, so no Aug–Sep 2026 changelog check beyond the live doc pages was possible.
> Corrected: Privy policies (default DENY) support per-request value caps and recipient/contract/chain allowlists for both transactions and EIP-712 typed data (eth_signTypedData_v4 via ethereum_typed_data_domain / ethereum_typed_data_message fields). Rolling aggregate ("stateful") caps exist — 1h–72h windows, sum only, max 10 per app, values in raw smallest units (wei or token decimals), computable over ethereum_transaction.value or decoded ERC-20 calldata amounts — but only for eth_signTransaction and eth_signUserOperation, so they do not cover x402 typed-data payments. Even where they apply they are best-effort (updated after signing; concurrent requests can exceed the cap). For x402 in a hackathon build: enforce per-payment caps via typed-data policy rules and/or the x402 wrapper's `maxValue`, allowlist recipients/verifyingContract/chainId in policy, and implement any rolling USDC budget in the agent/app layer.
> Sources: https://docs.privy.io/controls/policies/stateful-policies.md, https://docs.privy.io/controls/policies/overview.md, https://docs.privy.io/recipes/x402

### [high] conf 0.9: Privy x402 support (useX402Fetch in react-auth ≥3.7.0, createX402Client in @privy-io/node + @x402/fetch, maxValue) covers Base, Base Sepolia and Solana only; per-payment recipient screening via ethereum_typed_data_message TransferWithAuthorization fields is documented, Hedera is not.

Evidence: docs.privy.io/recipes/x402 lists networks and facilitators (Pay AI, Corbits, Coinbase); sanctions-screening recipe shows field_source 'ethereum_typed_data_message', typed_data 'TransferWithAuthorization', fields 'to' and 'value'.

Sources:
- https://docs.privy.io/recipes/x402
- https://docs.privy.io/recipes/agent-integrations/x402-sanctions-screening.md

> VERDICT REFUTED: Both cited sources are reachable (fetched 2026-09-04) and support most of the claim: docs.privy.io/recipes/x402 states "The useX402Fetch hook is built into @privy-io/react-auth (v3.7.0+)", shows createX402Client with @privy-io/node and @x402/fetch, shows "maxValue: BigInt(1000000) // Max 1 USDC (6 decimals)", names facilitators Pay AI, Corbits and Coinbase, and says "To restrict who an agent or user can pay, attach a wallet policy that screens the recipient in the signing request." The sanctions-screening recipe shows a rule with field_source "ethereum_typed_data_message", typed_data primary type TransferWithAuthorization, field "to" with operator in_condition_set (and also rules on "value"/"from", ethereum_typed_data_domain, and solana_token_program_instruction). Neither page mentions Hedera. However, the "Base, Base Sepolia and Solana ONLY" part is not supported as stated: the x402 recipe phrases networks as an example ("on the correct network (e.g. Base, Base Sepolia, or Solana)"), not an exhaustive list, and the sanctions-screening recipe explicitly states it "covers Base and Tempo (EVM) and Solana" with guidance "For Tempo, keep the same rule shape and set the domain conditions to Tempo's chain ID and USDC contract." So Privy's x402 docs also document Tempo, and no exhaustive network list is given. The word "only" must be dropped and Tempo added. (Web search budget was exhausted, so no additional Hedera search could be run; the absence-of-Hedera statement rests on the two cited pages.)
> Corrected: Privy's x402 recipe (useX402Fetch in @privy-io/react-auth v3.7.0+, createX402Client in @privy-io/node with @x402/fetch, maxValue option; facilitators Pay AI, Corbits, Coinbase) names Base, Base Sepolia and Solana as example networks (not an exhaustive list), and the companion sanctions-screening recipe additionally covers Tempo (EVM). Per-payment recipient screening via an eth_signTypedData_v4 policy with field_source "ethereum_typed_data_message" on TransferWithAuthorization fields ("to", "value", "from") is documented, alongside Solana SPL-transfer rules. Hedera is not mentioned in either page.
> Sources: https://docs.privy.io/recipes/x402, https://docs.privy.io/recipes/x402.md, https://docs.privy.io/recipes/agent-integrations/x402-sanctions-screening.md

> VERDICT REFUTED: The core facts survive (packages/versions, maxValue, the screening recipe, no Hedera mention anywhere in Privy or x402.org docs), but the word "only" is a clear overstatement contradicted by Privy's own documentation. (1) docs.privy.io/recipes/x402 says users need USDC "on the correct network (e.g. Base, Base Sepolia, or Solana)" — an illustrative "e.g.", not an exhaustive list; the recipe nowhere states these are the only networks. (2) Privy's own sanctions-screening recipe explicitly covers "Base and Tempo (EVM)" plus Solana, so Tempo is a documented x402 network beyond the claimed three. (3) createX402Client "infers chain type from address" and rides on the chain-agnostic @x402 stack (x402.org: "supports all EVM-compatible chains, Solana, and more"; coinbase/x402 ships @x402/evm, @x402/svm, @x402/stellar) — the binding constraint is which network your chosen facilitator (Coinbase CDP, PayAI, Corbits) will verify/settle, not a Privy-side network whitelist. So "Base/Base Sepolia/Solana only" should read "documented examples; actual coverage = any EVM/SVM network your facilitator settles". Minor cherry-pick: the screening DENY rule screens only the `to` field; `value` appears only as a trivially-true (>= 0) check in the ALLOW rule, so "fields 'to' and 'value'" overstates what is screened. The Hedera half stands: no Privy, x402.org, or coinbase/x402 page mentions Hedera, and hedera-x402 / Hedera x402 docs URLs I tried returned 404 (WebSearch budget was exhausted, so I could not confirm or rule out a third-party Hedera facilitator). Hackathon relevance: for a 9-day build, don't plan on Privy x402 → Hedera; Hedera USDC is an HTS token whose EVM facade is unlikely to expose EIP-3009 TransferWithAuthorization, and you would need a Hedera-settling facilitator plus a custom scheme — out of scope. Stick to Base Sepolia (Coinbase facilitator, free testnet) and treat any other EVM chain as facilitator-dependent.
> Corrected: Privy's x402 integration (useX402Fetch in @privy-io/react-auth ≥3.7.0; createX402Client in @privy-io/node with @x402/fetch; maxValue cap) is documented with Base, Base Sepolia and Solana as examples ("e.g."), and Privy's sanctions-screening recipe additionally covers Tempo; actual network coverage is facilitator-dependent (Coinbase CDP, PayAI, Corbits) on the chain-agnostic x402 stack rather than a fixed Privy whitelist. Per-payment recipient screening is documented via a policy rule with field_source 'ethereum_typed_data_message', typed_data TransferWithAuthorization, screening the 'to' field (value is only a trivially-true allow check), plus Solana Transfer/TransferChecked.destination rules. Hedera is not mentioned in any Privy or x402 documentation and should be treated as unsupported for a 9-day build.
> Sources: https://docs.privy.io/recipes/x402, https://docs.privy.io/recipes/x402.md, https://docs.privy.io/recipes/agent-integrations/x402-sanctions-screening, https://docs.privy.io/recipes/agent-integrations/x402-sanctions-screening.md, https://github.com/coinbase/x402, https://x402.org/

### [high] conf 0.9: @x402/hedera (2.25.0, published 2026-09-04) requires a raw Hiero PrivateKey for the client signer, so the Hedera x402 payment cannot be signed by a Privy embedded wallet under Privy policy; Hedera is absent from Privy's chain-support list.

Evidence: Package README: `createClientHederaSigner('0.0.1111', PrivateKey.fromString(process.env.HEDERA_PRIVATE_KEY!), {network:'hedera:testnet'})`; USDC testnet 0.0.429274 requires TokenAssociateTransaction; Privy chains page does not list Hedera; Privy rawSign exists but docs do not mention policy coverage.

Sources:
- https://www.npmjs.com/package/@x402/hedera
- https://docs.privy.io/wallets/overview/chains.md
- https://docs.privy.io/wallets/using-wallets/other-chains/index.md

> VERDICT REFUTED: Verified from primary sources (npm registry + unpacked tarball of @x402/hedera@2.25.0; Privy docs). SUPPORTED: version 2.25.0 is `latest`, published 2026-09-04T00:07Z. The README example is exactly `createClientHederaSigner("0.0.1111", PrivateKey.fromString(process.env.HEDERA_PRIVATE_KEY!), { network: "hedera:testnet" })`, and the .d.ts signature is `createClientHederaSigner(accountId: string, privateKey: PrivateKey, config?)`. README documents testnet USDC 0.0.429274 and that HTS tokens need TokenAssociateTransaction (or maxAutomaticTokenAssociations). Privy's chains page lists Ethereum, Solana, Tempo (Tier 3), Sui/Tron/Stellar/Aptos/Near (Tier 2), Bitcoin/Cosmos/Ton/Starknet/Pearl + "Other Ed25519 and secp256k1 chains" (Tier 1, raw sign); Hedera is not named. Privy's policies overview lists chain_type only as ethereum|solana|tron|sui|xrpl, and the chains page says transaction policies ("controls based on decoded transaction fields") are unavailable below Tier 2, and Tier 1 raw signing is the only route for an unlisted chain. So "no Privy policy enforcement for Hedera" is supported. OVERSTATED: the package does NOT require a raw PrivateKey for the client signer. `ExactHederaScheme` (exact/client) takes the `ClientHederaSigner` interface `{ accountId: string; createPartiallySignedTransferTransaction(requirements): Promise<string> }`; `createClientHederaSigner` is only the default SDK-backed helper (it builds a TransferTransaction, freezeWith(client), then `tx.sign(privateKey)`). The Hiero SDK (2.87.0) exposes `Transaction.signWith(publicKey, (bodyBytes) => signature)` for external signers, and Privy rawSign accepts hex bytes with keccak256/sha256 hashing or a precomputed hash, so a custom ClientHederaSigner backed by Privy raw signing is architecturally possible (not tested here; ed25519 raw-bytes signing and Hedera's exact ECDSA hashing were not verified against Privy). The causal "so ... cannot be signed by a Privy embedded wallet" therefore does not follow from the package; what does hold is that any such signing would be Tier 1 raw signing outside Privy policy coverage.
> Corrected: @x402/hedera 2.25.0 (npm `latest`, published 2026-09-04) ships only a PrivateKey-based default client signer (`createClientHederaSigner(accountId, PrivateKey, config)`), but its `ExactHederaScheme` accepts any object implementing the `ClientHederaSigner` interface (`accountId` + `createPartiallySignedTransferTransaction`), so a custom signer is possible in principle. Hedera is not listed on Privy's supported-chains page; it could only be reached via Privy's Tier 1 "Other Ed25519 and secp256k1 chains" raw signing, and Privy policies (chain_type limited to ethereum/solana/tron/sui/xrpl; transaction policies require Tier 2+) would not apply to such raw-signed Hedera payments. Testnet USDC 0.0.429274 requires token association (TokenAssociateTransaction or maxAutomaticTokenAssociations).
> Sources: https://www.npmjs.com/package/@x402/hedera (npm registry: version 2.25.0, time 2026-09-04T00:07:21Z; tarball README.md, dist/cjs/signer-DCjZrivX.d.ts, dist/esm/index.mjs), @hiero-ledger/sdk 2.87.0 tarball: src/transaction/Transaction.js (sign/signWith), src/PrivateKey.js, https://docs.privy.io/wallets/overview/chains.md, https://docs.privy.io/wallets/using-wallets/other-chains/index.md, https://docs.privy.io/wallets/using-wallets/other-chains/raw-sign.md, https://docs.privy.io/controls/policies/overview.md

> VERDICT REFUTED: The load-bearing premise is wrong. I unpacked @x402/hedera@2.25.0 (modified 2026-09-04T00:07Z) and read dist/cjs/signer-*.d.ts: `ClientHederaSigner` is a plain structural type `{ readonly accountId: string; createPartiallySignedTransferTransaction(requirements): Promise<string /*base64 tx bytes*/> }`, and `ExactHederaScheme(signer: ClientHederaSigner)` accepts any object matching it. `createClientHederaSigner(accountId, privateKey: PrivateKey, cfg)` is only the default convenience helper (its body: build TransferTransaction, setTransactionId(feePayer), freezeWith(client), tx.sign(privateKey), toBytes → base64). Nothing forces a PrivateKey; the README example is just the simplest path. The Hiero SDK (2.87.0) exposes `Transaction.signWith(publicKey, async (bytes) => Uint8Array)` and `addSignature(publicKey, sig)`, so an external signer can be plugged in. Hiero ECDSA signing is keccak256(bodyBytes) → secp256k1 (see @hiero-ledger/cryptography primitive/ecdsa.js), which maps directly onto Privy `rawSign({ bytes, encoding:'hex', hash_function:'keccak256' })` on a secp256k1 (Ethereum) embedded wallet (strip the recovery byte to get r||s). Hedera accounts can be auto-created from that key via EVM-address alias, the account key becomes the ECDSA key, and the package's facilitator verifier handles `ECDSA_SECP256K1` mirror-node keys. Also, Privy's chains page is not silent on Hedera: Tier 1 explicitly includes "Other Ed25519 and secp256k1 chains — Any chain on a supported curve". So "Hedera is absent" is technically true by name but misleading; it is covered generically. What SURVIVES: the policy part. Privy's policy method lists (ethereum: eth_sendTransaction, eth_signTransaction, eth_signTypedData_v4, personal_sign, …; signRawMessageBytes/signTransactionBytes only for Tron/Sui) do not include rawSign for Ethereum-type wallets, the chains page says transaction policies need Tier 2+ because Privy must decode the tx, and policies default-deny methods without rules. So a Privy wallet CAN produce a valid Hedera payer signature, but Privy cannot enforce Hedera-aware policy (amount/recipient/token) on it, and whether rawSign is even permitted when a policy is attached is undocumented (unverified; may require a '*' allow rule or a policy-less wallet). Hackathon note: the workaround is real but unexercised—no example exists in the coinbase/x402 repo for a browser/external Hedera signer; you'd also need TokenAssociateTransaction signed via the same rawSign path, must use the SDK instance re-exported by @x402/hedera (README warns about duplicate SDK installs), and the 64/65-byte signature handling is a detail to test. Search budget was exhausted; I could not check Privy's Jul–Sep 2026 changelog for a Hedera or rawSign-policy addition (changelog.md fetch returned 404).
> Corrected: @x402/hedera 2.25.0 does not require a Hiero PrivateKey: `ClientHederaSigner` is a two-member structural interface, and a custom implementation can sign the frozen TransferTransaction with any secp256k1 signer via `Transaction.signWith`, including a Privy Ethereum embedded wallet's `rawSign` (keccak256 hash) paired with a Hedera EVM-alias account. Privy lists Hedera implicitly under Tier 1 ("other secp256k1/Ed25519 chains"). The genuine limitation is narrower: Privy's policy engine cannot evaluate Hedera transactions (policies need Tier 2 decoding; rawSign is not a listed policy method for Ethereum wallets, and default-deny may block it when a policy is attached), so a Privy-signed Hedera x402 payment would be feasible but not policy-governed, and the path is undocumented/untested in the x402 repo — a meaningful integration risk for a 9-day build, not an impossibility.
> Sources: https://registry.npmjs.org/@x402/hedera/-/hedera-2.25.0.tgz (dist/cjs/signer-DCjZrivX.d.ts, dist/cjs/index.js createClientHederaSigner, README.md), https://registry.npmjs.org/@hiero-ledger/sdk (2.87.0, lib/transaction/Transaction.d.ts: signWith/addSignature), https://registry.npmjs.org/@hiero-ledger/cryptography (1.21.0, src/primitive/ecdsa.js keccak256 pre-hash), https://docs.privy.io/wallets/overview/chains.md (Tier 1: 'Other Ed25519 and secp256k1 chains — Any chain on a supported curve'; policies require Tier 2), https://docs.privy.io/wallets/using-wallets/other-chains/index.md (rawSign: hash or bytes+encoding+hash_function keccak256/sha256), https://docs.privy.io/api-reference/wallets/raw-sign.md, https://docs.privy.io/controls/policies/overview.md (method lists per chain_type; default DENY for methods without rules), https://docs.privy.io/wallets/wallets/create/create-a-wallet.md (chain_type enum, no 'hedera'), https://docs.hedera.com/hedera/core-concepts/accounts/auto-account-creation (EVM-address alias from ECDSA key; account key = ECDSA key), https://docs.hedera.com/hedera/core-concepts/accounts/account-properties

### [high] conf 0.9: Blocky402 hosted testnet facilitator is live with open access: https://api.testnet.blocky402.com, network id hedera:testnet, HBAR (asset 0.0.0) and HTS tokens, fee-payer 0.0.7162784, 100 req/min per IP; mainnet https://api.blocky402.com is Hedera-only.

Evidence: blocky402.com landing, /docs/quickstart/, /docs/networks/, /docs/testnet/ fetched and read.

Sources:
- https://blocky402.com/
- https://blocky402.com/docs/quickstart/
- https://blocky402.com/docs/networks/
- https://blocky402.com/docs/testnet/

> VERDICT SURVIVES: All four cited pages were fetched and support the claim. Landing page: "Testnet MVP Ready - Open Access, No API Key Required"; hosted testnet covers Polygon Amoy, Solana Devnet, Hedera Testnet; hosted mainnet is "Hedera Mainnet". /docs/quickstart/: hosted testnet URL https://api.testnet.blocky402.com, "open access (no API key required)", Hedera network hedera:testnet with fee-payer 0.0.7162784, HBAR asset ID 0.0.0, HTS tokens supported. /docs/networks/: testnet https://api.testnet.blocky402.com, mainnet https://api.blocky402.com; Hedera Testnet "Operational" with "HBAR (native), HTS tokens"; hosted mainnet lists only Hedera Mainnet (hedera:mainnet) "Operational (hosted)", other mainnets self-host only. /docs/testnet/: "100 requests per minute per IP" with 10-request burst, "No API key required". Live verification on 2026-09-04: GET https://api.testnet.blocky402.com/supported returned 200 with hedera:testnet, extra.feePayer 0.0.7162784, signers hedera:* [0.0.7162784] (plus eip155:80002 and Solana devnet); GET https://api.blocky402.com/supported returned 200 with only hedera:mainnet (feePayer 0.0.10571514). Minor caveats: the quickstart page still says "Mainnet support is coming soon" in one spot, contradicted by the networks page and the live mainnet endpoint; the testnet page example uses a placeholder feePayer 0.0.8011510 but tells users to read it from /supported, which advertises 0.0.7162784. Docs note the authoritative list is whatever /supported returns, so the fee-payer may change.
> Corrected: Blocky402 hosted testnet facilitator is live with open access (no API key): https://api.testnet.blocky402.com, network id hedera:testnet (also Polygon Amoy and Solana Devnet), HBAR (asset 0.0.0) and HTS tokens, fee-payer 0.0.7162784 (advertised via /supported; verified live 2026-09-04), rate limit 100 req/min per IP (burst 10); hosted mainnet https://api.blocky402.com serves only hedera:mainnet (live fee-payer 0.0.10571514), other mainnets self-host only.
> Sources: https://blocky402.com/, https://blocky402.com/docs/quickstart/, https://blocky402.com/docs/networks/, https://blocky402.com/docs/testnet/, https://api.testnet.blocky402.com/supported, https://api.blocky402.com/supported

> VERDICT SURVIVES: Independently verified live on 2026-09-04 (not just docs): GET https://api.testnet.blocky402.com/health -> 200 {"status":"ok","version":"1.0.0"}; GET /supported -> 200 listing x402Version 2, scheme "exact", network "hedera:testnet" with extra.feePayer "0.0.7162784", and signers hedera:* = ["0.0.7162784"]. No API key needed: 15 rapid unauthenticated requests all returned 200; POST /verify and /settle with empty bodies return a normal 400 "Payment requirements are required" (not 401/403). Hedera mirror node confirms 0.0.7162784 exists, is funded (~290,666 HBAR) and holds an HTS token. GET https://api.blocky402.com/supported -> 200 with only "hedera:mainnet" (feePayer 0.0.10571514), so the hosted mainnet is Hedera-only as claimed. Counter-evidence found, but it weakens rather than refutes: (1) the docs are internally inconsistent — /docs/testnet/ shows an example Hedera fee payer 0.0.8011510 and says mainnet is "Coming soon" and "will require API key authentication", while /docs/networks/ and the live API show 0.0.7162784 and a live, open mainnet; so docs are partly stale and a hackathon build must read feePayer dynamically from /supported rather than hard-code it. (2) The "100 req/min per IP" figure appears only in /docs/testnet/ (with a burst allowance of 10 and counters reset on server restart); the API exposes no RateLimit headers and 15 rapid calls were not throttled, so the limit is unverified and may differ from documented. (3) "HBAR and HTS tokens" comes from docs text; /supported does not enumerate assets, so HTS-token settlement is unverified end-to-end. (4) The testnet facilitator is not Hedera-only (also eip155:80002 and Solana devnet) — irrelevant to the claim but worth knowing. (5) Operational caveat for a 9-day hackathon: this is a small third-party hosted service with no SLA, version 1.0.0; fee-payer accounts have already changed once per the docs, and the docs mention self-hosting (localhost:3002) as a fallback. No Aug–Sep 2026 developments contradicting the claim were found (web search budget was exhausted, so this is limited to direct probing of the service and its docs).
> Corrected: As of 2026-09-04 the Blocky402 hosted testnet facilitator (https://api.testnet.blocky402.com) is live and open (no API key): /supported advertises x402 v2 scheme "exact" on hedera:testnet with feePayer 0.0.7162784 (also Polygon Amoy and Solana devnet), and /verify and /settle accept unauthenticated requests. The hosted mainnet (https://api.blocky402.com) is currently Hedera-only (hedera:mainnet, feePayer 0.0.10571514) and open, despite /docs/testnet/ still saying mainnet is "coming soon" and will need an API key. The 100 req/min per IP (burst 10) limit is documented but not exposed in headers and could not be confirmed; HTS-token (non-HBAR) settlement is documented but not verified via the API. For the hackathon: fetch feePayer from /supported at runtime rather than hard-coding it, treat the docs as partially stale, and keep the self-hosted facilitator (localhost:3002) as a fallback since the hosted service has no SLA.
> Sources: https://api.testnet.blocky402.com/supported, https://api.testnet.blocky402.com/health, https://api.blocky402.com/supported, https://api.blocky402.com/health, https://blocky402.com/docs/testnet/, https://blocky402.com/docs/networks/, https://testnet.mirrornode.hedera.com/api/v1/accounts/0.0.7162784

### [high] conf 0.85: The Graph gateway accepts x402 per-query payments in USDC on Base Sepolia (testnet) and Base (mainnet) via @graphprotocol/client-x402, which is exactly the network/asset Privy's x402 client supports — the natural place for the Privy-policied spend.

Evidence: thegraph.com x402-payments doc: testnet URL https://testnet.gateway.thegraph.com/api/x402/subgraphs/id/{id}, USDC 0x036CbD53842c5426634e7929541eC2318f3dCF7e on Base Sepolia; prizes.md line 130: 'let your agent pay per query autonomously with x402'.

Sources:
- https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/
- https://thegraph.com/blog/graphtally-micropayments-machine-economy/

> VERDICT REFUTED: Mostly supported, but one load-bearing part is wrong. SUPPORTED: The Graph docs page (fetched 2026-09-04) lists Base mainnet (gateway.thegraph.com, USDC 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913) and Base Sepolia (testnet.gateway.thegraph.com, USDC 0x036CbD53842c5426634e7929541eC2318f3dCF7e) under /api/x402/..., with no 'coming soon' language; The Graph blog (2026-07-01) confirms 'Subgraphs on The Graph Network now accept x402 payments directly ... per-query access in USDC over HTTP'. npm registry: @graphprotocol/client-x402 v1.0.0 exists (created 2026-04-14, modified 2026-08-31), chains base (eip155:8453) and base-sepolia (eip155:84532), built on @x402/fetch + @x402/evm. Privy docs (docs.privy.io/recipes/x402) confirm x402 support via useX402Fetch (@privy-io/react-auth v3.7.0+) and @privy-io/node + @x402/fetch, with 'USDC in their Privy embedded wallet on the correct network (e.g. Base, Base Sepolia, or Solana)'. prizes.md line 130 quote verified. NOT SUPPORTED: 'via @graphprotocol/client-x402 ... the Privy-policied spend'. Inspecting the published package: createGraphQuery only accepts a raw `privateKey` (or X402_PRIVATE_KEY env) and calls viem's privateKeyToAccount -> registerExactEvmScheme(client, {signer: account}); there is no way to inject a Privy wallet/signer. So a Privy embedded wallet cannot pay through client-x402; the Privy path must use @x402/fetch (or useX402Fetch) directly against the gateway's /api/x402/subgraphs/id/{id} endpoint. Also note from Privy docs: x402 payments are EIP-712 signatures, so an eth_sendTransaction policy does NOT cover them; the spend policy must be a signing policy that screens the recipient, plus maxValue caps on the client. Not independently verified: exact Privy 'supported networks' list beyond the doc's 'e.g.' phrasing; web search budget was exhausted.
> Corrected: The Graph gateway accepts x402 per-query payments in USDC on Base mainnet (https://gateway.thegraph.com/api/x402/subgraphs/id/{id}, USDC 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913) and Base Sepolia (https://testnet.gateway.thegraph.com/api/x402/subgraphs/id/{id}, USDC 0x036CbD53842c5426634e7929541eC2318f3dCF7e), and Privy's x402 support (useX402Fetch in @privy-io/react-auth >=3.7.0, or @privy-io/node with @x402/fetch) covers USDC on Base and Base Sepolia. However, @graphprotocol/client-x402 (v1.0.0) only signs with a raw private key (privateKeyToAccount) and cannot use a Privy wallet; to make this the Privy-policied spend, call the gateway's x402 endpoint via @x402/fetch / useX402Fetch with the Privy signer, and enforce the policy as an EIP-712 signing policy (recipient screening) plus maxValue, since eth_sendTransaction policies do not cover x402 payments.
> Sources: https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/, https://thegraph.com/blog/graphtally-micropayments-machine-economy/, https://registry.npmjs.org/@graphprotocol%2Fclient-x402 (v1.0.0 tarball: esm/createGraphQuery.js, typings/chains.d.ts), https://docs.privy.io/recipes/x402, /Users/jonas/Documents/web3/agentic-wallet/prizes.md (line ~130)

> VERDICT REFUTED: The network/asset half survives: The Graph's x402 doc lists Base (USDC 0x8335...2913, gateway.thegraph.com) and Base Sepolia (USDC 0x036C...CF7e, testnet.gateway.thegraph.com), and Privy's x402 recipe lists Base, Base Sepolia (and Solana) with USDC only — so the overlap is real. The flaw is the conjunction "via @graphprotocol/client-x402 ... Privy-policied": the npm registry metadata and README for @graphprotocol/client-x402 (v1.0.0, published 2026-04-14, the only release) show the client takes ONLY a raw private key (X402_PRIVATE_KEY env var / `privateKey` option / `config.x402PrivateKey`); there is no documented way to inject a viem Account, a Privy wallet, or any external signer. A raw key on the host is the opposite of a Privy-policied spend. To keep Privy policy in the loop you must bypass that package and hit the gateway's x402 URL (https://testnet.gateway.thegraph.com/api/x402/subgraphs/id/{id}) with Privy's own client (`createX402Client` from `@privy-io/node/x402` wrapped by `@x402/fetch`'s wrapFetchWithPayment). That is plausible because both sides use the @x402/* v2 packages (client-x402 depends on @x402/evm and @x402/fetch ^2.8.0), but it is unverified end-to-end and the Graph doc gives no price, payTo, facilitator, or raw-402 example to check against. Further caveats: (1) Privy's recipe states x402 payments are EIP-712 typed data, so an `eth_sendTransaction` per-tx/daily USDC cap (ARCHITECTURE.md line 155) does NOT cover them; you need an `eth_signTypedData_v4` policy with `ethereum_typed_data_domain`/`ethereum_typed_data_message` conditions (recipient/value on the EIP-3009 message), which is more fiddly and ARCHITECTURE.md currently assumes the wrong policy type. (2) Privy's `signatureOptions: {type: 'erc1271'}` for gas-sponsored smart wallets may not be accepted by The Graph's facilitator — use a plain EOA embedded wallet. (3) The Graph prize rubric (PLAN.md line 154) rewards Graph being load-bearing via reasoning over standardized data, not paying for it; paying per query is a Privy "financial flow" nicety, not a Graph qualifier, and the demo already has an x402 spend on Hedera (PLAN.md line 206), so a second x402 flow on Base is extra scope in nine days. (4) The doc's package is a single 1.0.0 release with no follow-up in five months — thin, low-support surface. No Aug–Sep 2026 developments contradicting the gateway's Base/Base Sepolia support were found (web search budget exhausted; findings rest on direct fetches of the docs, npm registry, and README).
> Corrected: The Graph gateway exposes x402-paid subgraph endpoints on Base (USDC 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913) and Base Sepolia (USDC 0x036CbD53842c5426634e7929541eC2318f3dCF7e), and Privy's x402 client supports USDC on Base/Base Sepolia, so a Privy-policied per-query Graph spend is feasible — but NOT via @graphprotocol/client-x402, which only accepts a raw private key. The policied path is Privy's `createX402Client` + `@x402/fetch` pointed directly at https://testnet.gateway.thegraph.com/api/x402/subgraphs/id/{id}, with a Privy policy on `eth_signTypedData_v4` (typed-data domain/message conditions), not on `eth_sendTransaction`. This interop is unverified end-to-end and is optional for the Graph prize; treat it as a stretch goal, keep the API-key path as the default, and prove the x402-under-policy story on the Hedera service already planned.
> Sources: https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/, https://registry.npmjs.org/@graphprotocol/client-x402, https://raw.githubusercontent.com/graphprotocol/graph-client/main/packages/x402/README.md, https://docs.privy.io/recipes/x402, https://docs.privy.io/controls/policies/overview, https://docs.privy.io/wallets/overview/solutions/agent-wallets, https://thegraph.com/blog/graphtally-micropayments-machine-economy/, /Users/jonas/Documents/web3/agentic-wallet/ARCHITECTURE.md (lines 155, 231), /Users/jonas/Documents/web3/agentic-wallet/PLAN.md (lines 154, 206)

### [medium] conf 0.85: Subgraph MCP is hosted at https://subgraphs.mcp.thegraph.com/sse behind a Bearer Gateway API key from Subgraph Studio (100k free queries/month); Messari standardized lending subgraphs are live on the decentralized network (aave-v3 on ethereum/base/arbitrum LIVE per the graph-lending-mcp registry, 65/90 live as of 2026-03-06); Agent0 ERC-8004 subgraphs are live on 9 networks incl. Base Sepolia but not Hedera.

Evidence: Graph docs Claude config JSON; Studio intro '100,000 free monthly queries'; PaulieB14/graph-lending-mcp SUBGRAPHS.md; Agent0 docs page.

Sources:
- https://thegraph.com/docs/en/subgraphs/tooling/subgraph-mcp/claude/
- https://thegraph.com/docs/en/subgraphs/providers/subgraph-studio/introduction/
- https://raw.githubusercontent.com/PaulieB14/graph-lending-mcp/main/SUBGRAPHS.md
- https://thegraph.com/docs/en/subgraphs/existing-subgraphs/agent0/

### [medium] conf 0.88: Privy ships a Telegram-bot recipe that already implements the pairing pattern: server-side privy.users().create with a telegram linked account at /start, wallet created with the bot's authorization key as additional signer + override_policy_ids, lookup via getByTelegramUserID; web-side Telegram login is the OAuth widget (seamless login is Mini-App-only).

Evidence: docs.privy.io/recipes/telegram-bot.md and the Telegram login-method page fetched; API endpoint POST /v1/users/telegram/telegram_user_id confirmed.

Sources:
- https://docs.privy.io/recipes/telegram-bot.md
- https://docs.privy.io/authentication/user-authentication/login-methods/telegram
- https://docs.privy.io/api-reference/users/get-by-telegram-user-id

### [medium] conf 0.85: WebMCP is in a Chrome origin trial (149–156, ends 2026-11-16; API moved to document.modelContext in 150) but no mainstream agent consumes it and the Claude Chrome extension request (#30645) was closed as not planned — it cannot carry a judge-visible demo in 9 days.

Evidence: Spronta 'State of WebMCP July 2026': 'no mainstream agent client calls modelContext tools yet'; GitHub API: issue 30645 state closed, labeled stale, 'Closed as not planned'; local Chrome is 152 (inside the trial window, still needs an OT token per origin).

Sources:
- https://www.spronta.com/blog/state-of-webmcp-july-2026/
- https://github.com/anthropics/claude-code/issues/30645
- https://ppc.land/chrome-149-origin-trial-puts-webmcp-in-developers-hands-at-last/

### [high] conf 0.95: None of the local repos ARCHITECTURE.md/PLAN.md rely on (invok, harness, humanhook, boter, starters, mini-app-host-platform spec, ~/code) exist on this machine.

Evidence: VERIFIED: `find ~ -maxdepth 5 -type d -iname <name>` and mdfind returned nothing for all five; ~/code does not exist; only unrelated matches (/Users/jonas/Documents/mushanghai/poker_arena/.../harness, ~/bob-harness = metadao-futarchy).

Sources:
- file:///Users/jonas/Documents/web3/agentic-wallet/ARCHITECTURE.md
- file:///Users/jonas/Documents/web3/agentic-wallet/PLAN.md

### [medium] conf 0.85: The hedera-dev x402 inference PoC is small and only partly reusable: created 2026-07-13, 5 commits, 2 stars, packages/service (4 payment-gated routes) + packages/agent (Express + React), coupled to LM Studio and uses x402.org/facilitator on testnet rather than Blocky402 (which the prize requires). HCS logging costs $0.0008/message (free on testnet).

Evidence: GitHub API: pushed_at 2026-07-15, stargazers 2; repo page structure; Hedera blog/X: ConsensusSubmitMessage $0.0001→$0.0008 from Jan 2026 (v0.69).

Sources:
- https://github.com/hedera-dev/x402-inference-pay-per-request-poc
- https://hedera.com/blog/price-update-to-consensussubmitmessage-in-consensus-service-january-2026/
- https://x.com/hedera/status/1999178115948360185

### [low] conf 0.8: Recommended LLM for the agent loop in this environment is claude-opus-5 via @anthropic-ai/sdk 0.123.0 (or @ai-sdk/anthropic 4.0.49 under Vercel AI SDK 7.0.92) with adaptive thinking; do not use claude-fable-5-1 for a tool loop because forced tool_choice returns 400 there.

Evidence: claude-api skill loaded in this session: 'ALWAYS use claude-opus-5 unless the user explicitly names a different model'; npm view versions checked locally.

Sources:
- https://www.npmjs.com/package/@anthropic-ai/sdk
- https://www.npmjs.com/package/@ai-sdk/anthropic

## Recommendations

- Fix the custody story before writing code: Hedera x402 leg = agent-owned Hedera testnet account (portal.hedera.com key in env, later TEE) with host-enforced per-tx/daily caps and an allowlisted payTo; Privy-policied leg = Base Sepolia USDC via createX402Client/useX402Fetch (maxValue + TransferWithAuthorization `to`/`value` policy) paying The Graph x402 gateway per query. Say this split plainly in the README; it satisfies Hedera (hosted 402 via Blocky402), Graph (live data, agent pays per query with x402) and Privy (live financial flow + policy) without lying about what Privy enforces.
- Daily cap: implement it in the host ledger (idempotency key per spend, serialized per session). Only add a Privy stateful policy for the on-chain `wallet_send`/swap path (eth_signTransaction, rolling 86400s, native units) — it does not cover x402 typed data.
- Own the Chrome slice as a single person and always pass an explicit Chrome `path`; write against raw `view.cdp()` (Page.startScreencast/screencastFrameAck, Input.dispatch*, Accessibility.getFullAXTree, Page.addScriptToEvaluateOnNewDocument) so the same code runs under puppeteer-core if Bun.WebView misbehaves. Budget days 0–3 for screencast + input + arbitration + snapshot; nothing else lands on that person.
- Use the Privy Telegram-bot recipe verbatim for onboarding: bot /start → privy.users().create({linked_accounts:[{type:'telegram', telegram_user_id}]}) → wallet with the bot's authorization key as additional signer + override policy; web app logs in with the Telegram OAuth widget (set bot domain) and resolves the same user. This removes the custom pairing-code table entirely.
- Pay for HBAR on the Hedera 402 first (asset 0.0.0, no token association); switch the demo to testnet USDC 0.0.429274 only if HBAR is working by day 3. Copy the middleware shape from hedera-dev's PoC `packages/service` but point it at https://api.testnet.blocky402.com.
- Solve the 'watch the unlock in the shared Chrome' moment explicitly: the host pays with x402, the service returns a one-time receipt token, the agent navigates the shared Chrome to `/answer?receipt=…`. Do not try to make the in-page browser pay.
- Cut WebMCP (producer and consumer) from the submission; if a browser-wallet moment is wanted, spend at most one day on a stubbed EIP-1193 provider injected via Page.addScriptToEvaluateOnNewDocument that forwards eth_sendTransaction to the host and shows a policy reject.
- Graph: start from Subgraph MCP over SSE with a Studio key for discovery/schema, but make the load-bearing query a direct GraphQL call to 2–3 Messari lending subgraphs (aave-v3 ethereum/base/arbitrum, all LIVE) so the demo does not depend on MCP resource activation; log the Graph snapshot hash into the receipt.
- Agent loop: Vercel AI SDK streamText with @ai-sdk/anthropic and model `claude-opus-5`, ~12-step cap, tools browser_snapshot/browser_execute/x402_fetch/graph_query/wallet_*/ask_user; never expose resolvePermission/freeze as tools.
- Drop the 'steal from invok/harness/humanhook/boter' line from the docs (they are not on this machine) or have whoever has them push the specific files (screencast.ts, arbitration.ts, keep.tsx) into a /reference folder on day 0.

## Open questions

- Does Privy's Telegram OAuth widget login on the web resolve to the same Privy user that the bot created server-side with the same telegram_user_id (so bot-first wallets show up in the web app)? Not verified; test on day 0.
- Can a Privy value cap be expressed on x402 typed data (`ethereum_typed_data_message` TransferWithAuthorization `value` with `lte`)? The sanctions recipe shows the `value` field exists; a cap rule was not shown verbatim.
- Does Blocky402's hosted testnet facilitator actually settle HTS USDC (0.0.429274) today or only HBAR? Docs say 'HTS tokens' generically; the PoC used x402.org/facilitator on testnet.
- Where did Bun's auto-detected HeadlessChrome/108 binary come from on this Mac, and does Bun 1.3.14 include the chrome-backend close() unhandled-rejection fix (#40991/#40992 closed 2026-09-01)?
- Is the Graph testnet x402 gateway (testnet.gateway.thegraph.com) serving the Messari lending subgraphs, or only Studio-deployed testnet subgraphs? If not, the Privy-policied x402 spend must target mainnet gateway with real USDC on Base.
- Which facilitator does Privy's createX402Client use on Base Sepolia by default, and does it require a Pay AI/Corbits/Coinbase key?
- Who on the team actually has the invok/harness/humanhook/boter repos, if anyone?
- Subgraph Studio pricing above 100k queries/month was not fetched (thegraph.com/studio-pricing).

## All sources

- https://bun.com/docs/runtime/webview
- https://bun.com/blog/bun-v1.3.12
- https://github.com/oven-sh/bun/issues/40991
- https://docs.privy.io/controls/policies/overview
- https://docs.privy.io/controls/policies/stateful-policies.md
- https://docs.privy.io/wallets/overview/solutions/agent-wallets
- https://docs.privy.io/recipes/x402
- https://docs.privy.io/recipes/agent-integrations/x402-sanctions-screening.md
- https://docs.privy.io/wallets/using-wallets/signers/add-signers.md
- https://docs.privy.io/controls/common-use-cases/delegation.md
- https://docs.privy.io/wallets/using-wallets/other-chains/index.md
- https://docs.privy.io/wallets/overview/chains.md
- https://docs.privy.io/basics/react/advanced/configuring-evm-networks
- https://docs.privy.io/authentication/user-authentication/login-methods/telegram
- https://docs.privy.io/recipes/telegram-bot.md
- https://docs.privy.io/api-reference/users/get-by-telegram-user-id
- https://www.privy.io/pricing
- https://blocky402.com/
- https://blocky402.com/docs/quickstart/
- https://blocky402.com/docs/networks/
- https://blocky402.com/docs/testnet/
- https://www.npmjs.com/package/@x402/hedera
- https://docs.hedera.com/solutions/ai/x402
- https://github.com/hedera-dev/x402-inference-pay-per-request-poc
- https://github.com/hashgraph/hedera-agent-kit-js
- https://hedera.com/blog/price-update-to-consensussubmitmessage-in-consensus-service-january-2026/
- https://thegraph.com/docs/en/subgraphs/tooling/subgraph-mcp/introduction/
- https://thegraph.com/docs/en/subgraphs/tooling/subgraph-mcp/claude/
- https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/
- https://thegraph.com/docs/en/subgraphs/providers/subgraph-studio/introduction/
- https://thegraph.com/docs/en/subgraphs/existing-subgraphs/agent0/
- https://thegraph.com/blog/community-builder-queried-defi-lending-protocols-subgraphs-mcp/
- https://github.com/PaulieB14/graph-lending-mcp
- https://raw.githubusercontent.com/PaulieB14/graph-lending-mcp/main/SUBGRAPHS.md
- https://github.com/graphprotocol/subgraphs-skills
- https://www.spronta.com/blog/state-of-webmcp-july-2026/
- https://ppc.land/chrome-149-origin-trial-puts-webmcp-in-developers-hands-at-last/
- https://github.com/anthropics/claude-code/issues/30645
- https://chromewebstore.google.com/detail/webmcp-bridge/chgjbookknohehmaocfijekhaocaanaf
- file:///Users/jonas/Documents/web3/agentic-wallet/ARCHITECTURE.md
- file:///Users/jonas/Documents/web3/agentic-wallet/PLAN.md
- file:///Users/jonas/Documents/web3/agentic-wallet/prizes.md
