# prizes

## Summary

RULES THAT BIND (all VERIFIED at ethglobal.com/events/ethonline2026/info/details and /info/start): submit by Sun 13 Sep 2026 12:00 EDT; teams up to 5; "select up to 3 Partner Prizes to apply for", and "if a partner has multiple tracks, you can be eligible for all of them while only counting as 1 Partner Prize"; Classic track = all project-specific work starts after kickoff, "large single commits or missing histories may be disqualified"; video 2-4 min, 720p, human narration (AI voiceover / text-only rejected); AI tool usage must be documented with attribution; first-round async screen, ~top 20% to live judging (4 min demo + 3 min Q&A); "the majority of prizes are paid out to projects that do not advance." Consequence: the 3-partner cap is a hard rule, not a team preference. Privy(2 tracks) + Graph(2 tracks) + Hedera(1) = 5 reachable tracks, $26k gross pool. Every "cheap extra" in PLAN.md (ENS, World, Ledger, Uniswap, Chainlink, Bazantic) costs one of those three slots. None is worth swapping in.

TRACK AUDIT vs ARCHITECTURE.md demo.
Hedera "AI & Agentic Payments" ($2k x up to 3): (1) host live x402 service on Hedera testnet settled via Blocky402 -> PASS in plan; facilitator https://api.testnet.blocky402.com, network hedera:testnet, assets HBAR 0.0.0 + HTS (USDC testnet 0.0.429274). (2) platform/agent completes >=1 real paid request end to end -> RISK: the Hedera exact scheme requires the client to sign a partially-signed Hedera TransferTransaction; @x402/hedera's createClientHederaSigner takes a raw PrivateKey; Privy's x402 client supports only Base/Base Sepolia/Solana. So the Hedera leg cannot be signed by a Privy-policy-gated wallet out of the box. Fix: agent Hedera account = Privy server wallet using secp256k1_sign (raw hash, VERIFIED on React and Node) wrapped in a custom Hedera signer, host enforces per-tx/daily cap, kill switch = revoke the Privy authorization key. ~8-12h and moderately fiddly (ECDSA alias account, token association). Cheaper fallback (4h): dedicated ECDSA agent account whose key lives only on the host; say so in README. (3) README with setup/architecture/payment flow -> 2h. (4) video <=5 min -> ETHGlobal's 2-4 binds. Extra points that are cheap: metered pricing (price the packed Graph answer per protocol/bytes, 1h); HTS token in settlement (pay in USDC 0.0.429274 instead of HBAR, 2h); HCS audit trail (TopicMessageSubmit of each receipt, or Hedera Agent Kit's HCS Audit Trail Hook, 2-3h); ERC-8004 identity (register agent on Base Sepolia registry, then read it back via the Agent0 subgraph on The Graph — double-counts for the Graph composable track, 4-6h); discovery (publish a service card at /.well-known, 1h). Skip A2A/ACP and Scheduled Transactions.

Graph "Best AI Use Case (From Scratch)" ($2.5/1.5/1k): load-bearing -> PASS if the Graph number decides the spend. Live provider -> PASS via Studio Gateway key (Subgraph MCP at https://subgraphs.mcp.thegraph.com/sse needs a Bearer Gateway key) — or better, the Graph x402 gateway: https://gateway.thegraph.com/api/x402/subgraphs/id/{id} (USDC on Base, 0x8335...) and testnet.gateway.thegraph.com (Base Sepolia USDC 0x036C...), package @graphprotocol/client-x402, no API key. The track text literally says "let your agent pay per query autonomously with x402". Meaningful work -> PASS. Open-source README/SKILL.md -> 2h. Pool selection -> Start Fresh; do not import Jonas's prior layer into the tree.
Graph "Composable / Standardized" ($2.5/1.5/1k): must compose >=2 Graph products OR build on a standardized schema; single-subgraph query is explicitly DQ -> GAP until the "cheapest USDC borrow" query runs the same Messari lending query across >=3 deployments (Aave v3 ETH JCNWRypm..., Aave v3 Base D7mapexM..., plus Compound/Morpho/Zerolend from messari deployment.json) and the README shows "what became easier". 4-6h. Composition bonus: Subgraph MCP for discovery + standardized subgraphs + Agent0 ERC-8004 subgraph. Lisbon 2026 write-up says every standardized-schema team built the same missing "protocol-to-deployment registry" — ship a tiny registry JSON + SKILL.md (2h) and say so.
Privy "Best financial flow" ($2.5k): core Privy + >=1 wallet -> PASS; >=1 functional flow with a GA feature -> PASS via x402 payment from the embedded wallet on Base Sepolia (useX402Fetch/createX402Client, maxValue) or a policy-gated USDC transfer; onramp: Stripe has no testnet, so use a faucet (allowed: cards may be mocked, one live flow required). Key RISK: Privy stateful aggregations (rolling/daily caps) only cover eth_signTransaction and eth_signUserOperation, not eth_signTypedData_v4 — and x402 on Base is EIP-3009 typed data. So the daily cap for x402 must be host-side; the "send the rest to 0xevil" jailbreak reject should be an eth_sendTransaction with to-allowlist + ethereum_calldata transfer.amount cap + a 24h aggregation, which IS enforced in Privy's enclave. Max 10 aggregations per app.
Privy "Best B2B financial product" ($2.5k): GAP on "business or organization use case" + "functional B2B workflow"; policies alone satisfy the control requirement (key quorums are gated: "reach out to Privy's team"; intents/manual approvals are API+dashboard). Cheapest honest bridge: frame as agent spend-management for a team — above-cap spend becomes a Privy intent approved in the dashboard or via Telegram card. 6-10h and it dilutes the video; apply to it anyway (costs no slot), do not build for it.

ADJACENT TRACKS (all need a slot swap): Ledger — needs a physical device, "must be built on the Ledger Agent Stack"; on-theme but no. World Selfie Check — beta flag "request access through your World point of contact", TestFlight sandbox, feedback doc; no. ENSv2 — Sepolia, ENSjs setRecords, ENSIP-26 agent-endpoint records; 6-10h but must be "central"; no. Uniswap — $1k x3, needs FEEDBACK.md + form; a policy-gated swap is 6h; only if you dropped Privy, which you should not. Chainlink Confidential Workflows — private beta via account team; no. Bazantic — beta waitlist; no.

SIGNALS: Lisbon 2026 Graph winners (BookerBob: Messari schemas + MCP driving a booking gateway; atlas: standardized subgraphs + x402 + MCP + SKILL.md) are the exact shape of this project. Hedera's Jul-2026 x402 bounty winner Tally already ships spend caps + HCS receipts + MCP on Hedera; ETHGlobal Lisbon 2026 had an "ethui agentic wallet" finalist — judges have seen "agent wallet with caps"; the shared, grabbable Chrome is the differentiator, keep it in the first 30 seconds.

RANKED EV (probability x dollars, my estimates): Hedera AI & Agentic ~0.35x$2k=$700 (hosting requirement thins the field); Privy financial flow ~0.25x$2.5k=$625; Graph composable ~0.3x~$1.5k=$450; Graph AI from scratch ~0.2x~$1.5k=$300 (most crowded track on the board); Privy B2B ~0.1x$2.5k=$250. Expected ~$2.3k; ceiling $11.5k if you place in all five.

## Claims

### [high] conf 0.95: ETHOnline 2026 lets a project apply to at most 3 Partner Prizes, but all tracks of one partner count as a single Partner Prize; so Privy+Graph+Hedera = 5 reachable tracks and any adjacent sponsor (ENS, World, Ledger, Uniswap, Chainlink, Bazantic) requires dropping one of the three.

Evidence: VERIFIED: rules page states teams may 'select up to 3 Partner Prizes to apply for' and 'if a partner has multiple tracks, you can be eligible for all of them while only counting as 1 Partner Prize'.

Sources:
- https://ethglobal.com/events/ethonline2026/info/details

> VERDICT REFUTED: The rules portion is fully supported: the cited ETHOnline 2026 details page (fetched 2026-09-04, submission deadline Sept 13 2026 12:00pm EDT) states teams may "select up to 3 Partner Prizes to apply for" and "If a partner has multiple tracks, you can be eligible for all of them while only counting as 1 Partner Prize." The consequence that adding a fourth partner (ENS, World, Ledger, Uniswap, Chainlink, Bazantic - all confirmed present on the prizes page) requires dropping one of the three follows directly. However, the specific number "5 reachable tracks" is NOT supported: the ETHOnline 2026 prizes page lists Privy with 2 tracks, The Graph with 3, and Hedera with 5, i.e. 10 tracks across those three partners. Unless "reachable" is a project-specific fit judgment (which the source cannot verify), the "= 5" figure is wrong. Also note the details page caveat: projects started before the event under the "From Scratch" track don't qualify for partner prizes, and Continuity-track eligibility varies by partner. Marked refuted because the numeric claim as stated does not match the source; the corrected claim below keeps everything that is supported.
> Corrected: ETHOnline 2026 (submissions due Sept 13, 2026, 12:00pm EDT) lets a project select up to 3 Partner Prizes, and if a partner has multiple tracks the project can be eligible for all of them while counting as only 1 Partner Prize. Privy (2 tracks), The Graph (3 tracks) and Hedera (5 tracks) together expose up to 10 tracks under 3 Partner Prize slots; adding any other sponsor (ENS 2, World 2, Ledger 2, Uniswap Foundation 2, Chainlink 3, Bazantic 4 tracks) would require dropping one of those three. Partner-prize eligibility also requires the project to be built during the event (pre-existing "From Scratch" projects are ineligible; Continuity-track eligibility varies by partner).
> Sources: https://ethglobal.com/events/ethonline2026/info/details, https://ethglobal.com/events/ethonline2026/prizes

> VERDICT SURVIVES: The core rule survives: the live ETHOnline 2026 details page (fetched 2026-09-04) states verbatim "you can select up to 3 Partner Prizes to apply for" and "If a partner has multiple tracks, you can be eligible for all of them while only counting as 1 Partner Prize." No newer announcement or contradicting partner-page rule was found; Graph and Hedera prize pages set no intra-partner exclusivity. Therefore the zero-sum conclusion (a fourth sponsor forces dropping one) is correct.

Strongest counter-argument / where the claim is sloppy: (1) The "5 reachable tracks" figure is wrong or at least unexplained. Privy has 2 tracks, The Graph 3, Hedera 4 = 9 nominal tracks. For a from-scratch (Classic) build the Continuity-only tracks are off-limits (Graph "AI Continuity" $5k, Hedera "Continuity" $1k), and the details page says Classic and Continuity are distinct tracks with different rules, so a from-scratch project realistically reaches Privy 2 + Graph 2 + Hedera 3 = 7 tracks, not 5 (Hedera's "Improve the Hedera Harness" open-source track is a plausible stretch, giving 6 if excluded). If instead the project is Continuity, Graph's "From Scratch" AI track is unavailable, changing the count again. (2) "Eligible for all of them" is not "compete in all of them": each track has hard qualification gates (Graph requires live data from a Graph provider and, for the composable track, actual composition/standardization; Hedera requires testnet/mainnet deployment plus a <=5 min demo video per track), so track count overstates realistic wins in a 9-day build. (3) The adjacent-sponsor comparison ignores that several of the "adjacent" partners are dominated by Continuity-only tracks (World AgentKit, Bazantic's agent track, Ledger/Uniswap/Chainlink/ENS continuity sub-tracks), so the true opportunity cost of swapping one in for a from-scratch project is smaller than their headline totals suggest (e.g., ENS effectively $4.5k, World $3.5k, Chainlink $2.5k). (4) Selection is a submission-time choice (deadline Sun Sep 13, 12:00 pm EDT), not something that must be fixed at build start, so the "3 partner" constraint should gate scope planning, not lock architecture on day one.
> Corrected: ETHOnline 2026 lets a project select up to 3 Partner Prizes at submission; all tracks of one partner count as one selection and the project is eligible for every track of that partner it actually qualifies for. For a from-scratch (Classic) build, Privy (2 tracks) + The Graph (2 non-Continuity tracks) + Hedera (3 non-Continuity tracks, one of which is an open-source harness contribution) yields up to 7 nominally reachable tracks, each with its own hard qualification gates (live Graph data, Hedera testnet/mainnet deployment + demo video). Adding any fourth sponsor (ENS, World, Ledger, Uniswap, Chainlink, Bazantic) requires dropping one of the three, but those sponsors' from-scratch-reachable pools are smaller than their headline totals because much of their money is Continuity-only.
> Sources: https://ethglobal.com/events/ethonline2026/info/details, https://ethglobal.com/events/ethonline2026/prizes, https://ethglobal.com/events/ethonline2026/prizes/the-graph, https://ethglobal.com/events/ethonline2026/prizes/hedera

### [high] conf 0.95: Hard submission constraints: 2-4 minute video (auto-rejected outside range), 720p, human narration (no AI voiceover), commit history required ('large single commits or missing histories may be disqualified'), AI tool usage documented with attribution, teams up to 5, deadline Sun 13 Sep 12:00 EDT; Hedera's '<=5 min' is looser and does not apply.

Evidence: VERIFIED on the ETHOnline 2026 details and start pages.

Sources:
- https://ethglobal.com/events/ethonline2026/info/details
- https://ethglobal.com/events/ethonline2026/info/start

> VERDICT SURVIVES: The ETHOnline 2026 details page (Submission Guidelines) states verbatim: videos must be 2-4 minutes ("Videos under 2 minutes or over 4 minutes will be automatically rejected"); minimum 720p ("Upload will fail if the video is less than 720p"); "DO NOT use a text to speech synthesizer / AI Voiceover"; "Submissions with large single commits or missing histories may be disqualified, as it's important to show your progress throughout the hackathon"; and teams must "clearly document in your submission where and how AI tools were used" including "which parts of the code, specific files, or assets were generated or assisted by AI." The start page states "Teams can have up to 5 people" and the deadline "Sunday, September 13th 2026 at 12:00 pm EDT". The Hedera prize page (ethglobal.com/events/ethonline2026/prizes/hedera) lists "Demo video of five minutes or less" for all four Hedera tracks. The only imprecision in the claim is "does not apply": Hedera's <=5 min requirement does technically apply to Hedera-track submissions, but it is looser and automatically satisfied by the event-wide 2-4 minute rule, so the ETHGlobal limit is the binding constraint. Every number, date, and quote in the claim is supported by the cited primary sources as of Sep 2026.
> Corrected: Hard submission constraints (ETHOnline 2026): 2-4 minute video ("Videos under 2 minutes or over 4 minutes will be automatically rejected"), minimum 720p (upload fails below), human narration ("DO NOT use a text to speech synthesizer / AI Voiceover"), commit history required ("Submissions with large single commits or missing histories may be disqualified"), AI tool usage documented in the submission (where/how used, which code/files/assets), teams up to 5 people, deadline Sunday 13 Sep 2026 12:00 pm EDT. Hedera's track requirement of a demo video "five minutes or less" is looser and is automatically satisfied by the binding 2-4 minute ETHGlobal rule.
> Sources: https://ethglobal.com/events/ethonline2026/info/details, https://ethglobal.com/events/ethonline2026/info/start, https://ethglobal.com/events/ethonline2026/prizes/hedera

> VERDICT SURVIVES: Re-fetched both cited pages today (2026-09-04) plus the prizes page. Every element checks out verbatim: video "between 2 and 4 minutes" with automatic rejection outside range; minimum 720p (uploads fail below); "DO NOT use a text to speech synthesizer / AI Voiceover"; version control required and "large single commits or missing histories may be disqualified"; must "clearly document in your submission where and how AI tools were used"; teams "up to 5 people" (on the start page, not the details page); deadline "Sunday, September 13th 2026 at 12:00 pm EDT", "Late submissions won't be accepted", no grace period. Hedera's four prize tracks each say "Demo video of five minutes or less". No newer developments or contradictory sources found (web search budget exhausted, but the primary sources are live and current). Strongest counter-arguments: (1) The claim is incomplete rather than wrong: it omits additional hard rules on the same page — no mobile-phone recording, no speeding up the video, Classic-track projects must be started during the hackathon with no pre-existing project-specific code (else ineligible for partner prizes/Finalist), max 3 partner prizes per submission, and a 7-minute finalist presentation format. (2) "Hedera's ≤5 min does not apply" is slightly misphrased: it does apply, it is simply subsumed by ETHGlobal's 2-4 min platform limit; but Hedera's rule also carries a content requirement (e.g. video must show "the paid request executing") that is a genuine additional constraint for the AI & Agentic Payments track. (3) The details page itself does not state team size; that comes from the start page, so the 'both pages verified' framing is a minor overstatement. None of these refute the claim.
> Corrected: Verified hard constraints (ETHGlobal ETHOnline 2026, as of 2026-09-04): video 2-4 minutes (auto-rejected outside), min 720p, human narration only (no TTS/AI voiceover), not recorded on a mobile phone, not sped up; version control with a real commit history (large single commits / missing histories may be disqualified); AI tool usage documented in the submission ("where and how"); teams up to 5 (solo allowed); deadline Sun 13 Sep 2026 12:00 pm EDT, no late submissions or grace period. Also binding: Classic-track projects must be started during the hackathon with no pre-existing project-specific code or assets (otherwise ineligible for partner prizes and Finalist), max 3 partner prizes per submission, finalists present 4 min demo + 3 min Q&A. Hedera's "five minutes or less" video rule is subsumed by the 2-4 min platform limit, but Hedera tracks add a content requirement (e.g. the video must show the paid request executing on Hedera).
> Sources: https://ethglobal.com/events/ethonline2026/info/details, https://ethglobal.com/events/ethonline2026/info/start, https://ethglobal.com/events/ethonline2026/prizes

### [high] conf 0.85: The Hedera leg cannot be signed by a Privy-policy-gated wallet out of the box: the Hedera x402 exact scheme requires signing a partially-signed Hedera TransferTransaction, @x402/hedera's client signer takes a raw PrivateKey, and Privy's x402 client only documents Base, Base Sepolia and Solana. 'Privy is the leash' is only literally true on the EVM leg unless you wrap Privy secp256k1_sign (raw hash signing) in a custom Hedera signer.

Evidence: VERIFIED: Hedera scheme spec ('Client signs the transaction with their wallet... partially signed'); Blocky402 quickstart uses createClientHederaSigner(accountId, PrivateKey.fromStringECDSA); Privy x402 docs list Base/Base Sepolia/Solana; Privy raw-hash docs show secp256k1_sign on React and Node wallets but say nothing about policies applying to it.

Sources:
- https://github.com/x402-foundation/x402/blob/main/specs/schemes/exact/scheme_exact_hedera.md
- https://blocky402.com/docs/quickstart/
- https://docs.privy.io/recipes/x402
- https://docs.privy.io/wallets/using-wallets/ethereum/sign-a-raw-hash

> VERDICT SURVIVES: Core factual legs check out against primary sources (as of Sep 2026): (1) The x402 Hedera exact scheme spec says the client builds a TransferTransaction and "signs the transaction with their wallet. This results in a partially signed transaction (the fee payer's signature is still missing)"; the payload's `transaction` field is the Base64 partially-signed tx and the facilitator adds the feePayer signature. (2) @x402/hedera `src/signer.ts`: `createClientHederaSigner(accountId: string, privateKey: PrivateKey, config: HederaClientSignerConfig = {}): ClientHederaSigner` — a raw @hiero-ledger/sdk PrivateKey; the Blocky402 quickstart uses `PrivateKey.fromStringECDSA('0xYourHederaPrivateKey')`. However, `ExactHederaScheme`'s constructor is typed `signer: ClientHederaSigner`, a structural type `{ accountId: string; createPartiallySignedTransferTransaction(requirements): Promise<string> }`, so a custom signer is feasible — consistent with the claim's "unless you wrap" escape hatch. (3) Privy x402 recipe mentions networks only as "e.g. Base, Base Sepolia, or Solana" — an illustrative list, not an explicit exhaustive statement; Hedera is nowhere in Privy docs I could reach (policy docs enumerate Ethereum, Solana, Tron, XRPL, Sui only). (4) Privy raw-hash docs show `secp256k1_sign` (React provider.request) and `privy.wallets().ethereum().secp256k1Sign(...)` (Node) with no mention of policies. Stronger than the claim states: Privy's policy overview enumerates the policy-gated methods (eth_sendTransaction, eth_signTypedData_v4, personal_sign, eth_sign7702Authorization, Solana signTransaction, etc.) and secp256k1_sign is NOT among them. So the final "unless you wrap secp256k1_sign" clause overstates: wrapping would give Privy-custodied keys on Hedera, but there is no documented policy enforcement on raw-hash signing, so the "leash" (policy gating) would still not apply to the Hedera leg. Also note Privy policies do cover Solana, so "only on the EVM leg" is accurate only relative to an EVM+Hedera architecture. Several Privy chain-support pages 404'd and web search budget was exhausted, so absence of Hedera support is inferred from the reachable docs rather than an explicit "unsupported" statement.
> Corrected: The Hedera leg cannot be signed by a Privy-policy-gated wallet out of the box: the x402 Hedera exact scheme requires the client to sign a Hedera TransferTransaction, producing a partially-signed transaction that the facilitator then co-signs as fee payer; @x402/hedera's `createClientHederaSigner(accountId, privateKey: PrivateKey, config)` takes a raw Hedera SDK PrivateKey; and Privy's x402 recipe names only Base, Base Sepolia and Solana (as "e.g." examples) with no Hedera support documented anywhere in Privy's wallet or policy docs. 'Privy is the leash' is therefore literally true only on the EVM leg. Because `ExactHederaScheme` accepts any object matching the structural `ClientHederaSigner` type, you could wrap Privy's `secp256k1_sign`/`secp256k1Sign` raw-hash signing in a custom Hedera signer to keep the key in Privy custody — but Privy's policy docs do not list secp256k1_sign among policy-enforced methods, so even that wrapper would not make the Hedera leg policy-gated.
> Sources: https://github.com/x402-foundation/x402/blob/main/specs/schemes/exact/scheme_exact_hedera.md, https://raw.githubusercontent.com/x402-foundation/x402/main/typescript/packages/mechanisms/hedera/src/signer.ts, https://raw.githubusercontent.com/x402-foundation/x402/main/typescript/packages/mechanisms/hedera/src/exact/client/scheme.ts, https://github.com/x402-foundation/x402/tree/main/typescript/packages/mechanisms/hedera, https://blocky402.com/docs/quickstart/, https://docs.privy.io/recipes/x402, https://docs.privy.io/wallets/using-wallets/ethereum/sign-a-raw-hash, https://docs.privy.io/controls/policies/overview

> VERDICT SURVIVES: Attempted refutation (web search budget was exhausted; used WebFetch on primary docs and inspected the actual npm packages @x402/hedera@2.25.0, published 2026-09-04, and @hiero-ledger/sdk). Findings:

1. Core facts hold. scheme_exact_hedera.md: the client payload is a base64 partially-signed native TransferTransaction, no EVM/EIP-3009 variant. Blocky402's network list is hedera:testnet / hedera:mainnet only, no eip155:295/296, so you cannot dodge native Hedera signing by routing through Hedera's EVM JSON-RPC relay with @x402/evm and Privy's ethereum wallet. Privy x402 recipe: Base, Base Sepolia, Solana only; Privy policy docs enumerate chain_type 'ethereum'|'solana'|'tron'|'sui'|'xrpl' and a fixed method list that does NOT include secp256k1_sign; the raw-hash and other-chains docs say nothing about policy enforcement. Privy's own 'other chains via raw signing' page names Cosmos/Stellar/Sui, not Hedera.

2. Strongest counter-argument (overstatement, not refutation): the 'custom signer' escape hatch is much cheaper than the claim implies, and it is the intended extension point. In @x402/hedera dist/esm/signer-*.d.mts, ClientHederaSigner is a plain 2-member type: { accountId: string; createPartiallySignedTransferTransaction(requirements): Promise<string> }. createClientHederaSigner(accountId, PrivateKey) is merely a convenience default (index.mjs line 139, does freezeWith(client); tx.sign(key)). The Hiero SDK's Transaction.signWith(publicKey, async bytes => signature) accepts an arbitrary async signer callback, and Hedera ECDSA signatures are secp256k1 over keccak256(bodyBytes), which is exactly the primitive Privy exposes as secp256k1_sign (drop the v byte, keep r||s). So a Privy-backed Hedera signer is roughly 40 lines, well within a 9-day hackathon.

3. However, the part that matters for 'Privy is the leash' survives: even after wrapping, there is no documented evidence that Privy policies (transfer limits, recipient allowlists) evaluate secp256k1_sign requests; the policy method enum omits it, and a raw 32-byte hash carries no parseable recipient/amount for a policy engine to inspect anyway. Any Hedera leash would have to be enforced app-side (or via Privy authorization-key/quorum gating of who may call the wallet at all), not by Privy's chain-aware policy rules. Not refuted; narrowed.
> Corrected: The Hedera x402 leg cannot use Privy's chain-aware policies: the exact scheme needs a client-signed native TransferTransaction, the only hosted facilitator (Blocky402) supports hedera:testnet/mainnet but not Hedera EVM (eip155:296), and Privy's policy engine covers ethereum/solana/tron/sui/xrpl methods only, not secp256k1_sign. A Privy-backed Hedera signer is nevertheless cheap to build (ClientHederaSigner is a 2-member interface; Hiero's Transaction.signWith takes an async callback; Hedera ECDSA = secp256k1 over keccak256(bodyBytes) = Privy secp256k1_sign minus the v byte), so key custody can stay in Privy on both legs, but recipient/amount limits on the Hedera leg must be enforced in your own app layer (or by Privy authorization-key gating of the wallet), not by Privy policy rules. 'Privy is the leash' is literally true only on the EVM leg.
> Sources: https://github.com/x402-foundation/x402/blob/main/specs/schemes/exact/scheme_exact_hedera.md, https://blocky402.com/docs/networks/, https://blocky402.com/docs/quickstart/, https://docs.privy.io/recipes/x402, https://docs.privy.io/controls/policies/overview, https://docs.privy.io/wallets/using-wallets/ethereum/sign-a-raw-hash, https://docs.privy.io/wallets/using-wallets/other-chains/index.md, https://docs.privy.io/security/wallet-infrastructure/policy-and-controls.md, npm @x402/hedera@2.25.0 dist/esm/signer-DCjZrivX.d.mts (ClientHederaSigner type) and dist/esm/index.mjs line 139 (createClientHederaSigner), npm @hiero-ledger/sdk lib/transaction/Transaction.js (signWith(publicKey, signerFn) accepting async callback)

### [high] conf 0.85: Privy rolling/daily caps (stateful aggregations) apply only to eth_signTransaction and eth_signUserOperation, not to eth_signTypedData_v4; since EVM x402 payments are EIP-3009 typed-data signatures, the per-day x402 budget must be enforced on the host. The jailbreak-reject demo should therefore be an eth_sendTransaction gated by a to-allowlist, an ethereum_calldata transfer.amount cap and a 24h aggregation, which Privy enforces in its enclave. Max 10 aggregations per app; values update after signing, so concurrent requests can slip through.

Evidence: VERIFIED from Privy stateful-policies page (supported methods list, 'designed for disaster prevention', 10-aggregation limit) and the Ethereum example policies (typed-data domain constraints, ERC-20 transfer.amount cap, native value cap).

Sources:
- https://docs.privy.io/controls/policies/stateful-policies
- https://docs.privy.io/controls/policies/example-policies/ethereum
- https://docs.privy.io/controls/policies/overview

> VERDICT REFUTED: Mostly supported, but two parts of the claim are contradicted or unsupported by the cited sources (fetched 2026-09-04).

SUPPORTED: (a) The stateful-policies page says verbatim "Aggregations can be configured for the following RPC methods: eth_signTransaction ... eth_signUserOperation" — no typed-data method is listed, so a per-day cap on EIP-3009 typed-data x402 payments cannot be done with a Privy aggregation and must live on the host. (b) "Each app can have a maximum of 10 aggregations." (c) "Aggregation values are updated after a request is successfully signed, not before. This means multiple concurrent requests may all pass policy evaluation before any of their values are recorded." and "Stateful policies are designed for disaster prevention ... rather than strict real-time enforcement." (d) Windows are rolling, 3600–259200 s, with an 86400 s (24h) example; aggregations can sum `value` or decoded calldata fields like `transfer.amount`. (e) The Ethereum examples page shows eth_signTypedData_v4 domain constraints (chainId/verifyingContract), an ethereum_calldata `transfer.amount` lte cap, a native `value` cap, and `to` allowlists (eq / in / in_condition_set).

REFUTED/UNSUPPORTED: (1) The claim's own recommendation is internally inconsistent with source (a): the demo is described as an "eth_sendTransaction ... [with] a 24h aggregation", but eth_sendTransaction does not appear anywhere on the stateful-policies page and is not an aggregation-supported method. The example aggregation policies use `method: 'eth_signTransaction'`. A demo relying on a Privy 24h aggregation must use eth_signTransaction (or eth_signUserOperation) and broadcast itself; alternatively use eth_sendTransaction with only stateless rules (allowlist + amount cap). (2) "which Privy enforces in its enclave" is not supported for the stateful component: the overview says the enclave enforces policies "by default" but that "Privy enforces some policies at the API level. For example, limiting transfer sizes requires transaction simulation which runs outside the enclave today." The stateful-policies page does not state aggregations are evaluated inside the enclave, and the overview indicates stateful/reference evaluation against historical data happens at the API layer. The safe statement is that Privy enforces the policy server-side (enclave for stateless rules; aggregation/stateful evaluation not documented as in-enclave).
> Corrected: Privy stateful aggregations (rolling windows of 1–72 h, e.g. 86400 s for 24 h, summing `value` or decoded calldata fields such as `transfer.amount`) can only be configured for eth_signTransaction and eth_signUserOperation — not eth_signTypedData_v4 and not eth_sendTransaction. Because EVM x402 payments are EIP-3009 typed-data signatures, Privy can only constrain them statelessly (typed-data domain chainId/verifyingContract, message fields); a per-day x402 budget must be enforced on the host. The jailbreak-reject demo should therefore be an eth_signTransaction (self-broadcast) gated by a `to` allowlist, an ethereum_calldata `transfer.amount` cap, and a 24 h aggregation; the allowlist/cap rules are enforced by Privy server-side (enclave by default, with some checks such as simulation-based transfer limits running at the API level outside the enclave), and the docs do not state that aggregation evaluation happens inside the enclave. Max 10 aggregations per app; values update only after a request is successfully signed, so concurrent requests can all pass before any is recorded — Privy describes stateful policies as "designed for disaster prevention" rather than strict real-time enforcement.
> Sources: https://docs.privy.io/controls/policies/stateful-policies, https://docs.privy.io/controls/policies/example-policies/ethereum, https://docs.privy.io/controls/policies/overview

> VERDICT REFUTED: The first half survives and is strengthened: Privy's stateful-policies page lists only eth_signTransaction and eth_signUserOperation as aggregation-capable (window 'rolling' only, 3600-259200s, metric 'sum' only, field sources ethereum_transaction/ethereum_calldata), the x402 sanctions-screening recipe confirms EVM x402 hits eth_signTypedData_v4 with an EIP-3009 TransferWithAuthorization types map, and the x402 recipe states 'x402 payments are signed as EIP-712 typed data, not as ordinary transactions, so a policy on eth_sendTransaction does not cover them.' No changelog entry from May-Sep 2026 extends aggregations to typed data. 10-aggregation cap and after-signing update/concurrency caveat are verbatim in the docs.

However, the claim's concrete demo recommendation contains a clear flaw that Privy's own recipe refutes verbatim: 'Aggregations only track eth_signTransaction and eth_signUserOperation requests. If the wallet also handles eth_sendTransaction calls, that spend does not count toward the cap.' The proposed 'eth_sendTransaction gated by ... a 24h aggregation' would therefore never trip the aggregation; the demo must use eth_signTransaction (sign in Privy, broadcast from the host) or eth_signUserOperation. Two further overstatements: (1) 'which Privy enforces in its enclave' is not supported — the overview says the TEE enforces policies by default but 'Privy enforces some policies at the API level. For example, limiting transfer sizes requires transaction simulation which runs outside the enclave today', and the stateful-policies pages never state where aggregation evaluation runs; (2) the claim frames ALL x402 budget enforcement as host-side, but a per-payment cap on x402 IS enforceable inside Privy via an ethereum_typed_data_message condition on 'value' with 'lte' (the ERC-20 Permit example shows exactly this), provided the policy's types map matches the request's types map exactly (field order matters) — only the cumulative/daily budget must be host-side. Also, aggregations are described as app-scoped ('tracks data for RPC requests made through that app's wallets'), so per-user scoping needs verification before treating it as a per-wallet budget. Note: the hackathon-relevance counter-argument (a host-side budget counter plus Privy per-payment typed-data cap is simpler and demo-equivalent) is plausible but not evidence-based.
> Corrected: Privy stateful aggregations (rolling window only, 1h-72h, sum only, over ethereum_transaction/ethereum_calldata fields, max 10 per app, updated after signing so concurrent requests can slip through, 'disaster prevention' not strict enforcement) cover only eth_signTransaction and eth_signUserOperation. They do NOT cover eth_signTypedData_v4 (so EVM x402 EIP-3009 payments need a host-side daily budget) and — per Privy's own recipe — eth_sendTransaction spend 'does not count toward the cap' either. Therefore the jailbreak-reject demo must be an eth_signTransaction (host broadcasts) or eth_signUserOperation, gated by a to-allowlist, an ethereum_calldata transfer.amount per-tx cap and a 24h aggregation reference condition. Per-payment x402 caps can still be enforced in Privy via an eth_signTypedData_v4 rule with ethereum_typed_data_message 'value' lte (exact types-map match required); only the cumulative budget is host-side. Say 'Privy enforces server-side under policy' rather than 'in its enclave' — Privy documents that simulation-dependent limits run at the API level and does not state where aggregations are evaluated. Verify whether aggregations are per-wallet or app-wide before using one as a per-user budget.
> Sources: https://docs.privy.io/controls/policies/stateful-policies, https://docs.privy.io/recipes/using-stateful-policies.md, https://docs.privy.io/controls/policies/overview.md, https://docs.privy.io/controls/policies/example-policies/ethereum, https://docs.privy.io/recipes/agent-integrations/x402.md, https://docs.privy.io/recipes/agent-integrations/x402-sanctions-screening.md, https://docs.privy.io/changelogs/product-updates.md

### [high] conf 0.85: The Graph runs its own x402 pay-per-query gateway (mainnet: gateway.thegraph.com/api/x402/subgraphs/id/{id}, USDC on Base; testnet: testnet.gateway.thegraph.com, Base Sepolia USDC 0x036CbD53842c5426634e7929541eC2318f3dCF7e; package @graphprotocol/client-x402, no API key). Having the agent pay The Graph per query from the Privy wallet satisfies the Graph AI track's own wording ('let your agent pay per query autonomously with x402') and doubles as the live Privy financial flow.

Evidence: VERIFIED on the Graph x402 docs page and hackathon-resources post; the ETHOnline track text lists x402 pay-per-query as an encouraged pattern.

Sources:
- https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/
- https://thegraph.com/blog/hackathon-resources/
- https://ethglobal.com/events/ethonline2026/prizes

> VERDICT SURVIVES: All factual elements check out against primary sources as of 2026-09-04. (1) The Graph docs page (thegraph.com/docs/en/subgraphs/tooling/x402-payments/) lists endpoint patterns `POST /api/x402/subgraphs/id/{subgraph_id}` and `POST /api/x402/deployments/id/{deployment_id}`, mainnet gateway `https://gateway.thegraph.com` paying USDC on Base (USDC 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913), testnet `https://testnet.gateway.thegraph.com` paying USDC on Base Sepolia (0x036CbD53842c5426634e7929541eC2318f3dCF7e — matches Circle's canonical Base Sepolia USDC), and states "No API keys, accounts, or sessions". No beta/deprecation notice. (2) npm registry confirms `@graphprotocol/client-x402` exists: v1.0.0, created 2026-04-14, last modified 2026-08-31, README table shows production `https://gateway.thegraph.com/api/x402` / `base` and testnet `https://testnet.gateway.thegraph.com/api/x402` / `base-sepolia`, "no API key required". (3) The Graph hackathon-resources blog (updated 2026-09-03) lists "x402 Payments: Pay per query in USDC over HTTP from a Subgraph Gateway, with no API key or account" and gives ETHOnline dates Sep 4–16, 2026. (4) The ETHGlobal ETHOnline 2026 prize page contains the phrase "let your agent pay per query autonomously with x402" in The Graph's AI track text (note: the page shows three Graph tracks — Composable/Standardized, AI From Scratch, AI Continuity — $5k each, not the "two tracks" the blog describes). (5) Privy has a "Best Financial Flow" ($2,500) track on the same page: "funding, moving, trading, or spending digital assets… completing functional flows like transfers or swaps" — a USDC pay-per-query spend from a Privy wallet plausibly fits, though that is a judging-fit judgment, not a fact in the sources. One practical caveat: the client-x402 package is built around a raw private key (`X402_PRIVATE_KEY` / `x402PrivateKey`), so paying from a Privy wallet requires wiring a Privy-backed signer for the EIP-712 USDC authorization rather than using the package's default config out of the box.
> Corrected: The Graph runs its own x402 pay-per-query gateway: mainnet `https://gateway.thegraph.com/api/x402/subgraphs/id/{id}` (also `/api/x402/deployments/id/{id}`) paid in USDC on Base (0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913); testnet `https://testnet.gateway.thegraph.com/api/x402/...` paid in Base Sepolia USDC (0x036CbD53842c5426634e7929541eC2318f3dCF7e); official package `@graphprotocol/client-x402` (v1.0.0, published Apr 2026, updated Aug 2026), no API key or account required. The ETHOnline 2026 Graph AI track text (three $5k Graph tracks total) literally says "let your agent pay per query autonomously with x402", and Privy's "Best Financial Flow" ($2,500) track rewards functional flows that fund/move/spend digital assets, so an agent paying The Graph per query from its Privy wallet is a defensible fit for both — with the caveat that client-x402 expects a raw private key by default, so the Privy wallet must be wired in as a custom signer for the USDC payment authorization.
> Sources: https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/, https://registry.npmjs.org/@graphprotocol/client-x402, https://thegraph.com/blog/hackathon-resources/, https://ethglobal.com/events/ethonline2026/prizes

> VERDICT REFUTED: Core of the claim survives, but two load-bearing details are wrong/overstated as of 2026-09-04, which matters for a 9-day build.

1) Mainnet gateway is real and live: a POST to https://gateway.thegraph.com/api/x402/subgraphs/id/5zvR82Qo... returned HTTP 402 with a base64 `payment-required` header decoding to x402Version 2, scheme "exact", network eip155:8453 (Base only), amount 10000 (= $0.01 USDC/query), asset 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913, payTo 0x79DC34E41B2b591078d3dE222C43EcaaBD52FcCB, assetTransferMethod eip3009, maxTimeoutSeconds 300.

2) The documented testnet gateway does NOT resolve: testnet.gateway.thegraph.com returns no A or CNAME record from Cloudflare DoH (1.1.1.1) and Google DoH (8.8.8.8) — only the thegraph.com SOA in the authority section — and curl fails with "Could not resolve host". So the Base Sepolia / free-testnet-USDC path in the docs and README is not usable right now. Any demo/dev loop must run on Base mainnet with real USDC ($0.01/query — cheap, but it needs real funding, and "testnet first" plans are dead on arrival unless The Graph fixes DNS).

3) @graphprotocol/client-x402 is v1.0.0, its only version, published 2026-08-31 (4 days old). Its source (esm/createGraphQuery.js, esm/index.js) hard-requires a raw private key: `privateKeyToAccount(key)` from X402_PRIVATE_KEY or a `privateKey` option; there is no signer/account injection. A Privy embedded or server wallet never exposes a private key, so "pay from the Privy wallet using @graphprotocol/client-x402" is not possible as stated. The correct path is Privy's own x402 integration (docs.privy.io/recipes/x402): `createX402Client` from `@privy-io/node/x402` or `useX402Fetch` from `@privy-io/react-auth` (>=3.7.0) with `wrapFetchWithPayment` from `@x402/fetch`, pointed at the Graph gateway URL. This is compatible because both sides speak x402 v2 (@x402/fetch ^2.8 in the Graph package; gateway advertises x402Version 2), but it means skipping the Graph package entirely, or using it only with a throwaway hot key (which breaks the Privy story).

4) Privy's docs warn x402 payments are EIP-712 typed-data signatures, not transactions, so `eth_sendTransaction` policies don't cover them; recipient screening needs a signing-request policy. That weakens the "Privy controls/policies" angle (B2B track) though it's fine for the "Best Financial Flow" track ("at least one functional financial flow using a GA Privy feature").

5) Track wording: the ETHOnline 2026 Graph AI track ("Best AI Tooling or Use Case with The Graph", $5k, from-scratch vs continuity pools) lists x402 payment tooling as one example alongside MCP servers and agent SKILLs; the hard requirement is "consume live data from a Graph provider" (no mocked datasets), plus public repo and 2–4 min video. x402 is encouraged, not required — an API-key query would qualify equally, so x402 is a differentiator, not a gate. Also, the hackathon-resources blog post only contains a one-line link to the x402 docs, not the detailed claims attributed to it.

Net: refuted as written (testnet endpoint dead, package cannot use a Privy wallet), but the underlying strategy (agent pays The Graph $0.01/query on Base mainnet via Privy's x402 client) is viable and still a strong dual-track fit.
> Corrected: The Graph runs a live x402 pay-per-query gateway on Base mainnet only in practice: POST https://gateway.thegraph.com/api/x402/subgraphs/id/{id} returns a 402 (x402 v2, "exact" scheme, EIP-3009 USDC 0x8335...2913, currently 10000 units = $0.01 per query, payTo 0x79DC...FcCB), no API key. The documented testnet gateway (testnet.gateway.thegraph.com / Base Sepolia) has no DNS record as of 2026-09-04 and should not be planned on. The official @graphprotocol/client-x402 (v1.0.0, released 2026-08-31) only accepts a raw private key, so it cannot sign with a Privy wallet; instead use Privy's x402 support (createX402Client from @privy-io/node/x402, or useX402Fetch in @privy-io/react-auth >=3.7.0, with @x402/fetch wrapFetchWithPayment) against the Graph gateway URL, funding the Privy wallet with a few dollars of real Base USDC. This satisfies the Graph AI track's live-data requirement with x402 as an encouraged (not required) differentiator, and counts as a Privy "financial flow", but note x402 signatures are EIP-712 typed data that bypass eth_sendTransaction policies, so Privy policy/controls demos need signing-request policies rather than transaction policies.
> Sources: https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/, https://ethglobal.com/events/ethonline2026/prizes, https://thegraph.com/blog/hackathon-resources/, https://docs.privy.io/recipes/x402, https://docs.privy.io/wallets/using-wallets/ethereum/sign-typed-data, npm registry: @graphprotocol/client-x402@1.0.0 (published 2026-08-31; deps viem ^2.39.3, @x402/evm ^2.8.0, @x402/fetch ^2.8.0) — package source esm/createGraphQuery.js and esm/index.js use privateKeyToAccount(X402_PRIVATE_KEY), Live probe 2026-09-04: POST https://gateway.thegraph.com/api/x402/subgraphs/id/5zvR82QoaXYFyDEKLZ9t6v9adgnptxYpKpSbxtgVENFV -> HTTP 402 with payment-required header (x402Version 2, eip155:8453, amount 10000, asset 0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913), DNS check 2026-09-04: https://cloudflare-dns.com/dns-query?name=testnet.gateway.thegraph.com&type=A and https://dns.google/resolve?name=testnet.gateway.thegraph.com&type=A both return no Answer (SOA only); curl: Could not resolve host

### [high] conf 0.85: The Graph composable track explicitly disqualifies a single-subgraph query; the plan passes only if the lending question runs one Messari-standardized query across >=2-3 deployments (Aave v3 Ethereum JCNWRypm7FYwV8fx5HhzZPSFaMxgkPuw4TnR3Gpi81zk, Aave v3 Base D7mapexM5ZsQckLJai2FawTKXJ7CqYGKM8PErnS3cJi9, Aave v2 ETH C2zniPn45RnLDGzVeGZCx2Sw3GXrbc9gL4ZfL8B8Em2j, Zerolend 4Zf4doH54RDit9KVsfCp3MkjrP3szhJZwvw2z5PHczx9) and the README shows the leverage; the Lisbon 2026 write-up says every such team ended up building a protocol-to-deployment registry, which is a cheap, judge-legible add.

Evidence: VERIFIED: prize text ('Simply querying one Subgraph with no composition or standardization does not qualify'); messari deployment.json query-ids; Graph Lisbon 2026 winners post.

Sources:
- https://ethglobal.com/events/ethonline2026/prizes
- https://github.com/messari/subgraphs/blob/master/deployment/deployment.json
- https://thegraph.com/blog/ethglobal-lisbon-2026-winners/

> VERDICT REFUTED: Mostly supported, but three parts overstate the sources. SUPPORTED: (1) The ETHOnline 2026 prize page for "Best Use of Composable or Standardized Graph Products" ($5,000: $2,500/$1,500/$1,000) states verbatim "Simply querying one Subgraph with no composition or standardization does not qualify; consider the Best AI Use Case track instead." It also requires live data from a Graph provider (mocked/static datasets do not qualify), a public repo plus a 2-4 minute demo video, and "Make the standards leverage clear: show what became easier because a shared schema or composed product was used." (2) All four query IDs resolve in messari/subgraphs deployment/deployment.json to prod decentralized-network deployments: aave-v3-ethereum, aave-v3-base, aave-v2-ethereum, zerolend-ethereum. (3) The Graph's Lisbon 2026 winners post (published July 28, 2026) does describe teams that "write one query against a shared schema, then register new protocols as data rather than code" and "hand-built a registry mapping schemas to deployments." NOT SUPPORTED AS STATED: (a) "passes only if" is too strong — the prize text offers two qualifying routes: "Either compose two or more of The Graph's products, or build meaningfully on a standardized schema (for example the Messari Standardized Subgraphs)"; authoring/extending a Standardized Subgraph or a composable Substreams module is also in scope. The multi-deployment Messari query is one sufficient path, not the only one. (b) No numeric threshold (">=2-3 deployments") appears in the prize text; "two or more" applies to composing Graph products, not to deployment count. "README shows the leverage" is a paraphrase — the text says make the leverage clear in the submission (repo + demo video), not specifically a README. (c) "every such team" is wrong: the blog says four named teams (Am I cooked, atlas, BookerBob, deeptrace) "each arrived at the same architecture" and "All four hand-built a registry mapping schemas to deployments" — not every composable-track team, and the post frames it as a signal that the conformance index is missing, not as advice that it is a cheap judge-legible add (that is the team's own inference). Also note Lisbon's track split/amounts differ from ETHOnline 2026, so Lisbon judging is only indicative.
> Corrected: The ETHOnline 2026 Graph track "Best Use of Composable or Standardized Graph Products" ($5,000: $2,500/$1,500/$1,000) states verbatim that "Simply querying one Subgraph with no composition or standardization does not qualify." A lending question that runs one Messari-standardized query across multiple live deployments — e.g. Aave v3 Ethereum (JCNWRypm7FYwV8fx5HhzZPSFaMxgkPuw4TnR3Gpi81zk), Aave v3 Base (D7mapexM5ZsQckLJai2FawTKXJ7CqYGKM8PErnS3cJi9), Aave v2 Ethereum (C2zniPn45RnLDGzVeGZCx2Sw3GXrbc9gL4ZfL8B8Em2j), Zerolend Ethereum (4Zf4doH54RDit9KVsfCp3MkjrP3szhJZwvw2z5PHczx9), all prod decentralized-network query IDs in messari/subgraphs deployment.json — satisfies the "build meaningfully on a standardized schema (for example the Messari Standardized Subgraphs)" route, which is one of two qualifying routes (the other being composing two or more Graph products). The prize gives no minimum deployment count; it requires live Graph-provider data (no mocked/static datasets), a public repo, a 2-4 minute demo video, and that the submission "make the standards leverage clear: show what became easier because a shared schema or composed product was used." The Graph's ETHGlobal Lisbon 2026 winners post (July 28, 2026) reports that four winning teams (Am I cooked, atlas, BookerBob, deeptrace) each "hand-built a registry mapping schemas to deployments" and registered new protocols "as data rather than code," which the post reads as evidence that an index of standard-conforming deployments is missing; a small protocol-to-deployment registry is therefore a precedented, low-cost pattern, though not something every team built nor something the post or prize text explicitly rewards.
> Sources: https://ethglobal.com/events/ethonline2026/prizes, https://github.com/messari/subgraphs/blob/master/deployment/deployment.json, https://thegraph.com/blog/ethglobal-lisbon-2026-winners/

> VERDICT SURVIVES: Re-verified all three sources today (2026-09-04). (1) ETHOnline 2026 prize page, Graph "Best Use of Composable or Standardized Graph Products": "Simply querying one Subgraph with no composition or standardization does not qualify"; must "either compose two or more of The Graph's products, or build meaningfully on a standardized schema"; "consume live data from a Graph provider"; "make the standards leverage clear: show what became easier because a shared schema or composed product was used"; public repo + 2-4 min video. Core disqualifier holds. (2) messari/subgraphs deployment.json: all four query-ids match, status "prod", schema 3.1.0. (3) Lisbon 2026 post: "every team that used [standardized schemas] built the same missing piece... Am I cooked, atlas, BookerBob, and deeptrace... All four hand-built a registry mapping schemas to deployments." deeptrace ran "one query template against Aave v3, Seamless, and Moonwell" with pinned deployment IDs; atlas spanned 86 deployment IDs.

Strongest counter-arguments (none refute, but they narrow it): (a) "passes ONLY if Messari multi-deployment" is overstated. The prize text gives an explicit alternative: composing two Graph products (e.g., Subgraph MCP + Token API, or Substreams + a subgraph). Subgraph MCP "for cross-protocol analysis" is named on the page, and ARCHITECTURE.md already plans Subgraph MCP. (b) The plan (PLAN.md line 154-155) targets BOTH the composable track and the AI-from-scratch track; the AI track only requires live Graph data and load-bearing reasoning, no composition. A single-subgraph query would lose the composable track, not "the plan". (c) Freshness risk the claim ignores: all four Messari deployments show "Last updated: 2 years ago" on the Graph Explorer (versions 3.1.0_2.4.1 / 1.4.1 / 2.2.0 / 1.0.0); explorer index-status did not render in fetch, so live sync/indexer coverage is unverified. Judges in Lisbon explicitly valued pinned deployment IDs and freshness ("a guard five minutes late is... a bug for one that spends"). The plan must verify these IDs actually serve current blocks via a Studio key before committing; deeptrace's Aave v3 + Seamless + Moonwell (Base-native) set is a proven fallback. (d) "Every such team built a registry" is accurate but two of the four (BookerBob, deeptrace) were in AI tracks, and the composable winner atlas covered 86 deployments, so a 4-entry hand-written registry is the floor, not a differentiator; it is a cheap add, not a win condition. (e) Note Graph's formal "subgraph composition" feature (specVersion 1.3.0, same-chain, max 5 sources) is a different thing from the prize's "compose products" wording; the plan should not conflate them.

Web search budget was exhausted this session (200/200), so no search for Aug-Sep 2026 developments (e.g., Messari deprecation notices) was possible; the messari/subgraphs GitHub page showed no archive/deprecation banner.
> Corrected: The Graph composable track (ETHOnline 2026) disqualifies "simply querying one Subgraph with no composition or standardization." The plan's cheapest qualifying path is one Messari-standardized lending query (schema 3.1.0) run against >=2 decentralized-network deployments (prod IDs verified in deployment.json: Aave v3 ETH JCNWRy..., Aave v3 Base D7mape..., Aave v2 ETH C2zniP..., Zerolend 4Zf4do...), with the README/video stating what the shared schema made easier and a small protocol->deployment registry (every Lisbon 2026 team on standardized schemas built one; deeptrace pinned deployment IDs and rejected mismatches). Caveats: (1) an explicit alternative path exists, composing two Graph products (e.g., Subgraph MCP + Token API/Substreams), so Messari is sufficient, not necessary; (2) the AI-from-scratch track has no composition requirement, so a single subgraph only forfeits the composable track; (3) all four Messari deployments were last published ~2 years ago and their live sync status is unverified; test them with a Studio key on day 1 and keep deeptrace's Base set (Aave v3 / Seamless / Moonwell) as fallback; (4) the registry is table stakes, not a differentiator (atlas spanned 86 deployments).
> Sources: https://ethglobal.com/events/ethonline2026/prizes, https://github.com/messari/subgraphs/blob/master/deployment/deployment.json, https://thegraph.com/blog/ethglobal-lisbon-2026-winners/, https://thegraph.com/explorer/subgraphs/JCNWRypm7FYwV8fx5HhzZPSFaMxgkPuw4TnR3Gpi81zk?view=Query&chain=arbitrum-one, https://thegraph.com/explorer/subgraphs/D7mapexM5ZsQckLJai2FawTKXJ7CqYGKM8PErnS3cJi9?view=Query&chain=arbitrum-one, https://thegraph.com/explorer/subgraphs/C2zniPn45RnLDGzVeGZCx2Sw3GXrbc9gL4ZfL8B8Em2j?view=Query&chain=arbitrum-one, https://thegraph.com/explorer/subgraphs/4Zf4doH54RDit9KVsfCp3MkjrP3szhJZwvw2z5PHczx9?view=Query&chain=arbitrum-one, https://thegraph.com/docs/en/subgraphs/guides/subgraph-composition/, https://github.com/messari/subgraphs, /Users/jonas/Documents/web3/agentic-wallet/PLAN.md, /Users/jonas/Documents/web3/agentic-wallet/ARCHITECTURE.md

### [medium] conf 0.85: Subgraph MCP (https://subgraphs.mcp.thegraph.com/sse) requires a Subgraph Studio Gateway API key (Authorization: Bearer) and counts as a live Graph provider; Agent0/ERC-8004 subgraphs are live on Base Sepolia and Ethereum Sepolia, so an ERC-8004-registered agent can be read back through The Graph — one feature hits Hedera 'on-chain agent identity' extra points and Graph composition at once.

Evidence: VERIFIED from Subgraph MCP Claude setup page and Agent0 subgraph docs (nine networks incl. Base Sepolia 84532, endpoint pattern gateway.thegraph.com/api/<KEY>/subgraphs/id/...).

Sources:
- https://thegraph.com/docs/en/subgraphs/tooling/subgraph-mcp/claude/
- https://thegraph.com/docs/en/subgraphs/existing-subgraphs/agent0/

### [medium] conf 0.8: Privy B2B track is a GAP for a B2C wallet: it requires a 'business or organization use case' and 'at least one functional B2B workflow'; policies alone satisfy the 'control' requirement, but key quorums are gated ('Key quorums are an advanced feature... reach out to Privy's team') and intents/manual approvals are API + dashboard. Apply to it (no slot cost) with an 'agent spend management' framing, but do not build for it.

Evidence: VERIFIED from the prize text and Privy key-quorum / intents overview pages.

Sources:
- https://ethglobal.com/events/ethonline2026/prizes
- https://docs.privy.io/controls/key-quorum/overview
- https://docs.privy.io/transaction-management/intents/overview

### [medium] conf 0.8: Privy 'Best financial flow' allows mocked onramp/cards but requires one live GA flow; Stripe onramp has no testnet support, so a faucet plus a live x402 payment or policy-gated USDC transfer on Base Sepolia is the eligible path. Privy's Telegram OAuth login exists (initOAuth({provider:'telegram'})) so the Telegram-user-to-Privy-wallet binding can be native rather than hand-rolled.

Evidence: VERIFIED: prize text ('Features requiring commercial or guided onboarding may be mocked, but they do not count'); Privy fiat-onramp docs (Stripe testnet limitation); Privy Telegram login docs.

Sources:
- https://ethglobal.com/events/ethonline2026/prizes
- https://docs.privy.io/wallets/funding/fiat-onramp
- https://docs.privy.io/authentication/user-authentication/login-methods/telegram

### [medium] conf 0.8: Hedera cheap extra points: HTS token in the settlement path (testnet USDC 0.0.429274 instead of HBAR; token association required), metered pricing instead of flat, HCS audit trail (Hedera Agent Kit ships an 'HCS Audit Trail Hook'; or a direct TopicMessageSubmit), and a discoverable service card. Blocky402 hosted testnet needs no API key; Hedera mainnet is also hosted if you want a 'real money' moment.

Evidence: VERIFIED: Hedera x402 PoC README (USDC 0.0.429274 testnet, $0.001/message), @x402/hedera README (explicit token association), hedera-agent-kit-js README (HCS audit hook), Blocky402 site (hosted testnet/mainnet Hedera, testnet open access) and prize extra-points list.

Sources:
- https://github.com/hedera-dev/x402-inference-pay-per-request-poc
- https://github.com/x402-foundation/x402/tree/main/typescript/packages/mechanisms/hedera
- https://github.com/hashgraph/hedera-agent-kit-js
- https://blocky402.com/
- https://ethglobal.com/events/ethonline2026/prizes

### [medium] conf 0.85: Adjacent tracks are not 'free': Ledger requires a physical device over USB and 'must be built on the Ledger Agent Stack'; World Selfie Check is beta-flagged ('request access through your World point of contact') with a TestFlight sandbox and a mandatory feedback document; Chainlink Confidential Workflows is private beta via an account team; Bazantic is waitlist/beta; ENSv2 needs ENSv2 to be 'central'; Uniswap needs FEEDBACK.md plus a feedback form. Each also consumes one of the three partner slots.

Evidence: VERIFIED on Ledger CLI docs, World sandbox/selfie-check docs, Chainlink hello-confidential-workflows template, Bazantic developers page, ENS/Uniswap prize text.

Sources:
- https://developers.ledger.com/docs/ai-tools/ledger-cli
- https://developers.ledger.com/ethonline
- https://docs.world.org/world-id/sandbox/testing-selfie-check
- https://docs.chain.link/cre-templates/hello-confidential-workflows
- https://bazantic.com/developers
- https://ethglobal.com/events/ethonline2026/prizes

### [medium] conf 0.75: Judges have already seen 'agent wallet with spend caps on Hedera x402': Hedera's Jul-2026 x402 bounty paid Tally (upto scheme, spend ceilings, HCS receipts, MCP server) and ETHGlobal Lisbon 2026 had an 'ethui agentic wallet' finalist; Graph Lisbon winners BookerBob and atlas used Messari schemas + MCP + x402 + SKILL.md. The shared, grabbable Chrome must lead the video or the project reads as a repeat.

Evidence: VERIFIED: Hedera bounty winners post, Tally README, Graph Lisbon winners post; ethui finalist name appears in ETHGlobal Lisbon 2026 finalist listing surfaced by search (not fetched directly).

Sources:
- https://hedera.com/blog/x402-bounty-on-hedera-winners-announced/
- https://github.com/Madhav-Gupta-28/Tally
- https://thegraph.com/blog/ethglobal-lisbon-2026-winners/

## Recommendations

- Lock the three partner slots as Privy, The Graph, Hedera and apply to all five of their tracks on the submission form; do not touch ENS/World/Ledger/Uniswap/Chainlink/Bazantic — each would cost a slot and none is a 9-day fit.
- Make the demo contain TWO paid requests: (1) the agent pays The Graph per query via the x402 gateway from the Privy embedded wallet on Base Sepolia (@graphprotocol/client-x402 or Privy useX402Fetch with maxValue) — this is the live Privy financial flow and makes Graph load-bearing; (2) the agent pays your hosted Hedera service via Blocky402 testnet. Verify on day 1 that the testnet gateway actually serves a Messari lending subgraph; if not, budget ~$1 of real USDC on Base mainnet at ~$0.01/query and say so.
- Day 1 spike (8-12h, one owner): wrap Privy secp256k1_sign in a custom Hedera signer for @x402/hedera so the Hedera agent account is a Privy server wallet (key never in-process, kill switch = revoke the authorization key). If it is not working by end of day 2, fall back to a host-held ECDSA agent account and document plainly in README that the Hedera leg's cap is host-enforced.
- Build the jailbreak reject as an eth_sendTransaction on Base Sepolia with a Privy policy: to-allowlist + ethereum_calldata transfer.amount <= per-tx cap + a 24h 'rolling' aggregation referenced via field_source 'reference'. Show the Privy policy id and the rejection in the wallet pane; state in README that typed-data x402 caps are host-side (Privy aggregations do not cover eth_signTypedData_v4).
- Satisfy the Graph composable track with one Messari lending query pattern across >=3 deployments (Aave v3 ETH JCNWRypm..., Aave v3 Base D7mapexM..., Aave v2 ETH C2zniPn..., Zerolend 4Zf4doH5...) plus Subgraph MCP for discovery; ship a 20-line protocol-to-deployment registry JSON and a SKILL.md — Graph named that exact missing piece after Lisbon.
- Cheap Hedera extra points in priority order: settle in HTS USDC 0.0.429274 (2h, associate token first); meter price per protocol/bytes in the packed answer (1h); write every receipt to an HCS topic and link the HashScan message in the receipt card (2-3h); register the agent on ERC-8004 (Base Sepolia) and read it back via the Agent0 subgraph (4-6h, also counts as Graph composition). Skip A2A/ACP and Scheduled Transactions.
- Video discipline: 2-4 min, 720p, human voice, shared-Chrome grab/freeze in the first 30s, both paid requests on screen, policy reject on screen, one HashScan and one Base Sepolia tx hash visible. Add a 'Built with AI tools' attribution section and a per-sponsor 'how we qualify' checklist to the README.
- Commit early and often from 4 Sep — small commits with real history; never a single squash on 12-13 Sep.
- Apply to Privy B2B with an 'agent spend management for a team' framing only if a Privy intent / dashboard manual-approval for above-cap spend is actually wired (6-10h); otherwise mention it in README and put zero video time on it.
- Join t.me/graphhackers and the Hedera Discord on day 1 and post the architecture; ask Hedera devrel directly whether a Privy-signed Hedera account is acceptable as 'the agent' and ask Graph devrel whether testnet x402 gateway serves Messari subgraphs — both answers change day-2 work.

## Open questions

- Does the Graph testnet x402 gateway (Base Sepolia USDC) serve the Messari standardized lending subgraphs, or only subgraphs published to The Graph's testnet network? If only the latter, the live-data leg needs a Studio key or ~$1 real USDC on Base mainnet.
- Do Privy policies evaluate secp256k1_sign (raw hash) requests at all? Docs are silent; if not, the Hedera leg's cap is host-side by construction.
- Whether a Continuity-pool entry for the Graph AI track (wrapping Jonas's prior Graph layer) would be judged in a thinner pool than Start Fresh, and whether that pool choice would jeopardize Privy/Hedera eligibility ('eligibility for partner prizes may vary by event and partner'). Privy has no Continuity track at ETHOnline 2026.
- Actual ETHOnline 2026 submission volume per track (probabilities above are estimates; ETHOnline 2025 had many hundreds of hackers but per-track counts were not fetched).
- Whether Hedera judges accept a Privy-signed (secp256k1) Hedera account as 'the agent' or expect Hedera Agent Kit usage; the ETHOnline text only requires Blocky402 settlement.
- What Privy/Graph/Hedera devrel said in ETHOnline 2026 Discord/workshops this week — WebSearch budget ran out before I could pull X or Discord posts dated 4 Sep 2026; treat sponsor 'wants' as inferred from track text and Lisbon/Cannes write-ups.
- Price per query on the Graph x402 gateway (crypto.news reported $0.01 USDC; the docs page does not state it).

## All sources

- https://ethglobal.com/events/ethonline2026/info/details
- https://ethglobal.com/events/ethonline2026/info/start
- https://ethglobal.com/events/ethonline2026/prizes
- https://ethdaily.io/ethglobal-introduces-continuity-track
- https://ethglobal.com/events/ethonline2025/info/details
- https://blocky402.com/
- https://blocky402.com/docs/quickstart/
- https://blocky402.com/docs/testnet/
- https://blocky402.com/docs/examples/
- https://github.com/x402-foundation/x402/blob/main/specs/schemes/exact/scheme_exact_hedera.md
- https://github.com/x402-foundation/x402/tree/main/typescript/packages/mechanisms/hedera
- https://github.com/hedera-dev/x402-inference-pay-per-request-poc
- https://github.com/hashgraph/hedera-agent-kit-js
- https://hedera.com/blog/x402-bounty-on-hedera-winners-announced/
- https://hedera.com/blog/hedera-and-the-x402-payment-standard/
- https://docs.hedera.com/solutions/ai/x402
- https://github.com/Madhav-Gupta-28/Tally
- https://docs.privy.io/wallets/overview/solutions/agent-wallets
- https://docs.privy.io/controls/policies/overview
- https://docs.privy.io/controls/policies/stateful-policies
- https://docs.privy.io/controls/policies/example-policies/ethereum
- https://docs.privy.io/recipes/x402
- https://docs.privy.io/wallets/using-wallets/ethereum/sign-a-raw-hash
- https://docs.privy.io/recipes/agent-integrations/agentic-wallets
- https://docs.privy.io/wallets/using-wallets/signers/overview
- https://docs.privy.io/wallets/actions/transfer/overview
- https://docs.privy.io/controls/key-quorum/overview
- https://docs.privy.io/transaction-management/intents/overview
- https://docs.privy.io/authentication/user-authentication/login-methods/telegram
- https://docs.privy.io/wallets/funding/fiat-onramp
- https://github.com/privy-io/privy-agentic-wallets-skill
- https://ethglobal.com/events/newyork2026/prizes/privy
- https://thegraph.com/docs/en/subgraphs/tooling/subgraph-mcp/introduction/
- https://thegraph.com/docs/en/subgraphs/tooling/subgraph-mcp/claude/
- https://thegraph.com/docs/en/subgraphs/tooling/x402-payments/
- https://thegraph.com/docs/en/subgraphs/existing-subgraphs/standard-subgraphs/
- https://thegraph.com/docs/en/subgraphs/existing-subgraphs/agent0/
- https://thegraph.com/blog/hackathon-resources/
- https://thegraph.com/blog/ethglobal-lisbon-2026-winners/
- https://thegraph.com/blog/querying-blockchain-data-natural-language-mcp-skills/
- https://thegraph.com/blog/onchain-agent-infrastructure/
- https://github.com/messari/subgraphs/blob/master/deployment/deployment.json
- https://ethglobal.com/events/cannes2026/prizes/hedera
- https://ethglobal.com/events/lisbon2026/prizes/hedera
- https://ethglobal.com/events/cannes2026/prizes/ledger
- https://ethglobal.com/events/cannes2026/prizes/ens
- https://developers.ledger.com/ethonline
- https://developers.ledger.com/docs/ai-tools/ledger-cli
- https://docs.ens.domains/ensv2/overview
- https://docs.ens.domains/ensv2/tutorial-app-developers
- https://docs.ens.domains/ensip/26/
- https://docs.world.org/world-id/sandbox/testing-selfie-check
- https://docs.world.org/world-id/sandbox/sandbox-access
- https://docs.chain.link/cre-templates/hello-confidential-workflows
- https://bazantic.com/
- https://bazantic.com/developers
- https://developers.uniswap.org/hackathon-feedback
- https://hol.org/docs/standards/hcs-14/
