# Status against the plan

**11 September 2026, evening — High bugs still real at HEAD:** the signed-out freeze promise (B-01) was already gone. This pass shows a chip per stubbed integration and `balanceLabel` on the Wallet, renders `ledgerNote` when spend history cannot be read, lists a minted token's true unrestricted reach, and scopes service-task reads to the connection that created them. B-02 and B-03 stay unwired until after submission. B-08 ("Send a test now") is still a product call.

**11 September 2026, afternoon — The Graph, two composed products:** the twelve pinned deployments are now pinned by ipfs hash rather than subgraph id, read from the gateway by that hash on both the Studio and the x402 paths, and every answer's `_meta.deployment` is checked against the pin, so the receipt's "exact indexed artefact" is true rather than approximately true; the hash shows on the receipt beside the block. And `graph_discover` asks The Graph's hosted Subgraph MCP by name or by contract and hands `graph_query` a deployment to read beside the pinned ones under the same standardized query — the composition the prize audit promised and [GRAPH.md](../evidence/GRAPH.md) had recorded as not used. Both driven live that afternoon: 12/12 fresh by hash, a Moonwell search and a Uniswap v4 contract lookup answered in under a second each. Options researched and not taken (on-demand subgraph deployment, Substreams, the Token API) are in [graph-expansion-2026-09-11.md](../research/graph-expansion-2026-09-11.md).

**11 September 2026 — Review and cleanup:** a review of the 9–10 September work found the code in good shape — at `7f786a9` the type-aware lint, typecheck and 990 unit tests are green; no `as any`, no `@ts-ignore`, every lint suppression on its own line with a reason — and the drift in the documents and the copy. Landed today, one commit each and named by subject because their hashes change until they are pushed: the Robinhood research prompt off the root and into `docs/research` (it had been the one thing in `git status` and was turning `check:fast` red for every agent), the signed-out page no longer promising a freeze, the 6 September UI plan pack archived and the pasted transcript removed, the README brought up to the tree, this entry, the allowance's real reach recorded in decision 0019, the trading-capital decision renumbered 0021, and the workspace tool's launcher untracked. Two leash gaps are confirmed at HEAD and deliberately **not** wired before the video: the action-kind table in `packages/domain/src/authority.ts` is never handed to `authorize` (the judgement in `session.ts` carries no `allowance`), and a cap change reaches Privy and the store but not the live mandate (`applyAllowance` has no caller). They are B-02 and B-03 in [the bug triage](../product-description/bug-triage.md) and one change after submission; the review, the sequenced cleanup and the reasons are in [REVIEW_2026-09-11.md](REVIEW_2026-09-11.md).

**10 September 2026 — Four lanes in one day:** the **agent door** (`8263461`..`cf8e688`, [decision 0020](../decisions/0020-agent-door.md), [what was checked](../evidence/AGENT_DOOR_FABLE51.md)): a stdio MCP server served at `/froggy-mcp.mjs` for agents that are not ours, paying from their own Hedera account, with a free catalogue and a free receipt view; a stranger's real purchase is still pending, and on 11 September whether the door stays at all became a team decision in progress. **Robinhood Chain** in the trading lane (`55171da`, `b92aecc`, `e333e00`): Birdeye markets, Uniswap quotes on `eip155:4663` by default, and the read-only `pons_token` tool. **Person-owned Privy policies** ([decision 0019](../decisions/0019-user-owned-policies.md), `97eeb90`..`152dfe8`): each person's agent signs under a policy they own, minted at first need and editable from the live site through a browser-signed relay. **Home as the front page** (`e9eb902`): Home, Explore and Wallet are the whole navigation, Connections and Account sit behind them, the rail stays at desktop widths and the pill on phones; the approved frog with five poses (`7072034`) and the preproduction design pack under `design/preproduction/`. And the outside-in **behaviour description** at `docs/product-description/` (57 files) with its [bug triage](../product-description/bug-triage.md): eight high findings, none yet checked against the running app.

**9 September 2026 — Browser Use is the only browser:** local Chrome is removed — the `Bun.WebView` launch, Chromium detection, profile locks, the popup re-opening workaround, the per-user worker process and its IPC protocol, the Chromium layer in the image, `BROWSER_PROVIDER`, `CHROME_PROFILE_DIR` and `FROGGY_CHROME`. Every browser is a Browser Use hosted browser driven over CDP; a missing key is a loud stub and `browser` is now a service mode. Live API probing found two bugs no local fixture could: `cdpUrl` is an HTTPS endpoint, not `wss:`, so the socket is resolved from Chrome's `/json/version`; and browsers, profiles and billing are `/api/v3`, not `/api/v4`. Browsing is paid-only everywhere, and the seat cap now bounds provider spend and concurrency rather than this box's memory. Deployed as `c9de114` (Railway `ea1e81d6`, 11:40 UTC); `BROWSER_USE_API_KEY` and `BROWSER_COUNTRY=us` are set on the production service and `/health` reports `browser=live`. The adapter was driven against a real hosted browser end to end — navigate, snapshot, takeover, resume, stop, cost recorded — for $0.000334. **9 September, later:** browsing is unblocked. `BROWSER_MODEL_*_USD_PER_MILLION` are set to `2.00`/`6.00` — `qwen3.8-max`'s published flat list rate, used deliberately as a ceiling a prepaid plan cannot exceed, so the failure mode is a task that ends early rather than one that loses money. A deadlock in the pane was also found by driving the real UI against a real hosted browser: the hosted browser starts with the agent owning it, every human message was refused with "Take control before interacting", and the Take control button is disabled until the browser runs — which only happens once someone navigates. A person's own input now takes the page the way the panic button does. Typing a URL provisions a browser and renders the live viewer in the pane with the page visible. [Decision 0018](../decisions/0018-browser-use-only.md), [setup and URLs](../BROWSER_SETUP.md), [evidence](../evidence/BROWSER_USE_MIGRATION.md).

**8 September 2026 — Agent onboarding:** Home and Wallet now offer **Copy for your agent**, with manual-copy recovery, then switch to a connected-agent count. Agents is instructions, connection rows, Telegram, and collapsed advanced tokens. Public `/llm.md` and `/skill.md` share the deployment-specific installation and usage source; `/froggy/SKILL.md` remains an alias. `/agents/:id` shows scopes, dates, disconnect, and the newest 50 MCP/task/payment invocations, with task result links and signed-versus-paid amounts. Migration `0011_fixed_pretty_boy.sql` adds the metadata trail with a memory-store twin. Verification: **107/107 browser tests** with two workers and a 90-second per-test budget, the full gate, Knip, production build, and isolated Postgres contracts. [Install text and verification](../evidence/HERMES.md#simplified-onboarding--8-september-2026).

**8 September 2026 — Motion second pass:** visible directional page slides, a spring pill indicator and menus, rising/staggered stream cards and wallet rows, spring approvals and receipts, live shimmer and word fades, 600ms torph totals, press feedback, and a whole-page appearance crossfade. Reduced motion, keyboard immediacy and held controls are covered; the full browser suite passes **96/96**. [Second-pass decisions and evidence](../design/MOTION_NAVIGATION_2026-09-08.md#second-pass--8-september-2026).

Updated Thu 11 Sep 2026. [Iteration 3](ITERATION_3.md) contains the recovered 7 Sep decisions and current completion list; [NEXT_ITERATION.md](NEXT_ITERATION.md) retains the broader submission plan. Dated sections below are historical implementation notes.

- **Tue 8 Sep, 15:05 CEST (Session F):** Kristjan asked to drop the Integrations card (live/stub badges) from Settings now that every integration is live; removed with its component. `/health` still reports the modes for operators. Also verified at 14:55: the browser consent button attached the agent signer to Kristjan's wallet at Privy (`docs/evidence/PRIVY.md`).

- **Tue 8 Sep, 15:55 CEST (Session F):** `X_API_BEARER_TOKEN` set on the Railway app service and validated against X; after the redeploy the X search service reads configured. Also today from Session F: the Integrations card left Settings (`4c11acb`), the daily digest got "Send a test now" (`b001e24`), torph number morphing (`76a11d0`), the Add-funds dialog leads with the address (`ffca8fe`), and the pages-and-modules proposal (`docs/plan/IA_PROPOSAL_2026-09-08.md`) awaits three answers.

- **Wed 9 Sep, afternoon (Session F):** Funding now accepts other chains. Privy mints a deposit address in the browser (`identity.startDeposit`), the person picks a chain and token, and it arrives as USDC on Base; EVM and Solana only, not Bitcoin and not Hedera, and the dialog says so. Server-side minting is impossible on this app and the spikes that prove it are in [ADR 0015](../decisions/0015-deposit-addresses.md). Also today: the balance no longer reads unavailable when the public Base endpoint rate-limits (`3c5d304`), the funding landscape research is at `docs/research/funding-landscape-2026-09-09.md`, and the X credential is live.

## Motion and navigation lane — 8 September 2026

Built on Passbook/Lilypad and retained their tokens, typography and financial behaviour. Motion landed first in `00d8cfd`; the pill navigation is the next coherent commit. [Audit, decisions and review](../design/MOTION_NAVIGATION_2026-09-08.md).

- **Motion:** 125ms page and stream enter/exit fades with stable targets, inert exiting approvals, 200ms torph figures, an appearance indicator that moves behind fixed buttons, interruptible 200/250ms dialogs and sheets with 125ms exits, and a reserved browse-status cell. Keyboard actions are immediate; reduced motion removes movement. Repeated word animation, tool shimmer, shadow breathing and unused rise-in were removed. Review caught and corrected a hidden appearance focus outline.
- **Navigation:** one bottom-centred pill at every width, containing Chat, Wallet, Services and More. Agents and Settings are inside More, which opens above its trigger. One `Primary` landmark includes the popover; links and More identify the current page, and Chat retains the approval-waiting badge. Targets are at least 44px; the composer and scrolling pages reserve space above the pill and phone safe area. The desktop rail and separate phone tab bar are removed. Brand and connection status remain in the header.
- **Browser verification:** `FROGGY_E2E_PORT=3500 heavy bun run e2e --workers=4` passed **all 88 tests**, with explicit local stubs. Twelve new tests cover motion/focus and navigation at 1440, 768, 390 and 320px, both themes, keyboard traversal, focus return, outside/Escape dismissal, composer clearance and the waiting badge across page changes. The four changed existing spec files only replace five-visible-link assertions or open More before selecting Agents/Settings. One full-suite attempt was terminated by SIGTERM; its orphaned stub server was cleaned up and the complete suite rerun successfully.
- **Gate:** `heavy bun run check:fast`, the full `heavy bun run check`, and standalone `bun run knip` all passed before the navigation commit and push.
- **Captures:** [Passbook desktop](../evidence/motion-navigation-2026-09-08/passbook-pill-1440.png), [Lilypad desktop](../evidence/motion-navigation-2026-09-08/lilypad-pill-1440.png), [Passbook phone](../evidence/motion-navigation-2026-09-08/passbook-pill-390.png), [Lilypad phone](../evidence/motion-navigation-2026-09-08/lilypad-pill-390.png), [Passbook More](../evidence/motion-navigation-2026-09-08/passbook-more-390.png), [Lilypad More](../evidence/motion-navigation-2026-09-08/lilypad-more-390.png).
- **Not done:** the leash meter is absent from this checkout after the earlier removal of spending-limit UI; this lane does not restore it. Physical-device touch testing and the restyle handoff's separate screen/copy rearrangements remain outside this lane. No onchain spending, Privy-policy change or Railway-variable change was made; nothing was posted to external chat.

## Restyle lane — 8 September 2026: Passbook and Lilypad

Implemented the component styling from [the FABLE51 handoff](../design/SCREENS_HANDOFF_FABLE51.md), after reading the wallet brief and design-sync conventions and opening the HTML board in Chromium. Both directions use the existing component tree. Passbook is the default light palette; Lilypad applies through the existing `.dark` class. Inter Tight replaces the two previous sans faces; `text-machine` / `font-mono` use IBM Plex Mono in Passbook and Inter Tight in Lilypad.

- **Switch versions:** Settings → Appearance → **Passbook**, **Lilypad**, or **System**. The choice persists in this browser under `froggy-theme`; System follows device colour-scheme changes. Open `/wallet?theme=passbook`, `/wallet?theme=lilypad`, or any app URL with `?theme=system` for a preview on load without changing the saved preference. The preview lasts through in-app navigation in that tab; a reload without the parameter uses the saved choice. Choosing in Settings removes the override and saves that choice.
- **Styled:** semantic colour tokens, heading/body/label steps, 20/22px cards, 12/14px rows and chips, Passbook rounded actions and Lilypad pill actions, gradient primary buttons, circular icon buttons, inset controls, card/dialog shadows, inputs, badges, tabs, tables, empty states, tool/status rings, and wallet/receipt figures. The main wallet figure is 32/36 in both themes. `text-money`, `text-machine`, tabular figures, `MorphText`/torph, accessible controls and reduced-motion support remain.
- **Commits:** `66f6b63` shared component systems and fonts; `f59d4ef` remaining surface outlines converted to inset surfaces/status rings; `ebbe39d` persisted appearance choice and browser coverage.
- **Verification:** `heavy bun run check:fast` and `heavy bun run check` passed, including type-aware lint, typechecks, package boundaries, unit tests and Knip. The final `FROGGY_E2E_PORT=3300 heavy bun run e2e --workers=4` passed **all 76 browser tests** (67 existing and 9 new). New coverage exercises saved choices, query override precedence, System changes, wallet figures and dialogs at 1440, 768, 390 and 320px with reduced motion; screen/theme checks report no browser console or page errors. All browser flows used the explicit local stubs.
- **Comparison captures (local stubs):** [Passbook wallet, desktop](../evidence/restyle-2026-09-08/passbook-wallet-1440.png), [Lilypad wallet, desktop](../evidence/restyle-2026-09-08/lilypad-wallet-1440.png), [Passbook wallet, phone](../evidence/restyle-2026-09-08/passbook-wallet-390.png), [Lilypad wallet, phone](../evidence/restyle-2026-09-08/lilypad-wallet-390.png), [Passbook Settings](../evidence/restyle-2026-09-08/passbook-settings-1440.png), [Lilypad Settings](../evidence/restyle-2026-09-08/lilypad-settings-1440.png), [Passbook funding at 320px](../evidence/restyle-2026-09-08/passbook-add-funds-320.png), [Lilypad funding at 320px](../evidence/restyle-2026-09-08/lilypad-add-funds-320.png).
- **Not done in this lane:** the screen rearrangements, consumer-copy changes, new charts, and data/control moves described in handoff section 4. Existing page structure and financial behaviour are retained. The Claude Design export snapshots have not been regenerated. No onchain spending, Privy policy change or Railway-variable change was made.

## Current release

Froggy has one dollar balance, automatic USDC-to-HBAR conversion when a payment needs it, and three destinations: Home, Explore and Wallet, with Connections and Account behind them. Spending-limit controls are hidden by default; each person's agent signs under a Privy policy they own and can edit from Wallet (decision 0019); provenance, allowlists and the policy engine remain. Telegram supports scheduled reminders and reports, with cancellation in Settings.

Production runs on Hedera mainnet and Base mainnet. Its health check was healthy during the recovery, with all six integrations live. Existing financial proofs are in [MAINNET_RELEASE.md](../evidence/MAINNET_RELEASE.md). Treasury supplier rules and payees were activated earlier on 7 Sep; `X_API_BEARER_TOKEN` remains absent and provider delivery proofs remain pending in [MARKETPLACE.md](../evidence/MARKETPLACE.md).

The interrupted OAuth worktree is merged and deployed as `3179edb`: remote `/mcp`, consent and manual-code pages, `froggy login`/`logout`, grants in Agents, and database migration 0009. The recovery also fixes concurrent code replay, refresh resource validation, the separate legacy-token display and CLI login process completion. CI passed all gates and 56 browser tests; Railway succeeded on 7 Sep at 13:56 UTC. Production metadata, unauthenticated discovery and invalid-token refusal passed. [HERMES.md](../evidence/HERMES.md) records the release and distinguishes it from the signed-in live checks still required.

Remaining: exercise a real OAuth client; the signed-in funding/conversion journey; real Telegram scheduling and restart recovery; X configuration and paid provider delivery proofs. Read-only production inspection confirmed Privy still holds the previous 2 USDC per-transfer and 5 USDC rolling daily top-up caps on both Base networks; the planned 10/25 update still needs to be applied. Card checkout is conditional on those lanes completing. Branding, repository publication and demo recording remain team decisions. See [ITERATION_3.md](ITERATION_3.md) for the order and owner inputs.

## Lane 4 landed on Mon 7 Sep (two-way Telegram)

Branch `worktree-agent-acc848be4cb1b1377`, on top of lane 1: `50ee258` (contracts: `Schedule`, `ScheduleRequest`, the `notice` message, `RunSurface` `schedule`), `c2f195d` (the `schedules` table and migration 0008, `Store.schedules` with the claimed tick, `schedules.ts`, `notices.ts`, `TelegramPager.notify`, the `notify` tool, `runScheduledFor` with the digest and prompt jobs, `/api/schedules`), `596a7c5` (the `schedule`, `schedules_list` and `schedule_cancel` tools and the prompt paragraph). ADR `docs/decisions/0011`. The Settings list and its browser test subsequently landed in `f23a3f2`; real Telegram delivery remains unverified by the recovery session.

## Landed on Sat 5 Sep by the Opus session (commits `3622c96`..`5db02e8`)

| Plan item | State | What landed | Still open |
| --- | --- | --- | --- |
| 1.7 auth | done | Privy access token on every `/api/*` route (bearer) and both sockets (subprotocol, `packages/protocol/src/handshake.ts`); missing `Origin` refused; `e2e/auth.spec.ts` | token cache, `DEMO_USER_DID` |
| 1.3 per-user sessions | partial | `apps/server/src/workspaces.ts`: one `WorkspaceSession` + one in-process `BrowserSession` per DID, `MAX_BROWSERS` (0 = unlimited), hashed profile directories | worker process per user (`Bun.WebView` is one Chrome and one profile per process, so in-process sessions share a profile), queue, idle kill, `freeze()` as one function, parked approvals |
| 1.4 ledger | partial | Postgres ledger keyed on the DID (`packages/wallet/src/ledger-postgres.ts`), `users` table, migration `0000`, `preDeployCommand` as a shell string, `database=stub` mode; same-key concurrent spends settle once (ownership + in-process join); HBAR priced from the mirror-node exchange rate, fails closed | rolling-cap read is still outside the reserve critical section for different keys; `refused`/`abandoned` rows; runtime decode of `SpendIntent`; frozen/aborted re-check before the outbound call; receipts and mandates persisted |
| 1.5 outbound hygiene | done (`f491806`) | `x402_fetch` consults the host allowlist before the first request | private-IP/scheme checks, redirect re-validation, `browser_navigate` scheme check, `x-payment-response` decoded as base64 `SettleResponse` |
| 1.6 model | open | — | placeholder-prefix bug in `model.ts`, DashScope wiring, daily budget |
| 2.3 Graph registry | mostly done | four pinned Messari deployments, `_meta` freshness gate (2 h), deployment ids and blocks on evidence, `GRAPH_SUBGRAPH_ID` removed | x402 gateway provider paid by the demo wallet; the oracle body as the metered four-deployment brief |
| 2.4 Privy | partial | wallet minted by Privy at login and owned by the user; the agent is added as an additional signer under `docs/privy-agent-policy.json` (rule a only: typed-data `TransferWithAuthorization` to The Graph, Base mainnet USDC, ≤ $0.25), authorised by the user's access token; freeze revokes the signer; `tools/privy-agent-key.ts` | the signing call itself (typed data through the agent key), rule b top-up + aggregation, a policy-owner key, `wallet_send` producing a raw Privy denial |
| 2.1 Hedera live | open | — | `extra.feePayer` from `/supported`, response header decode, first settlement |

## Deviations from the plan, adopted

- The Privy wallet is client-minted at login (`createOnLogin: "all-users"`) and user-owned; the server asks to be added as a signer with the user's token. This replaces the plan's server-created wallet, so the owner step "embedded-wallet creation on login **off**" is reversed: leave it **on**.
- `UserId` is the branded Privy DID, not a TypeID. Profile directories are the hashed DID.
- The policy's per-payment cap is $0.25 rather than the spec's $0.02; the mandate's own per-transaction rule stays the binding number on screen. Tighten the JSON when the Graph payment path lands.

## Landed by this session (Sat 5 Sep)

- P0.2 `8c99fd1`: Bun 1.4.2 pinned everywhere; hand-written `Bun.WebView` types removed.
- P0.4 `07d41c8`: planning docs moved to `docs/plan/`, research to `docs/research/`, links rewritten, `docs/README.md` index.
- P0.3 `8f060ff`: every prior-project name scrubbed from code and docs; `tools/check-names.ts` in `bun run check`.
- P0.5 `3c860e9`: Railway runbook, profile volume 10 GB (applied), model variables preserved. `checkSuites` is true live.
- P1.6 `56290f8`: model chosen by configuration (`selectModelProvider`), DashScope wired, the scripted model pays the oracle so a keyless run reaches the ledger and a receipt.
- P1.1 `09bac94`: CAIP-2 network ids (`eip155:8453`, `eip155:84532`, `hedera:testnet`), Base mainnet USDC known, `user` provenance.
- P1.2 `edf4435`: one browser worker process per user (`packages/browser/src/{worker,worker-host,worker-serve,remote}.ts`, `packages/protocol/src/worker.ts`), env allowlist, frame credits, cast-only-while-watched, private-network blocklist per tab, freeze reaches the browser, ADR 0006. Real spawn verified on the build box.
- P1.3: seats and queue (`MAX_BROWSERS=8`, `RESERVED_BROWSERS=1` for `DEMO_USER_DID`, position on `BrowserState.queue`, automatic seating with the queued start replayed), idle sweep (`BROWSER_IDLE_MS`), `freeze.ts` as the one kill-switch function (mandate → run → parked approvals → browser → Privy signer), `interactions.ts` parking registry with the three-way race, `approval.resolve` honoured for the owning user only, open cards re-sent to a reconnecting tab, `POST /api/chat` answers 423 while frozen.
- P1.5 `f491806`: `publicHttpUrl` (scheme, credentials, literal private/loopback/link-local/CGNAT addresses, `.internal`/`.local` names), `safeFetch` (DNS refuse-private, manual redirects re-checked per hop, 15 s, 1 MB), both tools validate before anything is sent; `x-payment-response` is the base64 `SettleResponse` envelope on both sides.
- P2.7 `539d0a3` (code; message on `9c2150c`): `ask` parks the tool call on a card through `InteractionRegistry`, outside the session lock; the answer is judged again with `approved`; `allow_session` writes an `ask_exemption` rule; `deny_stop` freezes with the answerer's token; timeout/aborted/unavailable are denial codes; `Receipt.approval` records the id and resolution. The in-app card itself is P3.5.
- P3.0–3.6 `41dbe07`: the light workspace. Header strip (spent vs the widest rolling cap, driving dot, one stub chip, Freeze with confirm on unfreeze), the stream with receipts filed under the turn that produced them (server stamps `runId`/`at` on every assistant message), the live page as an inline card with a driving ring and a compact strip while it is scrolled away, tickets for receipts and approvals, the details drawer (policy, history, wallet), sign-in states (loading/failed/ready). Reducer, stream model, painter and letterbox mapping are unit-tested; e2e updated.
- P3.7 `32d5569`: mandate editor in the drawer (`mandate.update`), `DELETE /api/me` wipes store rows, the workspace and the profile directory (`Store.forget`, `Workspaces.forget`); `e2e/policy.spec.ts`.
- P3.8–3.9 `46367d2`: pop-out to a split pane (draggable, remembered width) and to `/browser` in its own window (`BroadcastChannel` claim/release, the tab drops its browser socket meanwhile); queue/starting/crashed overlays; phone is watch-only; `e2e/pop-out.spec.ts`.
- P3.5/3.11 `3763463`: approval round trip e2e (`e2e/approval.spec.ts`), plus policy and pop-out specs; 19 e2e green.
- P2.11 `7b1cb1d`: daily digest — schedule per user (migration 0001, `GET/PUT /api/digest`, drawer control), minute scheduler (`scheduler.ts`, tested), `runDailyFor` on the same loop with `interactive: false`, one minute / twelve steps / five cents / one paid request, report to a sink.
- P4 `c2759e0`: Telegram via the Chat SDK (ADR 0007): pairing codes (`/api/telegram`, `/start CODE`), digest card, approval card wired into the same `InteractionRegistry`, `/freeze` and a freeze button, DM chat on the shared `turn.ts` loop recorded into the run; `run.started` app message; migration 0002; live only with token + webhook secret. Owner: BotFather token, secret, `setWebhook`, `TELEGRAM_BOT_USERNAME`.
- P2.8 `8bf511f`: 402 probe (`packages/payments/src/probe.ts`, per-option reasons), `x402_probe` tool, per-user directory (migration 0003, `GET/POST/DELETE /api/directory`, `POST /api/directory/probe`), adding an entry puts host and payee on the mandate's allowlists (`session.allow`/`disallow`), Directory tab in the drawer with paid counts.
- P5 skeletons `79a38d7`: `docs/evidence/{HEDERA,GRAPH,PRIVY,AI-USE}.md` with what is live and `TODO` marks for the on-camera beats; README picture and surfaces updated.
- P3.11 `068113d`: `e2e/directory.spec.ts`; 21 e2e green.
- P6.1 `146b3e3`: WebMCP producer — four tools on `navigator.modelContext` when present (three read-only, `pay_402` consequential and routed through an ordinary agent turn), descriptors unit-tested, drawer reports availability. No browser on this box exposes the API, so registration itself is unverified.
- P2.5 `a438252`: Privy-signed EVM x402 — `privyTypedDataSigner` under the agent key, `evmPayer` via `@x402/evm` for Base and Base Sepolia, both x402 dialects on buyer and seller, payer chosen per offer, `Receipt.failure` carries the signer's refusal verbatim. Unverified live: needs a signed-in wallet with the agent granted, and USDC on Base for a settlement.
- P2.6 (partial) `87f6976`: HCS audit trail — `liveHcsWriter` (creates a topic at first use when none is pinned), one note per settlement on both sides, `Settlement.hcsSequence` on the receipt and the ticket stub. Verified live: topic `0.0.10381647`, notes #1 sold and #2 paid, settlement `0.0.7162784@1788632323.333261031` (pocket −0.05 HBAR, payee +0.05, facilitator paid the fee). Pocket-per-user and reconciliation remain.
- P2.3 second half `e365eef`: pay-per-query Graph — `GraphTransport` on the live client (`studio` or `x402`), `GRAPH_PAY_PER_QUERY`, one `paid-request.ts` choke point shared by `x402_fetch` and the Graph transport, one payment per deployment per minute. Needs USDC on Base in a signed-in wallet to run live.
- Deploy: the check-suite gate had held everything since `3763463` (CI red on an unformatted STATUS, then a name-check line; doc-only commits are skipped by Railway's watch paths). `13baf25` deployed at 17:58 UTC with migrations 0001–0003 applied; live `/health` shows `telegram=stub`.

## Blockers for the owner (as of Sat 5 Sep, 23:30 CEST)

Everything below is a key, a dashboard toggle, money, or a click that no session can do.

1. **`DEMO_USER_DID`** — the judge or demo account's Privy DID, so the reserved browser seat and the pay-per-query demo know whose wallet is the demo wallet. Set on Railway.
2. **USDC on Base** in the demo wallet (about $5) — needed for the first Privy-signed x402 payment to The Graph's gateway and any Base seller. Then set `GRAPH_PAY_PER_QUERY=true` on Railway and add `https://gateway.thegraph.com/api/x402/subgraphs/id/JCNWRypm7FYwV8fx5HhzZPSFaMxgkPuw4TnR3Gpi81zk` to the demo account's directory (Details → Directory → Probe → Add). Without funds the signature still happens and the facilitator refuses; that refusal lands on the receipt.
3. **Telegram** — a bot from @BotFather; on Railway set `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET_TOKEN` (any long random string) and `TELEGRAM_BOT_USERNAME`; then register the webhook: `curl "https://api.telegram.org/bot$TOKEN/setWebhook" -d url="https://app-production-58dd.up.railway.app/telegram/webhook" -d secret_token="$SECRET"`. Pair from Details → Wallet → Telegram.
4. **Privy dashboard** — confirm the Railway origin is in allowed origins and that a real login mints the embedded wallet and grants the agent signer (the wallet strip says "agent signer: granted"). A policy-owner key for wiping rules on freeze (plan 2.10) is still to create.
5. **Repo public** — after a last look at `docs/plan/` (the FABLE51 files are kept as written).
6. Not blockers, for the record: `HEDERA_HCS_TOPIC_ID=0.0.10381647` is set locally and on Railway (created by the server on 5 Sep); the three untracked `.claude/`, `.codex/`, `.grok/` directories are a workspace tool's and stay untracked.

## Landed by session A (the planning session), Sat 5 Sep evening to Sun 6 Sep

- A1 `ee726c2`, `005acaf`, `1516bde`, `2b19f61`: the Privy signer signs transactions as well as typed data (`packages/wallet/src/evm-signer.ts`); an ERC-20 encoder, a six-method JSON-RPC client and an ERC-20 transfer the host broadcasts (`erc20.ts`, `evm-rpc.ts`, `transfer.ts`); a person-typed payee passes the host allowlist and meets Privy's policy instead; `wallet_send` is a real transfer with Privy's refusal on the receipt; the committed policy gained rule (b) (Base Sepolia USDC to the treasury, 2 USDC per transfer, 5 USDC per rolling day through aggregation `mpjhq6o0t9gzdvg3x0x4gmb1`) and an expiry, applied with `bun run privy:policy`; the denial transcript is in `docs/evidence/PRIVY.md`. `TREASURY_EVM_ADDRESS` is the pocket account's own EVM alias, set locally and on Railway.
- A2: the pocket as a per-user balance (`users.pocket_usd_micros`, migration 0004, `Store.pocket`), a `pocket_exhausted` denial in the pure policy, the draw-down under the reservation lock with a refund on failure, a starting allowance credited once (`POCKET_STARTING_USD`), `pocketUsdMicros` on the wallet summary and in the strip and drawer; `wallet_topup` signs a USDC transfer to the treasury under rule (b) and credits the pocket one-to-one; freeze zeroes the pocket. A saved mandate is re-listed with the server's own payees on load, so the treasury is payable for people whose mandate predates it.
- 1.6 budget: `apps/server/src/budget.ts`, turns and steps per person per UTC day (`MODEL_RUNS_PER_DAY=40`, `MODEL_STEPS_PER_DAY=400`, the demo account exempt), counted at turn start and between steps for the web chat and Telegram alike; a refused turn is a 429 with the reason, shown as a notice in the workspace and posted in the Telegram thread. In memory: a redeploy forgets the counts, which is the bounded loss this protects against anyway.
- 2.6 (unlock and card): a paid answer mints a one-time, ten-minute link (`apps/server/src/unlock.ts`, `GET /unlocked/<token>`) and the agent is told to open it in the shared Chrome, so the person watches the page unlock: seller, price, transaction with explorer link, HCS note, answer. `GET /.well-known/x402.json` is the service card, built from the same challenge the 402 carries. Not built: mirror-node reconciliation on a facilitator timeout.
- Evidence and README: the twelve deployments' live blocks (12/12 fresh, 6 Sep 06:09 UTC) in GRAPH.md; a real paid request against the **hosted** oracle from our own payer code, settlement `0.0.7162784@1788674975.439553201`, in HEDERA.md; the README gained the demo in order, the on-chain evidence table, the honesty box, the not-in-scope list and the team. `tools/spikes/history-secret-scan.sh` walks every commit for credential shapes and is the gate before the Sat 12 flip; it is clean.
- Open in this lane, all needing a signed-in person: `wallet_send` and `wallet_topup` run live from a wallet the agent has a signer on (the app's one user wallet carries none yet; owner check), the typed-data x402 payment once the demo wallet holds USDC on Base, the on-camera runs.
- **The chat surface, Sun 6 Sep** (`caa4564`..`0ce45ca`, ADR 0008): the shadcn chat components ejected into `packages/ui` (message-scroller, message, bubble, marker, collapsible) with `@shadcn/react` allowed; the stream on the message scroller, anchored to the person's message, `role="log"` busy while a turn runs, a shimmering marker in the gaps; Streamdown renders the agent's words as markdown while they stream (links http(s)/mailto only, no images or raw HTML), and the scripted model closes in markdown so a keyless run exercises it; tool cards say what a call came to in the tool's own words (one reader of state, verbatim fixtures in tests), a money tool running under an open approval shows "waiting for you"; a receipt names its tool call (S1) and is the body of that card, with the layer that refused in plain words, explorer links and a stamp-in for a fresh refusal; `graph_query` returns fields beside its prose (S2, schema in `packages/protocol/src/tools.ts`) and the card lays out the rate, the runners-up and every index's freshness; a run of page steps is one browse card; the empty screen is the mandate as a ticket plus the demo in order; freezes, unfreezes and top-ups are markers in the conversation; state-earned suggestion chips; the composer says what it waits for and honours a composing input method; the page strip floats; a phone gets its own header row; one live region for a screen reader. 24 e2e (`e2e/stream.spec.ts` new). **Phase 2 landed the same day** (`1d5ad59`..`e753bc3`): the pause, the answer and a turn started from Telegram or the digest are markers in the conversation (the latter with its Reload button, no longer a notice); Enter during a turn queues one message; `/freeze`, `/stop`, `/status`, `/topup N` (no `/unfreeze`, on purpose); copy on a finished answer and Try again on an errored turn that spent nothing; a new ticket focuses "Not this time" unless the person is typing; the welcome carries `hcsTopicId` and `policyId` (S3), the HCS chip links to the note and the mandate ticket and drawer name the signer policy; `wallet_status` as a small table; a cost line per turn. Part D (memo, content-visibility) waits for the Thu 10 Sep freeze.

## Lanes (Sat 5 Sep, 21:00 CEST)

Two sessions share this tree tonight. Each claims a task here before touching code and commits by pathspec; a task listed under one session is not picked up by the other unless it stays unclaimed for an hour after that session's last commit.

- **Session A** (the planning session, `session_011fhisYdtVt2vh8DtW4MkPU`): done above: 2.4 remainder, 2.9 top-up with the pocket balance, 2.10 remainder, 1.6 budget, 2.6 unlock and card, evidence and README. **Sun 6 Sep: the chat surface upgrade, Parts A, B, S1, S2 and Phase 2 all landed; only Part D (after the Thu freeze) remains** (plan file `use-ask-user-qeustion-shimmying-cray.md` on the box): `apps/web/src/components/stream/**`, `apps/web/src/lib/{tool-*,keymap,stream-model,suggestions,denial,turn-model,slash}*`, `apps/web/src/components/{composer,announcer,top-bar}.tsx`, new `packages/ui` registry components (message-scroller, message, bubble, marker, collapsible), `apps/server/src/model.ts` final text, and the two additive server changes S1 (`toolCallId` on receipts: `receipt.ts`, `session.ts`, `paid-request.ts`, `tools.ts`) and S2 (structured `graph_query` output: `tools.ts`, `turn.ts`, `jobs.ts`). Please stay out of those paths.
- **Session B** (the building session that landed everything above): unclaimed at 21:00; P6.2 WebMCP consumer and 6.3 injected provider are the only stretch items left, after the owner blockers above are cleared.

## Landed by Session F (the facilitator session), Sun 6 Sep evening

Iteration-2 core per `docs/plan/NEXT_ITERATION.md`, all in stub mode, gate and 25 browser tests green:

- `b819e32` sales, tasks and agent-token records: domain schemas, tables, migration 0005, store sections in memory and Postgres; ledger status `uncertain`.
- `4a9d213` seller durability: the sale is written before the work, a replayed proof is answered from the book, a paid failure is a 502 with the settlement, `GET /oracle/sales/:id`; buyer refunds only when nothing was sent or the mirror node says the transaction failed, otherwise `uncertain`.
- `363faf5` task API (`POST /api/tasks` as an x402 seller, durable ids, `GET /api/tasks/:id` with ticket and receipts, `/events`), agent tokens (`/api/agents`, restricted routes), `POST /api/wallet/pay` signing a header under the mandate for a keyless agent; stub payer salts its proof.
- `ae7a9a9` the `froggy` CLI served at `/froggy-cli.js`, the skill text with one source and `skills/froggy/SKILL.md` kept equal by a test.
- Agents tab in the drawer with `e2e/agents.spec.ts`; the brief's best-supply sentence.
- `e253cfb` five audit gaps: social login only, one price constant, twelve deployments in the tool text, no phantom 503 in GRAPH.md, the signer comment's real cap.
- Freeze removed from the product (task 1.6, phase 1): header, slash, Telegram, 423, mandate flag, pocket zeroing, signer revocation; migration 0006. The browser worker's frozen gate remains for phase 2.
- Task 1.4 (network as configuration): `HEDERA_NETWORK` picks testnet or mainnet, fails closed on anything else, the facilitator host follows it, the payer, the gate, the HCS writer, the probe, the pocket and the directory take it from the environment; `hedera:mainnet` HBAR and USDC are known assets and on the default network rule. AGENTS.md's onchain rule now states the owner's mainnet direction.

Smoke-tested end to end on a stub server: mint, curl the CLI, `brief` through Node to a done task, sale public by id, revoke kills the token.

Later the same evening:

- **Deployed.** `9d787f5` pushed at 20:40 CEST after the full gate (format, type-aware lint, typecheck, tests, knip, build, 24 browser tests); CI green; Railway deployment `377f3771` live at 20:46 CEST with migrations 0005 and 0006 applied; `/health`, `/.well-known/x402.json`, `/froggy-cli.js` (163 KB of JavaScript), `/froggy/SKILL.md` answer, `/api/agents` and `/api/tasks` refuse without a token. Three findings the plain lint had missed (`lint:types`) fixed first. The evening's timestamps in the record and the plan were recomputed from the transcript and the relay (they had been guessed, up to four hours late).
- `045ec63` **freeze removal, phase 2**: the browser worker's frozen gate, the protocol commands, the state flag, the error class and the grey drive mode are gone; deployment `ee35e9a1` at 20:55 CEST.
- **Task 1.5, per-person Hedera accounts**: `packages/wallet/src/keystore.ts` (AES-256-GCM under `HEDERA_KEK`), `packages/payments/src/accounts.ts` (the host opens and funds accounts), `apps/server/src/hedera-accounts.ts` (open at first need, fund at top-up, the person's own payer), `Store.hedera`, `WalletSummary.hederaAccountId`, the drawer row and `wallet_status` field, `/health` `hederaAccounts: own|host`. Proven on testnet at 20:57 CEST: account `0.0.10396038` opened by the float and paying a `froggy brief` task; ids in `docs/evidence/HEDERA.md`. `HEDERA_KEK` was set on the production service at 21:01 CEST; Railway had skipped the two code deployments (their CI runs were cancelled by the next push), so the accounts code was built by hand as deployment `faa8c1e5` at 21:07 CEST.
- **Task 1.8, the Privy `raw_sign` spike (21:06 to 21:10 CEST)**: Ethereum and Solana wallets are refused by the endpoint; a cosmos-type wallet signs a keccak256 hash, its key verified by the Hiero SDK, became account `0.0.10396162` and paid a transfer signed only through Privy (`0.0.10396162@1788721784.801491552`); a raw-bytes policy rule takes no conditions, so Privy cannot cap that leg. Verdict and ids in `docs/evidence/PRIVY.md`; the custody choice is open for Monday (`DECISIONS.md`).
- **Task 2.4, the onboarding surfaces (21:20 CEST)**: the drawer's Wallet tab shows the address with Copy, the service credit in dollars, the person's Hedera account linked on HashScan, and an Add funds button that opens Privy's fiat onramp toward USDC on Base mainnet (`useFiatOnramp`, lifted through the identity bridge; sandbox on dev builds); Privy's refusal is shown verbatim; without a Privy sign-in the sentence says what is missing. Not verified live: the onramp needs the dashboard toggle and a signed-in owner. Deployed as `6f66acb7` at 21:23 CEST.

## The overnight run (Sun 6 Sep from 21:40 CEST), Session F

Per `NEXT_ITERATION.md`'s overnight section. Owner items outstanding at the time of writing: the Telegram bot token, HBAR at the two mainnet aliases, USDC at the treasury.

- **Phase A, 22:05 CEST.** No free money: the account opens at the first top-up and a person with no account and no credit is refused with the reason; `STARTING_CREDIT_DIDS` and `TEAM_STARTING_USD` for the team. Base is configuration (`EVM_NETWORK`, `EVM_RPC_URL`), boot refuses an RPC on the other chain when Privy is live, the tools name the network. The Privy policy carries the mainnet top-up rule to the treasury `0x8Cc232c9EB25b4b20ee448106858e3B6281708C2` with its own 24-hour aggregation (`cx3m6izyslg00jkb9dbb63t5`); the `aggregation` verb did not turn out idempotent and left one duplicate Sepolia aggregation unused.
- **Phase B, 22:15 CEST.** The wallet home above the chat (`apps/web/src/components/wallet/wallet-home.tsx`): total, USDC on Base, the person's Hedera account with HBAR at the mirror rate, service credit, Copy, Add funds, Top up credit $1, Connect an agent, recent receipts. `WalletSummary.balances` carries what the chains said (USDC by `eth_call`, HBAR by the mirror node, 15 s cache in `services.ts`) and the networks, so HashScan and Basescan links follow the deployment.
- **Phase E code, 22:25 CEST.** `treasury.ts`: the treasury wallet (`TREASURY_WALLET_ID`, Privy app wallet `yikihk1kul6vh518z6pers1f`) pays The Graph's 402 on Base under its policy, no mandate on that leg, the settlement noted on HCS; the Graph client prefers it when pay-per-query is on. Unit-tested against a fake gateway; the live run waits for USDC at the treasury.
- **Phase C, 22:20 CEST.** Froggy's Telegram bot `@froggy_onchainbot` live on production: the token reached Railway from `~/.config/secrets.env` through the CLI's stdin (the owner had pasted it into the terminal chat; regenerate it in BotFather after the hackathon), a fresh `TELEGRAM_WEBHOOK_SECRET_TOKEN`, the webhook registered with it; `/health` telegram live, an unsigned webhook call answers 401. Deployment `c33eb76c` (`d735e67`) carries the wallet home, the treasury payer and the Telegram variables.
- **Bridge attempt, 22:46 to 22:58 CEST.** Kristjan chose to bridge rather than touch Hedera. LayerZero/Stargate V2 does carry Base ETH to Hedera (`StargatePoolNative` `0xdc181Bd607330aeeBEF6ea62e03e5e1Fb4B6F7C7`, dstEid 30316); one message `0x52c377…bb6c` delivered 0.002 WETH (an ERC-20, `0xCa367694…1ef2`, no Hedera DEX trades it) to the float alias, and its 20 HBAR native drop failed with INSUFFICIENT_GAS because the executor cannot create a new Hedera account. The executor's cap is 2,000,000,000 tinybars (20 HBAR) per message; once the float account exists, 20 HBAR per message at about $0.31 is the plan. The treasury policy `wdct7xe9re788wr3htum96pw` now also allows `eth_signTransaction` to that pool, value ≤ 0.008 ETH. The treasury then swapped 0.003 ETH into **7.53 USDC** on Uniswap V3 (SwapRouter02, policy rule `swap-eth-to-usdc-uniswap-base`), so the Graph x402 upstream has funds; it holds 0.0239 ETH besides. The deadlock for HBAR is one HBAR into the float alias from any existing Hedera account; no instant-swap service pays HBAR to an alias.
- **Phase D, 00:02 to 00:11 CEST (7 Sep).** The owner installed HashPack and sent one HBAR to the float alias, which created `0.0.10847552`; the bridge's 20-HBAR drops then worked (six messages from the treasury's ETH), the owner sent 500 HBAR more, and the float opened the service account `0.0.10847556`. The Blocky402 **mainnet** gate passed on the production code path: account `0.0.10847558` opened by the float paid a brief task, settlement `0.0.10571514@1788732364.891462934`, HCS topic `0.0.10847557`. Production flipped to `hedera:mainnet` and `eip155:8453` with the treasury, Privy custody, no free money and the team credit; facilitator set explicitly to `https://api.blocky402.com`. **Verified live at 00:15 CEST** (deployment `2dfa421d`): `/health` all live, the x402 card and the oracle's 402 name `hedera:mainnet`, payee `0.0.10847556`, fee payer `0.0.10571514`, topic `0.0.10847557`; the float held 618 HBAR. The mainnet HCS topic showed no note ten minutes after the gate's settlement, so the seller's best-effort note on mainnet is unverified until the first production settlement; the card's description also still said "Hedera testnet" from a fixed string, fixed the same night. Also found and fixed tonight: every relay post to the group between 21:00 and 00:05 had failed silently (the relay's `send` reads stdin only); a consolidated status went out as message 2607 at 00:10 CEST.
- **Privy production checklist, 00:20 to 00:35 CEST (7 Sep).** Kristjan walked Privy's dashboard: allowed origins set to the Railway URL and the two local dev origins, the app converted to production mode (same keys and users). Session F added the page's security headers (`apps/server/src/security-headers.ts`, `20fbe1e`): a Content Security Policy from Privy's implementation guide narrowed to what the app loads (its bundle, inline styles, data and blob images, its own sockets, `auth.privy.io` and the WalletConnect verifiers in frames), `frame-ancestors 'none'`, `X-Frame-Options: DENY`, `Referrer-Policy`, `X-Content-Type-Options`; `CSP_MODE=report` on Railway turns the policy report-only without a code change. Verified live at 00:37 CEST: the headers are on the page and a headless load under the policy shows Privy's iframe, the sign-in button and no violation. At 00:40 CEST Kristjan confirmed the dashboard toggles (card onramps, swaps, gas sponsorship) are enabled, and asked Session F to review the other two agents' lanes every ten minutes and report when everything is deployed and configured for mainnet.
- **Phase G, 22:30 CEST.** Privy holds each person's Hedera key (ADR 0009): `packages/wallet/src/privy-hedera-keys.ts` creates a cosmos-type wallet in the person's name and signs hashes; `packages/payments/src/payer.ts` gains `signerHederaPayer`; `hedera-accounts.ts` opens the account by paying the key's alias and reads both custodies; migration 0007. Proven on testnet: account `0.0.10396804` paid the hosted oracle (`0.0.7162784@1788726082.952826873`). `PRIVY_HEDERA_POLICY_ID` (`chxmr9kx3p6zljal8wo4tfoa`) is set on production. The seller's book now names the account whose HBAR left, not the fee payer.

## Morning of Mon 7 Sep (Session F, from 08:00 CEST)

- **08:00 to 09:05:** Jones and Kristjan signed in on production (Google). Logs clean: sign-in, receipts, one chat turn in 33 s, Telegram pairing calls, all 200.
- **09:06, "Why is wallet unavailable?"** Both saw "Balance unavailable" and no Add funds. Cause: the agent-signer grant fails on production and, when it failed, `grantAgentSigner` returned no wallet at all, so the session never learned the person's address; the onramp needs only the address. Fixed in `1df3d40` (deployment `dddc3756`, live 09:24): the wallet is kept whatever Privy says about the signer (a fresh wallet reads $0.00), a refused grant is retried after a minute instead of once per process, and Privy's reason is logged. Kristjan's embedded wallet `0x5eca…0344` holds 0 USDC and 0 ETH; no onramp has landed yet.
- **09:24, the reason:** Privy's JWT exchange answers `400 Invalid JWT token provided` for every user; no user-owned Ethereum wallet has ever carried the agent signer (this was the "unverified" item in `docs/evidence/PRIVY.md`). Owner action for Kristjan: Privy Dashboard → User management → Authentication → Advanced → enable **Server-side access**, then reload. Until then the person's wallet, balances and funding work, and the agent cannot pay from the person's wallet (Hedera payments go through their own account and are unaffected).
- **09:33, "Something went wrong setting up checkout":** Kristjan's first Add funds failed inside Privy's onramp. Cause: Privy loads Stripe's scripts into our page (`js.stripe.com/v3`, `crypto-js.stripe.com/crypto-onramp-outer.js`) and renders the checkout in frames from `crypto.link.com`; the Content Security Policy added at 00:25 named none of them, so Stripe's init failed. Fixed in `2adf614` with Stripe's documented onramp origins (script, frame, connect, img) and MoonPay's quote API; `47b1c44` shows the wallet address with a copy button under "Wallet funds". Deployment follows CI run 34095784529.
- **09:45, the signer without the toggle:** Kristjan's dashboard has no "Server-side access" setting (the Authentication → Advanced tab lists CLI/agent access, session durations, test and guest accounts, identity tokens), so the JWT exchange stays refused. `3694aed` adds Privy's primary path: the Wallet tab shows "Let the agent sign under policy" while the agent has no signer; the browser calls Privy's `addSigners` with the agent's key quorum and the policy, Privy prompts the person, and the server reads the signer off the wallet (`wallets().get`) instead of patching it. The welcome carries `agentSignerId`; `POST /api/agent-signer/refresh` re-asks at once. First real use pending: a person taps it on production and the drawer reads "the agent may sign under policy".
- **10:06, the first person-paid task on mainnet (Session F offline 10:00 to 13:10, usage limit; reconstructed from the Railway log and the mirror node):** Kristjan minted an agent token, paired Telegram, and asked for a task from the CLI. His own Hedera account `0.0.10848064` was opened from the float with his $1 team credit (12.48 HBAR, tx `0.0.10847552-1788768364-629577051`), and the task price of 6.19 HBAR settled from that account to the receiver through the facilitator (tx `0.0.10571514-1788768371-653824122`, task `tsk_01m1xeeqwbe0794xz9demhg7sh`). The task then failed: DashScope answered the model call with `400 data_inspection_failed: Input text data may contain inappropriate content`, so the task is failed, paid, not refunded, as decided. A second purchase was refused with 403 before any payment (the mandate's deny message; text pending from Kristjan). The consent button was not tried. Kristjan at 13:20: the instruction was deliberately an adult site, so the refusal is correct; a probe of the production prompt with an ordinary shopping task against DashScope answered normally. No model change.
- Jones's product notes, for the plan: after the onramp the HBAR account should be funded and a task paid in one step rather than "Add funds" then "Add task credit"; he is writing the signup-to-onramp improvements.

## Review of the other lanes (Session F, 00:55 to 01:40 CEST Mon 7 Sep)

Kristjan asked Session F to watch the two Codex sessions, finish their work if their usage ran out, and review it once landed. Neither ran out: both finished on their own, everything is committed and pushed, and no takeover was needed. Every commit since `bc59bb1` not by Session F was read in full: `bb7ba44` (release lane: mainnet payment delivery and Telegram linking), `f7fa2ee` (marketplace lane: paid services across workspace, chat, CLI and MCP), `6fcd329` (marketplace lane: bounded inline-image handling, no supplier activation) and `51564b5` (release lane: evidence, README and this file). **Verdict: accepted as landed; nothing to change; three items for the owner below.**

- **Against the plan and decisions.** The release fixes are the two mainnet faults the local gate could not see (a payment header about 10 KB over the hosted proxy's limit, now about 1.3 KB with three SDK-selected nodes; Bun's ten-second idle timeout closing in-flight settlements, now 180 s on `/oracle` and `/api/*`), plus a fresh `froggy_mainnet` database and Telegram linking under Connect an agent. All within `NEXT_ITERATION.md` tasks 1.5 to 2.3 and the one-continuous-run decision. The marketplace is added scope (ADR 0010): the person pays Froggy first from their own account, the sale is written before the work, the supplier is paid from the treasury only when a pinned payee, asset, network and price ceiling match its 402, no refunds, and every service reads unavailable on live Hedera until `SERVICE_SUPPLIER_PAYEES` and the X credential exist. That is consistent with the no-free-money model and the no-refund decision.
- **Verified live at 01:35 CEST on HEAD `51564b5`** (Railway deployment `c2a78308` SUCCESS, CI green, full gate `heavy bun run check` exit 0 with 17 tasks): `/health` shows all six integrations live; the card and the 402 on `/oracle/snapshot` are `hedera:mainnet`, payee `0.0.10847556`, facilitator `api.blocky402.com`, topic `0.0.10847557`, fee payer `0.0.10571514`; the topic holds two sold notes (sequence 2 is the release lane's post-deployment proof, transaction `0.0.10571514@1788735637.380133493`, sale `sal_01m1wf8tygesqb1t728qv9f475`); float 617.83 HBAR, receiver 3.71 HBAR; CSP, `X-Frame-Options: DENY`, nosniff and referrer policy on the page; `/froggy-cli.js` (with the `mcp` and `service` commands) and `/froggy/SKILL.md` served; `/api/services` and `/api/mcp` answer 401 without a token. The treasury paid The Graph 0.01 USDC on Base mainnet (`0x9355a0c0…993cd`).
- **Owner item 1, supplier activation (a decision, not a bug).** The five services stay unavailable in production until Kristjan merges the rules in `docs/privy-service-supplier-rules.json` into the treasury policy `wdct7xe9re788wr3htum96pw` (You.com at most 0.01 USDC and BlockRun at most 0.10 USDC per signature, Base USDC only, expiring 30 Sep), sets `SERVICE_SUPPLIER_PAYEES` from `docs/evidence/MARKETPLACE.md` and an `X_API_BEARER_TOKEN` in Railway, then buys one small task per provider. The Codex lanes refused to add that authority themselves, correctly.
- **Owner item 2, a real person in production.** Sign-in, first top-up, the opening of a person's own Hedera account, the Telegram Start link and Hermes over `/api/mcp` were all exercised with local stubs and browser tests, not by a signed-in human on the live URL. That is tomorrow's end-to-end test.
- **Owner item 3, standing.** Hermes token from the Agents tab into Contabo `~/.hermes/.env` as `FROGGY_TOKEN`; regenerate the Telegram bot token after the hackathon; back up `HEDERA_KEK` from Railway; task 2.6 (MCP OAuth 2.1 and the public agents page) on Monday.

## Lanes (Sun 6 Sep, 21:45 CEST) — iteration 2, overnight run per `NEXT_ITERATION.md`

- **Session F** (the facilitator session running the Telegram grilling; Fable 5.1): claims the iteration-2 core per `docs/plan/NEXT_ITERATION.md` tasks 1.1 to 1.7: seller durability (`apps/server/src/oracle-route.ts`, `packages/payments/src/oracle.ts`), the task API and lifecycle (`apps/server/src/tasks.ts` new, `router.ts`, `packages/domain/src/id.ts`, `packages/protocol/src/**`), agent tokens (`apps/server/src/auth.ts`, `router.ts`, settings drawer in `apps/web/src/components/details/**`), the settle and refund fix in `apps/server/src/session.ts` and `paid-request.ts`, freeze removal across `apps/server/src/freeze.ts`, `sockets.ts`, `telegram/**`, `apps/web/src/**`, and the CLI plus skill (`apps/cli/**` new, `skills/froggy/**` new). Database changes go in new migrations only. Commits by pathspec; please stay out of those paths until this block is updated.

## Marketplace lane — Codex, 7 September

Marketplace implementation and verification are complete locally: `service-{providers,tasks,routes}*`, `mcp.ts`, `tasks.ts`, `tools.ts`, `cli/froggy.ts`, `skill.ts`, the additive service handlers in `router.ts`, `service-browser.tsx`, `lib/tool-{call,stories,summary}*`, the service schemas, and the marketplace docs/tests. The full gate, build and 39 browser checks pass, including a real Node MCP stdio client. Follow-up work and activation limits are recorded in `docs/plan/MARKETPLACE_RUN.md`. Other agents' Telegram, payer, security-header and production work is being preserved. Activation handoff: `docs/evidence/MARKETPLACE.md`. No production policy or config changed here.

## Lanes (Mon 7 Sep, 13:00 CEST) — iteration 3 per `~/.claude/plans/team-demoed-froggy-s-new-zesty-bird.md`

The 7 Sep team sync (Kristjan, Jonas, Hemang) set nine decisions: one balance, spending limits hidden, USDC to HBAR just in time, two-way Telegram, MCP with OAuth 2.1, sidebar routes, a redesigned services page, marketplace activation as a lane, the card lane last. Lane 1 landed on `main` this morning (`3d1cdb0` one balance and `conversion_failed`; `9cf1200` allowlist-only mandates under `SPENDING_LIMITS`; `ea6fef4` the just-in-time conversion inside `spend()`, `wallet_topup` removed). Owner action for lane 1: apply the raised top-up caps in `docs/privy-agent-policy.json` (10 USDC per transfer, 25 USDC a day) with `bun run privy:policy apply`, and grant the agent its signer once with the "Let the agent sign under policy" button.

- **Session M (this session, on `main`):** lane 2, the web restructure (`apps/web/**`, `e2e/**`, `packages/ui/src/components/{card,empty,alert,spinner}.tsx`), then lane 3, the services page. Please stay out of `apps/web` and `e2e`.
- **Worktree agent A (branch off `ea6fef4`):** lane 4, two-way Telegram: `packages/domain/src/schedule.ts`, `packages/protocol/src/schedules.ts`, migration 0008 (`schedules` table), `Store.schedules`, `apps/server/src/{notices,schedules,schedule-routes}.ts`, `jobs.ts`, `telegram/pager.ts`, `tools.ts` (notify, schedule, schedules_list, schedule_cancel), ADR 0011.
- **Worktree agent B (branch off `ea6fef4`):** lane 5, MCP OAuth 2.1 and `froggy login`: `packages/domain/src/oauth.ts`, `Store.oauth`, `apps/server/src/oauth.ts`, `router.ts`, `agents.ts`, `mcp.ts`, `tasks.ts`, `cli/froggy.ts`, `skill.ts`, two web routes `/oauth/authorize` and `/oauth/manual`, ADR 0012. Its migration is generated after merge, as 0009.

Merges back to `main` are rebases by Session M, in the order the lanes finish.

### Session M, Mon 7 Sep afternoon: lanes 2 and 3 landed on `main`

- `9b2ad42` card, empty, alert and spinner primitives in `packages/ui`.
- `71b55a5` the browser tests set an approval threshold on the app socket (`e2e/mandate.ts`) instead of through a drawer.
- `4d4a5bc` five pages under one layout route (`apps/web/src/routes/workspace-layout.tsx`, contexts in `lib/workspace-context.ts` and `lib/chat-context.ts`), a rail on a desktop and a tab bar on a phone, one wallet total with the breakdown behind a disclosure, Add funds as one button, the policy tab, mandate editor, leash meter and task-credit form gone; specs walk the routes.
- `9c0a16a` the words "pocket" and "top-up" leave every screen; the wallet status card and WebMCP read the one total; README updated.
- `dc0bdb7` the services page redesigned: cards, one form, a task list, the chosen service in the URL.
- `4c20408` the Agents page: connect by URL first (loud until `/mcp` answers), Telegram, the token path behind Advanced; the minted skill survives navigation.
- `e2e/screens.spec.ts` walks every page at 1440, 768 and 390 (52 browser checks green); the wallet and services shots are in `docs/evidence/ui-wallet/`.

Owner-visible on the live URL after deploy: `/wallet` shows one dollar total; `/services`, `/agents`, `/settings` are pages. Lane 4 (schedules, notify) and lane 5 (MCP OAuth, `froggy login`) continue in their worktrees.

- **Lane 5 landed on its branch (agent B, 7 Sep):** `c0b4e03` the three OAuth tables and `Store.oauth` (migration to be generated after merge, as 0009); `eb78e13` the authorization server, scopes, `/mcp` with the resource-metadata 401, grants beside tokens on `/api/agents`; `3dd40d2` the consent and manual pages with `e2e/oauth.spec.ts`; `1907f66` `froggy login`/`logout` and the secret-free skill; ADR 0012. Note for the merge: the minted-token input was added to `apps/web/src/components/drawer/agent-settings.tsx`, which `main` has since moved to `components/agents/agent-settings.tsx`. Still owed: `docs/evidence/HERMES.md` and the live checks in plan 5.6.
