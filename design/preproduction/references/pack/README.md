# The original pack, as supplied

The two files the archive carried that are not deliverables in their own right: the instruction note the agent was pointed at, and the pack's own readme. Kept so nothing from `Froggy_Preproduction_Pack.zip` is lost now that the archive is gone — everything else it contained (the specification, the three reference images and their manifest) is committed one directory up, checksum-verified against `REFERENCE_MANIFEST.json`.

One redaction: `START_HERE.txt` line 1 named an earlier internal project, and `tools/check-names.ts` forbids that anywhere in this tree — correctly, since a submission should not carry other projects' names. The name is replaced with `[another project]`; nothing else in either file is altered.
