# Code state audit (raw digest)

## One paragraph
At HEAD 0b076fe the implementation is seven commits by grmkris on the evening of 2026-09-04 (61ef247..96a4917); everything since is planning docs. The spine the handover describes is real and coherent: one Bun process serves the SPA, an AI SDK v7 agent loop with 8 tools and SSE replay, a Bun.WebView Chrome with JPEG screencast, input capture, 1.5s/15s human-agent arbitration and accessibility snapshots, a pure authorize() policy engine (frozen, provenance, expiry, network/payee/host allowlists, per-tx $2, window $10/24h, ask over $1) wired into a single spend choke point with reserve-before-settle, a 402-shaped oracle route, and a Railway deploy gated on CI. Against PRODUCT_FABLE51.md it is single-tenant by construction and stubbed end to end: the live URL reports graph/hedera/model/privy all "stub", the 402 advertises payTo 0.0.0 and settles against a stub gate that accepts any well-formed envelope, the Graph answer is a fixture, the Privy adapter (verifyAccessToken + users().get) has zero callers so sign-in changes nothing server-side, wallet_send is a hard-coded refusal with a stub settle, the ledger and receipts are in-memory (migrations folder missing, Postgres provisioned but unused), and there is no authentication on any route or socket (a missing Origin header is trusted, so curl can drive the shared Chrome, rewrite the mandate or unfreeze). Nothing exists for per-user W_user/K_pocket, P_agent policy JSON, typed-data x402 to The Graph, the four-deployment registry/freshness gate, HCS, Telegram, guest pool, 402 probe/directory, navigation allowlists, fresh-profile-per-session, approval cards, or any evidence file. Two Day-2 correctness tests already fail on paper: concurrent same-key spends settle twice (reproduced by a verifier against the real ledger) and a settled replay returns no cached body. Hygiene is red on three DAY0 items (209-file Chrome profile tracked, undisclosed "Ported from invok/harness/humanhook" headers, handover naming a prior project's Privy app). Local test suites for payments/graph/domain/protocol/wallet/database/server cannot run on this checkout (no node_modules: "Cannot find package 'effect'"); only apps/web (14 pass) and packages/browser (26 pass) run because their imports are type-only.

## Status matrix
| capability | status | plan day | h | evidence |
|---|---|---|---|---|
| Shared Chrome + screencast + grab + freeze | partial | Day 1 (measurement 1h), Day 2-3 (frozen state + freeze stops browser 2h) | 3 | Screencast/ack/backpressure working (packages/browser/src/screencast.ts:144-170; apps/server/src/sockets.ts:106-119, 245-252); arbitration 1500ms/15000ms matches plan (arbitration.ts:20,25; 8 tests pass); browser.take aborts runs first (sockets.ts:146-151). Freeze is only setFrozen + runs.abortAll (sockets.ts:184-194): never reaches the browser, no grey state (protocol/browser.ts:22 InteractionMode = agent|human|idle; browser-pane.tsx:279 static ring, MODE_TONE human is emerald not blue), chat.ts:75-88 starts new runs while frozen. No fps/latency measurement exists (docs/measurements.md absent). |
| Per-user isolation (worker-per-user or contexts, fresh profile per session, stealth argv) | missing | Day 2, Sun 6 (isolation 18h, fresh profile 3h, stealth/viewport 1h) | 22 | One SessionId/WorkspaceSession per process (apps/server/src/index.ts:36-41), one BrowserSession with one persistent profile (services.ts:83-86; environment.ts:124-133; Dockerfile:86 /data/chrome-profile on railway.ts browser-profile volume); every browser socket subscribes to the same screencast and any client's browser.take/mandate.freeze aborts all runs (sockets.ts:150,190,235-254); runs.ts:132-133 a second chat aborts the first. README 'Known limits' and packages/browser/src/session.ts:40 document the persistent shared profile as the design, the opposite of plan §2. argv is --no-sandbox/--disable-dev-shm-usage/--hide-scrollbars only (chrome-detect.ts:179-184). |
| Agent loop + model | partial | Day 1 (pin + verify model id) | 0.5 | Loop working: streamText with server-owned abort, stepCountIs(12), SSE tee/replay (chat.ts:79-105; runs.ts:63-125; router.ts:123-133; 9 runs tests). Model tiers Anthropic / OpenAI-compatible / scripted MockLanguageModelV3 (model.ts:49-127). Live URL model=stub (scripted graph_query -> wallet_status -> text). model.ts:29 hardcodes 'claude-sonnet-5', unverified against the API and different from DAY0 E.5's 'claude-opus-5'. |
| Tools (browser_navigate/snapshot/click/type, graph_query, x402_fetch, wallet_send, wallet_status) | partial | Day 2 (nav allowlist 3h + page-initiated/popup gating 5h + x402_fetch host check before fetch 1h) | 9 | Browser tools call agentNavigate/agentSnapshot/agentClick/agentType with 50k cap (tools.ts:70-124); graph_query carries evidence into the next spend (tools.ts:126-151, 226-230); x402_fetch does 402 -> spend -> pay -> retry (tools.ts:153-245); wallet_status works but address is always null (setAddresses never called). Gaps: browser_navigate has no URL parser/scheme/origin check (tools.ts:73-84; grep 'new URL(' in packages/browser = 0); x402_fetch does fetch(url) before any policy runs (SSRF, tools.ts:157-158); wallet_send hard-codes provenance 'model' and a stub settle returning ok:false (tools.ts:264-277, 286); agent type is insertText only (browser/session.ts:280-288). |
| Host policy engine (authorize + mandate) | working | Day 2 (drop or route the ask band 0.5h; real quote 1h) | 1.5 | packages/wallet/src/policy.ts:72-178 evaluates frozen, provenance, expiry, network, payee, host (label-boundary), per_tx, window, ask in that order; 12 tests; defaultRules $2/$10/24h/approval $1 (domain/mandate.ts:170-204); wired at session.ts:184-284 with receipt on every outcome. Caveats: parQuote prices 1 HBAR = $1 (session.ts:186-190; receipt source 'par:stablecoin'); the $1-$2 'ask' band has no human path (tools.ts:239-241, 283-285; sockets.ts:200-206 only echoes; web ignores approval.request); caps are USD not the plan's tHBAR pocket caps; Provenance enum has no 'user' value (domain/address.ts:46-51). |
| Authentication on HTTP routes and WebSockets | missing | Day 2 (interim 1h shared-secret gate before any real key lands on Railway) | 6 | router.ts:86-145 and index.ts:105-128 have no auth; sockets.ts:52-62 isTrustedOrigin returns true when Origin is absent (verified live: curl upgrade with no Origin -> 101 on /ws/browser and /ws/app, pushes browser.state, screencast frames, session.welcome and full mandate; Origin evil.example -> 403). mandate.update / mandate.freeze{frozen:false} / approval.resolve accepted from any socket client (sockets.ts:184-206; session.ts:158-166). Live /api/wallet and /api/receipts answer anonymous curl 200. PrivyServer.verify exists (packages/wallet/src/privy.ts:102) and is never called. |
| Privy wallet creation / agent signer / P_agent policy | missing | Day 1 (fresh app, auth key, P_agent v0, denial script 4h), Day 2 (per-user W_user with owner + agent signer 6h), Day 3 (wallet_send -> real Privy denial with policy id, provenance 'user', pane shows policy id + raw error 7h) | 17 | packages/wallet/src/privy.ts makes exactly two SDK calls (users().get by id_token :91, verifyAccessToken :104-107; verified against @privy-io/node 0.34.0 surface); no wallets().create, no policies, no authorization key, no eth_signTransaction/eth_signTypedData_v4/_rawSign anywhere (grep = 0); no P_agent JSON in the repo; services.privy (services.ts:75-81) and session.setAddresses (session.ts:146) have zero callers; the web never sends identity.token (privy.tsx:41,111 unconsumed; app-shell.tsx:16 uses login/logout only). Web Privy config is loginMethods google/email/wallet + createOnLogin 'all-users' (privy.tsx:204-211) vs plan 'email OTP on, create-on-login off'. Live privy=stub, VITE_PRIVY_APP_ID build arg '' (Dockerfile:49-50) so Sign in is disabled. |
| Privy top-up transfer (wallet_topup, rule b with 24h aggregation) | missing | Day 4, Tue 8 | 5 | No wallet_topup tool (tools.ts exports 8 tools, none signs); WalletSummary has no aggregation fields (session.ts:175-181; protocol/app.ts:113); Network literal is base-sepolia|hedera:testnet only (domain/money.ts:18); no Privy signing path exists. |
| Typed-data x402 to The Graph mainnet gateway (rule a) | missing | Day 3, Mon 7 | 6 | No eth_signTypedData_v4 / EIP-3009 payer; packages/payments/src/payer.ts selects only hedera:testnet exact (:36-58); liveGraphClient uses only a Bearer Studio key (graph/client.ts:132-140); no $1-per-day typed-data ledger; receipt has no 'path' field (domain/receipt.ts:44-58). |
| Hedera 402 service (payTo, metering, feePayer, HCS, receipt token, service card) | partial | Day 1 (real payTo + feePayer from /supported + first hosted settlement 3h), Day 2 (per-deployment metering + unlock page 4h), Day 5 (service card 1h) | 8 | Real 402 envelope on GET /oracle/snapshot (oracle-route.ts:27-88; e2e asserts x402Version 2, hedera:testnet, exact; verified live 402 at 08:19 UTC); liveOracleGate does /verify then /settle against the facilitator (packages/payments/src/oracle.ts:127-208). Live gate is stub: payTo 0.0.0 (STUB_PAY_TO oracle.ts:212), stub settle accepts any envelope with accepted/payload/x402Version keys and returns 'stub-not-a-real-hedera-transaction-*' (oracle.ts:214-236; verified live 200 with a garbage transaction). Flat 5000000 tinybar (oracle-route.ts:19) not per-deployment; extra:{} hard-coded even in live mode (oracle.ts:55-65) so no feePayer; /supported never read (grep feePayer = 0; Blocky402 lists 0.0.7162784 today); no HCS, no receipt token/unlock page, no /.well-known/x402.json. |
| Hedera payer + first real settlement | partial | Day 1 (accounts funded + first settlement, 2h), Day 2 (K_pocket per user + encrypted key 6h, tHBAR caps + directory payTo rule + accepts[] feePayer check 4h) | 12 | liveHederaPayer with @x402/hedera ExactHederaScheme exists (payer.ts:69-125) but is one env key (HEDERA_ACCOUNT_ID/HEDERA_PRIVATE_KEY, services.ts:67-73) shared by every session, selected only when both are set (.env.example placeholders 0.0.0 / 0xREPLACE_ME); live hedera=stub; no HashScan transaction id anywhere in the repo, no HEDERA.md. Plan's K_pocket (per-user ECDSA account from treasury, key encrypted under env key, 0.5/2 tHBAR host caps, payTo must be a server-issued directory id, spends serialized per user) is absent. |
| Idempotency state machine (reserved/signed/settled, cached replay body, 409, mirror-node reconcile) | partial | Day 2, Sun 6 | 4 | memoryLedger reserve/settle with (sessionId, idempotencyKey) (ledger.ts:62-115); key is server-derived `x402:${url}:${amount}` (tools.ts:195), not client-minted. session.ts:246 short-circuits only on status 'settled': a verifier reproduced two concurrent same-key spends both calling settle() (settleCalls = 2) with a 20ms settle; a sequential replay skips settle and tools.ts:242 returns 'Paid, but the server returned no body.' (no body stored on row or receipt). No 'signed' state, no 409, no mirror-node reconcile. Plan Day 2 test 'paid twice, charged once' is not met. |
| HCS message per settlement | missing | Day 3, Mon 7 | 2 | grep -rniE 'hcs|topicmessage|mirror' apps packages = 0 hits; Receipt schema has no HCS sequence field (domain/receipt.ts:38-58). |
| Graph registry of 4 deployments + parallel query + _meta block + freshness gate | partial | Day 1 (Studio key + four freshness curls into GRAPH.md 1h), Day 2 (registry, parallel, _meta, freshness, SKILL.md 6h, oracle brief per deployment folded into metering), Day 3 (deployment ids on receipts 1h) | 8 | liveGraphClient queries one GRAPH_SUBGRAPH_ID with the Messari lending shape and decodes strictly (graph/client.ts:17-37, 122-170); no registry JSON, no _meta block, no freshness gate, no deployment id/block on rows (types.ts GraphSnapshot has capturedAt/query/source/stubbed only) or receipts (Evidence = query/snapshotHash/source/stubbed); oracle-route.ts:69 makes one lendingMarkets call. Live graph=stub: source 'fixture:messari-lending', capturedAt 1756000000000 hard-coded — .env.example says a stubbed Graph query qualifies for nothing. No SKILL.md for Subgraph MCP (only .agents/skills/froggy-*). |
| Receipts (fields, replay, public page) | partial | Day 3 (fields + replay on open 4h, blocked card + share 2h partly with the two buttons), Day 4 (public receipts page + counters 4h) | 9 | Receipt carries intent, decision (code/message/ruleId), evidence hash, quote, settlement.transactionId, stubbed (domain/receipt.ts:44-58; session.ts:286-320); written on every outcome incl. deny. Missing vs plan §2: Graph deployment ids/blocks, Graph path, Privy policy id, HashScan link, HCS sequence, ledger line ('0.10 / 2 tHBAR today'); receipts are an in-memory array (session.ts:110); socket open never replays history and the client never fetches /api/receipts (sockets.ts:214-232; use-app-socket.ts:43,98-101) so a reload shows 'Nothing spent or refused yet'; no public receipts page (router.tsx single '/' route); no share button or blocked card in chat (chat-pane.tsx:78-100). |
| Ledger persistence (Postgres, migrations, preDeploy) | broken | Day 2, Sun 6 | 6 | services.ts:96 wires memoryLedger() unconditionally; packages/wallet/src/ledger.ts:62 is the only SpendLedger; packages/database/src/schema.ts (spends/receipts/mandates) is imported by nothing but migrate.ts; packages/database/drizzle (migrationsFolder, migrate.ts:22) does not exist in the tree; migrate.ts:2-7 claims a Railway preDeployCommand that .railway/railway.ts does not declare; DATABASE_URL is preserve()'d and read only by the never-run migrate script; Postgres + 50GB volume provisioned unused. Every restart resets the $10/24h window and the idempotency state. |
| Users table + nonces | missing | Day 2, Sun 6 | 2 | packages/database/src/schema.ts:3-7 explicitly declares 'no users table' (Privy DID is the identity), contradicting plan §10 Day 2 'users table, nonces'; no frozen_at, pocket ciphertext, TTL or pairing columns anywhere. |
| Telegram pager (pairing, Freeze button with nonce, /bug, digest) | missing | Day 2 (grammY bot 8h), Day 5 (digest cron 3h, cut first) | 11 | grep -rni telegram apps packages = 0; handover.md:54 says it is a fresh day of work; no bot token in .env.example; no digest cron. |
| Guest path / entry doors (Try it pool, IP cap, TTL, consent, invite code, delete-my-data, judge slots, phone door) | missing | Day 2 (landing/consent/phone door copy + clip 6h, Jonas), Day 3 (pooled identities, IP cap, TTL, delete-my-data, reserved judge worker 10h), Day 4 (read-only judge screencast 4h) | 20 | apps/web/src/router.tsx:12-21 single route; app-shell.tsx has only Sign in/Sign out; no guest/consent/invite strings in apps/web/src; server has no guest provisioning, IP cap, TTL or delete route; mobile is a stacked flex column with the wallet pane off-screen (workspace-page.tsx:27), no read-only clip door. |
| 402 probe + server-issued seller directory + paid-call counters + two pre-typed stranger buttons | missing | Day 3, Mon 7 (probe + directory 4h, buttons + blocked card 4h) | 8 | No probe route or directory in router.ts; no directory/probe/counter markup in apps/web/src or protocol app.ts:120-174; chat-pane.tsx:15-19 has three different suggestion strings that vanish after the first message; payee allowlist is only the server's own oracle payTo (index.ts:80-86) and x402_fetch labels any 402 body's payTo provenance 'server' (tools.ts:196-201). |
| Evidence files (README winner shape, HEDERA.md, PRIVY.md, GRAPH.md, VALIDATION.md, FEEDBACK.md, ACQUISITION.md, AI-USE.md, docs/measurements.md) | missing | Day 1, Sat 5 (skeletons; completed Day 6) | 2 | ls shows none of PRIVY.md, HEDERA.md, GRAPH.md, VALIDATION.md, FEEDBACK.md, ACQUISITION.md, AI-USE.md, docs/measurements.md; only README.md, README_FABLE51.md and the planning docs exist. |
| CI / deploy | working | Day 1 (pin runtime, keep CI green) | 1 | .github/workflows/ci.yml runs bun install --frozen-lockfile, bun run check (format, type-aware lint, typecheck, graph boundaries, agents:check, test, knip), build, playwright e2e on every push/PR; .railway/railway.ts one replica with checkSuites:true, /health healthcheck, browser volume; live /health 200 at 08:19 UTC. Caveats: bound to github('grmkris/agentic-wallet') which is private (GitHub API 404) so CI results cannot be inspected from here; local bun 1.3.14 vs pinned 1.4.0 (DAY0 wants 1.4.1); no node_modules locally so 7 of 9 test suites fail with 'Cannot find package effect' / '@froggy/domain'; no preDeployCommand for migrations; VITE_PRIVY_APP_ID build ARG defaults to ''. |

## Track readiness
| track | today | blocking gap | h |
|---|---|---|---|
| Hedera: AI & Agentic Payments on Hedera | A hosted, curl-able 402 on hedera:testnet with the right envelope (x402Version 2, exact, HBAR 0.0.0) and live verify/settle code against Blocky402 exist, but the deployed service is entirely stub: payTo 0.0.0, no feePayer in extra, settlement accepted without a chain, transaction id 'stub-not-a-real-hedera-transaction-*', fixture data behind the paywall, no HashScan tx, no HCS, no service card, no per-user pocket. Qualification requires a real paid request end to end; none has happened. | Fund payTo + payer accounts, read feePayer from /supported at boot and emit it in accepts[].extra, set HEDERA_* (locally first, on Railway only after the sockets/routes are gated), get one settled tx on HashScan into HEDERA.md; then K_pocket per user with host tHBAR caps, idempotency state machine, HCS per settlement, service card. | 25 |
| The Graph: Best Use of Composable or Standardized Graph Products | One Messari standardized lending query shape with strict decoding runs against a single GRAPH_SUBGRAPH_ID when a Studio key is set; the live URL serves a fixture (source 'fixture:messari-lending'), which .env.example itself says qualifies for nothing. No registry of the four pinned deployments, no _meta block, no freshness gate, no deployment id or block number on rows or receipts, no x402 mainnet gateway path. | Studio key + four freshness curls (Day 1), registry JSON of the four deployments queried in parallel with _meta and a two-hour freshness gate, deployment id + block on every row and receipt, SKILL.md for the Subgraph MCP; the typed-data x402 gateway path via rule (a) is the Day 3 add-on. | 16 |
| The Graph: Best AI Tooling or AI Use Case (From Scratch) | The mechanism the track wants exists in code: the agent loop calls graph_query, hashes the snapshot, and carries it as evidence onto the x402 spend and receipt. But the live model is a scripted MockLanguageModelV3, the Graph data is a fixture, and 'From Scratch' is at risk because three browser files, runs.ts, privy.ts, privy.tsx and app.ts carry undisclosed 'Ported from invok/harness' and 'lifted from humanhook' headers with no AI-USE.md. | Everything in the Composable track plus a live model key with a verified model id, an AI-USE.md disclosure (or header removal) for the ported patterns, and the profile/handover hygiene so the repo can go public. | 5 |
| Privy: Best financial flow | Nothing Privy runs. The server adapter makes two identity calls and is never invoked; the client never sends its token; wallet address is always '—'; no wallet creation, no agent signer, no P_agent policy, no eth_signTransaction/eth_signTypedData_v4; wallet_send is a hard-coded host refusal with a stub settle; live privy=stub and Sign in is disabled; the only Privy credentials on the box belong to a prior project's app, which the checklist forbids reusing. | Fresh Privy app + P-256 authorization key + committed P_agent v0 + the 40-line denial script into PRIVY.md (Day 1); per-user W_user created server-side with owner + agent signer and the web sending the access token (Day 2); wallet_send built as eth_signTransaction with provenance 'user' and denied by Privy with the policy id shown in the pane (Day 3); wallet_topup under rule (b) with the 24h aggregation on screen (Day 4); typed-data x402 to The Graph under rule (a). | 28 |
| Privy: Best B2B financial product | Nothing; the audit says tick it only if the Wednesday six-hour Intents box ships, which is conditional on Day 4 being green and a third builder existing. No Intents, approval cards or B2B flow exists (approval.request is never emitted, the client ignores it). | All of the financial-flow gaps plus the Intents/approval-card box; recommendation stays 'do not tick' unless Day 4 is green. | 34 |

## Hygiene risks
- Tracked Chrome profile: git ls-files apps/server/.froggy = 209 files (~4.1 MB) incl. Default/Cookies (20480 B), Login Data (40960 B), Login Data For Account, History, Web Data, Secure Preferences, Local State, Sessions/*, added in fd4bdc9; .gitignore has no .froggy entry (only .dockerignore excludes it). Verifiers opened the SQLite blobs read-only: 0 cookies, 0 logins, history = example.com only, so it is a leak vector not an existing credential leak; the working tree already shows History/History-shm/History-wal modified, so the next 'git add -A' ships browsing history. DAY0 E.1 requires the grep to print nothing.
- Undisclosed prior-project headers (DAY0 E.2): packages/browser/src/arbitration.ts:11, input.ts:6, chrome-detect.ts:9 ('Ported from invok'), apps/server/src/runs.ts:15 ('Ported from harness'), apps/web/src/hooks/use-browser-socket.ts:10, packages/protocol/src/app.ts:43, apps/web/src/lib/privy.tsx:4 and packages/wallet/src/privy.ts:7 ('lifted from humanhook'); docs/architecture.md:116-132,145,165 and docs/PLAN.md:309-433 cite machine-local paths ~/code/invok, ~/code/humanhook, ~/code/boter; no AI-USE.md exists. Risk for the Graph 'From Scratch' track and the event's disclosure rule.
- docs/handover.md:34 states the Privy app id/secret on the box are 'humanhook's app' and suggests adding the Railway domain to it; DAY0 C.1 forbids reusing an inherited app and E.3 greps docs/ for this; docs/architecture.md:116 and :225 use 'steal' (E.3 grep hits).
- No authentication anywhere and a missing Origin header is trusted (sockets.ts:52-62): verified live that curl gets 101 on /ws/browser and /ws/app with browser.state, screencast frames and the full mandate pushed; any non-browser client can send input.*, browser.navigate, browser.take, mandate.update (replace all rules), mandate.freeze{frozen:false} (unfreeze) and approval.resolve; /api/chat, /api/chat/stop, /api/receipts, /api/wallet are open. With real HEDERA keys on Railway this is the payer anyone can widen the payee allowlist of.
- Single-tenant assumptions baked in: one SessionId/WorkspaceSession/mandate/ledger/receipt list/ChatRunRegistry per process (index.ts:36-41; router.ts:110,116,124 ignore the chat id), one BrowserSession and one persistent profile shared by every visitor on a Railway volume; a second visitor's chat aborts the first's run (runs.ts:132-133) and every app-socket broadcast goes to all clients (sockets.ts:73-78).
- Navigation is unchecked on every path: agent browser_navigate, human browser.navigate and first-start URL go straight to Bun.WebView.navigate (tools.ts:73-84; browser/session.ts:200-204, 236-243; tabs.ts:98-101); only popup adoption has an http(s) regex (popups.ts:39-42) and it switches the screencast to the popup without arbitration. Chrome runs as root with --no-sandbox in the container, so file:// reads of /data/chrome-profile and the env are the concrete risk. x402_fetch also fetch()es any model-supplied URL before policy (tools.ts:157-158).
- Live deployment misrepresents readiness if read casually: /health reports all four modes stub, the 402 advertises payTo 0.0.0, the stub gate settles any well-formed envelope with a fake tx id and a fixture body; nothing in the UI distinguishes 'live URL' from 'stubbed live URL' beyond amber chips. handover.md:5 'a real 402 and its paid round trip' is a zero-value stub settlement.
- Persistence is unwired end to end: memoryLedger only, receipts in an array, migrate.ts:22 points at a non-existent packages/database/drizzle (and the path is cwd-relative, doubly wrong from apps/server), migrate.ts header claims a preDeployCommand that railway.ts does not declare; every redeploy resets spend history, the 24h window and idempotency.
- Concurrent double payment: two in-flight spends with the same idempotency key both reach request.settle() (ledger.ts:95-102 returns the 'reserved' row; session.ts:246 guards only 'settled'); reproduced by a verifier with settleCalls = 2. The key `x402:${url}:${amount}` has no run/user component, so on the shared session two strangers buying the same URL share one settlement and the second sees 'Paid, but the server returned no body.'
- Freeze is not a kill switch for money in flight or for browsing: setFrozen flips a flag consulted at the next authorize (session.ts:163-166); a spend already inside request.settle() continues; runs.abortAll does not cancel the settle promise; chat.ts starts new runs while frozen; the browser is never told. Unfreeze is the same unauthenticated message.
- Secrets: environment.ts:198-218 unwraps every Config.redacted value (HEDERA_PRIVATE_KEY, PRIVY_APP_SECRET, GRAPH_API_KEY, DATABASE_URL) into plain strings on services.environment, reachable from every tool's deps; nothing logs them today.
- Pricing: parQuote values 1 HBAR at $1 (session.ts:186-190; money.ts:137-141) so USD labels in the pane and receipts (quote source 'par:stablecoin') are wrong by the HBAR/USD factor; the $2/$10 caps are really 2/10 HBAR.
- Runtime/model pin drift: local bun 1.3.14 vs package.json/ci.yml/Dockerfile 1.4.0 vs DAY0 E.5's 1.4.1 (1.3.x has the WebView close() bug); model.ts:29 'claude-sonnet-5' vs DAY0 'claude-opus-5', neither verified; types/webview.d.ts is hand-written against a runtime prototype absent from @types/bun.
- Local verification impossible on this checkout: no node_modules anywhere, so packages/payments, graph, domain, protocol, wallet, database and apps/server test suites all fail at import resolution ('Cannot find package effect', 'Cannot find module @froggy/domain'); only apps/web (14) and packages/browser (26) pass; CI results are uninspectable because the GitHub repo is private and gh is not authenticated. Test coverage gaps: nothing tests WorkspaceSession.spend, sockets, router, oracle-route, cdp, screencast, snapshot, tabs, popups, session (createView injection hook unused).
- Repo identity: .railway/railway.ts binds the deploy to github('grmkris/agentic-wallet'), currently private; every track requires a public repo, and a deletion commit of the profile keeps it in history (rules penalise history rewrites, so a README disclosure line is the prescribed fix).
- Plan/README design conflict on the profile: README 'Known limits', packages/browser/src/session.ts:40, profile.ts:10-14 and railway.ts advertise a persistent shared profile ('the agent shops as you') while PRODUCT_FABLE51.md §2 requires a fresh profile per session wiped at session end; one of the two documents must change before the public README is written.

## Refuted or corrected claims
- handover.md:5 'Verified live: ... a real 402 and its paid round trip' — correction: the 402 envelope is real, the round trip settles against stubOracleGate (services.ts:59-65) which accepts any base64 JSON with accepted/payload/x402Version keys and returns 'stub-not-a-real-hedera-transaction-<ts>' with a fixture body; verified live with payload.transaction='garbage' -> HTTP 200. No chain, no facilitator call, payTo 0.0.0.
- README 'Reserve before you pay ... a retried tool call cannot pay twice' — refuted for concurrent retries: a verifier ran the real ledger.ts with a 20 ms settle and both same-key spends called settle() (settleCalls = 2) because ledger.ts:95-102 returns the 'reserved' row and session.ts:246 only short-circuits on 'settled'. Sequential replay is deduped but returns no cached body (tools.ts:242).
- README 'Freeze aborts the run first, then takes the page' — corrected: mandate.freeze calls setFrozen + runs.abortAll only (sockets.ts:184-194) and never calls browser.takePage; only browser.take does abort-then-take (sockets.ts:146-151). After freeze the arbitration falls to idle, the human and new POST /api/chat runs can keep browsing; only spends are denied.
- handover.md 'the server honours mandate.update' listed as a feature and 'the freeze toggling both ways' as verified — narrowed: both are accepted unauthenticated from any app-socket client, including one with no Origin header (verified 101 live), replacing every rule of the single shared mandate (session.ts:158-161 pins only frozen and sessionId).
- Server reader: 'stubOracleGate.settle accepts any base64-JSON X-PAYMENT' — narrowed by verifier: decodeHeader (oracle.ts:91-111) requires the envelope keys accepted, payload and a finite x402Version; base64({"foo":1}) and non-base64 return 402 'X-PAYMENT was not base64-encoded JSON.' Values are unchecked (amount '1', payTo '0.0.999', x402Version 1 all settle).
- Readers (all four areas): 'a real Chrome profile with Cookies and Login Data is committed' — narrowed: 209 genuine Chrome 151 profile files are tracked since fd4bdc9 and not ignored, but sqlite read-only checks show cookies 0 rows, logins 0 rows, autofill/credit_cards 0, history = https://example.com/ only; .dockerignore excludes .froggy and Railway uses /data/chrome-profile, so it is a git-history leak vector, not a credential leak and not the profile the deployed box runs.
- Browser reader: 'popup adoption ... without any URL check' — narrowed: popups.ts:39-42 isAdoptable requires type 'page', an openerId and /^https?:/, so javascript:/file:/data: popups are not adopted; the arbitration bypass and screencast steal (tabs.ts:109 sets the popup active) are confirmed, and no origin allowlist exists.
- Browser reader: 'only 3 of 12 source files are tested' — 3 of 13 non-test files (index.ts barrel included); the substance stands: cdp, screencast, snapshot, tabs, popups, session, input have no tests and the createView injection hook (session.ts:42-46) is unused.
- Server reader: '`bun run migrate` throws because packages/database/drizzle does not exist' — partly: the folder is confirmed absent, but the script throws earlier on a missing DATABASE_URL (migrate.ts:15-17) and, on this checkout, at import resolution; the migrationsFolder path is cwd-relative so from apps/server it resolves to apps/server/packages/database/drizzle. The one verifier who said 'confirmed' and the one who said 'partly' agree no migrations and no Postgres ledger exist.
- Wallet reader: the quoted grep for Privy call sites 'returns only the definitions in privy.ts:40-43,86,102 and session.ts:146' — corrected: the pattern with a leading dot matches only session.ts:146; the conclusion (zero callers of verify/addresses/setAddresses, no token ever sent) is confirmed by broader greps and live /api/wallet address:null.
- Wallet reader: 'SDK calls in privy.ts not verifiable on this checkout' — corrected: a verifier fetched @privy-io/node 0.34.0 (lockfile-matching integrity) into the scratchpad and confirmed users().get({id_token}), utils().auth().verifyAccessToken(), VerifyAccessTokenResponse.user_id and the wallet/smart_wallet LinkedAccount shapes all match. What remains unverifiable is the CI result at 61ef247 (private repo, gh unauthenticated); only checkSuites:true plus a live deploy implies it passed.
- migrate.ts:2-7 'runs as a Railway preDeployCommand' — refuted: .railway/railway.ts declares no preDeployCommand; DATABASE_URL is merely preserve()'d; the Postgres service and 50 GB volume are unused by the running code.
- handover.md:34 implication that setting PRIVY_APP_ID/SECRET on Railway makes sign-in work — corrected: with keys set the pane would show a 'live' Privy chip while the server still never verifies a token or reads an address (services.privy has no callers), so a signed-in user and an anonymous visitor remain indistinguishable; and the app named is a prior project's, which DAY0 C.1 forbids reusing.
- Server reader: the Privy denial beat and 'user' provenance — confirmed missing and further narrowed: the Provenance enum (domain/address.ts:46-51) has no 'user' value at all, so the plan's beat cannot be expressed without a domain change; wallet_send provenance is always 'model' and its allow branch returns 'transfers are not wired to a signer yet' (tools.ts:264, 286).
- README/handover 'It runs with no keys at all' and 'Verify it: bun run check' — true only after bun install; on this checkout no node_modules exists and 7 of 9 test suites fail at import, so the audit could not confirm the suites pass; apps/web (14) and packages/browser (26) pass because their imports are type-only.
- DAY0 E.5 'the model id is claude-opus-5' vs model.ts:29 'claude-sonnet-5' — the plan and the code disagree and neither id has been verified against the API; the plan's Day 1 'model and runtime versions pinned' is open in both documents.
- Web reader: 'Verified live on Railway' for the screencast painter — narrowed: the verifiers confirmed the socket upgrade and a browser.state + JPEG frame arriving over curl; fps and click round-trip were never measured (docs/measurements.md absent), so the plan's 5 fps / 300 ms gate is unknown, not passed.

## First five tasks
1. 1. Hygiene commit (Day 1 A, ~1.5h, blocks the public repo every track needs): `git rm -r --cached apps/server/.froggy`, add `.froggy/` to .gitignore, one README line stating the profile was tracked and held no cookies or logins (no history rewrite); create AI-USE.md disclosing the invok/harness/humanhook pattern sources with public links or strip the 'Ported from' headers in packages/browser/src/{arbitration,input,chrome-detect}.ts, apps/server/src/runs.ts, apps/web/src/{hooks/use-browser-socket.ts,lib/privy.tsx}, packages/{protocol/src/app.ts,wallet/src/privy.ts}; rewrite docs/handover.md:34 and the ~/code paths in docs/architecture.md and docs/PLAN.md; commit the eight evidence-file skeletons (README shape, HEDERA.md, PRIVY.md, GRAPH.md, VALIDATION.md, FEEDBACK.md, ACQUISITION.md, AI-USE.md).
2. 2. Measurement + isolation decision (Day 1 A, ~1h, gates 18h of Day 2 work): 30 minutes on the live URL counting screencast frames per 10 s and click-to-changed-frame latency, numbers into docs/measurements.md; then write the isolation choice (puppeteer-core contexts vs worker-per-user) and the Sun 6 12:00 CEST cut line into PRODUCT_FABLE51.md; also fix the pin drift (bun 1.4.x everywhere, decide and verify the model id in model.ts:29).
3. 3. Close the open door before any real key lands (interim 1h now, full 6h Day 2): make sockets.ts isTrustedOrigin reject a missing Origin, put a per-session token on /ws/app, /ws/browser, /api/chat, /api/chat/stop, /api/receipts and /api/wallet (bearer or signed cookie), and refuse mandate.update / mandate.freeze{frozen:false} / approval.resolve without it; add a host check to x402_fetch before the first fetch (tools.ts:157). Do not set HEDERA_PRIVATE_KEY on Railway until this is merged.
4. 4. Hedera first real settlement (Day 1 B, ~3h): create and fund the service payTo and a separate payer at portal.hedera.com (mirror-node balance >= 100 HBAR each); read feePayer from https://api.testnet.blocky402.com/supported at boot and emit it in accepts[].extra (oracle.ts:55-65 currently hard-codes extra:{}); set HEDERA_* locally, run the 402 round trip through liveOracleGate verify+settle, confirm the tx on hashscan.io/testnet SUCCESS, paste local and (after task 3) hosted tx ids into HEDERA.md; flip the live /health to hedera:live.
5. 5. Privy Day 1 (B, ~4h, independent of the codebase): create a fresh Privy app (email OTP on, create-on-login off), the agent's P-256 authorization key, commit P_agent v0 JSON (default deny + rule b + 7-day expiry), run the 40-line @privy-io/node script that creates a wallet with the user as owner and the agent key as additional signer and calls eth_signTransaction to 0x...dEaD, paste the policy-denial error with its policy id into PRIVY.md, run the 15-minute secp256k1_sign-under-policy test. Then Day 2 starts with the ledger fixes (in-flight duplicate guard, cached body on the row, persisted ledger + migrations + preDeployCommand) before any per-user pocket is built on top of it.

Total hours to plan Day 3 milestone: 155

## Per-area summaries and verdicts

### server (apps/server)
apps/server is one Bun process (index.ts) that serves the SPA, /health, /oracle/snapshot (x402 402 gate), a hand-written /api router (chat, chat/stop, chat/:id/stream replay, receipts, wallet), and two origin-gated WebSockets (/ws/app for mandate/freeze/receipts, /ws/browser for the screencast and human input). The agent loop (chat.ts, runs.ts, tools.ts, model.ts) is real: AI SDK v7 streamText with a server-owned abort signal, tee'd SSE replay, 8 tools, and a scripted MockLanguageModelV3 fallback when no key is set. Every spend funnels through WorkspaceSession.spend (session.ts) into the pure authorize() policy and an in-memory ledger, and freeze arrives on the app socket and aborts runs. The whole thing is deliberately single-tenant (one SessionId per process, index.ts:41), has zero authentication on any route or socket, keeps no persistent state (memoryLedger, no drizzle migrations folder), and the live Railway deployment reports graph/hedera/model/privy all in stub mode with the oracle's payTo = 0.0.0. Against PRODUCT_FABLE51.md the server has the spine and the leash but none of the per-user custody, persistence, navigation allowlisting, Graph registry, oracle metering, guest path or Telegram work; the 3 commits are all by grmkris on 2026-09-04.

Features:
- [working] Process lifecycle / one-origin Bun.serve with Effect layer: apps/server/src/index.ts:85-147 (Layer.effect, acquireRelease stops server and closes browser); live https://app-production-58dd.up.railway.app/health returns 200 {"modes":{graph,hedera,model,privy all "stub"},"runtime":"bun"}
- [working] Environment loading with placeholder-driven stub/live modes: apps/server/src/environment.ts:30-48 PLACEHOLDER + modeOf; :176-196 modes computed; secrets read via Config.redacted but unwrapped into plain strings at :198-218
- [working] services.ts composition root (graph/oracle/payer/privy/browser/ledger): apps/server/src/services.ts:47-101 picks live vs stub per mode; ledger is always memoryLedger() at :96; privy is built at :75-81 but never referenced anywhere else in apps/server (grep for services.privy / setAddresses returns only session.ts:146 definition)
- [working] Agent loop: streamText, step cap 12, run-owned abort, SSE tee and replay: apps/server/src/chat.ts:79-105 (abortSignal: run.signal, stopWhen stepCountIs(12), consumeSseStream records to run); runs.ts:63-125 record/replay with 8MB buffer cap; router.ts:123-133 GET /api/chat/:id/stream returns replay or 204
- [working] Model tiers: Anthropic / OpenAI-compatible / scripted: apps/server/src/model.ts:111-127; scripted MockLanguageModelV3 emits graph_query then wallet_status then a text stop (model.ts:49-52, 54-109). ANTHROPIC_MODEL hardcoded to "claude-sonnet-5" at :29 (not verifiable offline)
- [working] Tools: browser_navigate / browser_snapshot / browser_click / browser_type: apps/server/src/tools.ts:70-124 call services.browser.agentNavigate/agentSnapshot/agentClick/agentType, outputs capped at 50k chars (cap, :46-56). No URL allowlist/scheme check on navigate (:73-77) and none in packages/browser (grep allow|deny|javascript: finds nothing)
- [working] Tool graph_query with evidence carried into the next spend: apps/server/src/tools.ts:126-151 stores lastEvidence {query, snapshotHash, source, stubbed} per turn; attached to x402 spend at :226-230. Single-subgraph client (environment.ts:140 GRAPH_SUBGRAPH_ID), not the four-deployment registry
- [working] Tool x402_fetch: 402 -> session.spend -> payer.pay -> retry with X-PAYMENT: apps/server/src/tools.ts:153-245; idempotencyKey `x402:${url}:${amount}` (:195); provenance "server" (:201); asset hardcoded hedera:testnet decimals 8 (:184-191). On ledger-duplicate (session.ts:246-259) settle() is skipped so paidBody stays null and the model gets "Paid, but the server returned no 
- [partial] Tool wallet_send (jailbreak refusal beat): apps/server/src/tools.ts:247-301: provenance is always "model" (:264) so authorize() denies untrusted_provenance (packages/wallet/src/policy.ts:85-90); settle() is a stub returning ok:false (:267-277) and the allow branch returns "transfers are not wired to a signer yet" (:286). No provenance "user"
- [working] Tool wallet_status: apps/server/src/tools.ts:303-323 returns mandate rules, frozen, windowSpent; address is always null because setAddresses (session.ts:146) is never called
- [working] WorkspaceSession.spend: price -> authorize -> reserve -> settle -> receipt: apps/server/src/session.ts:184-284; parQuote (:189) so 1 HBAR == $1 and 1 USDC == $1; decision published before settle (:223); receipt written on every outcome incl. deny (:226-236, 286-320). Receipts held in an in-memory array (:110, :317)
- [working] Default mandate rules (leash): session.ts:124-135 builds defaultRules with oracle host + payTo allowlisted (index.ts:80-86); packages/domain/src/mandate.ts:170-204: per_tx $2, window $10/24h, payee/host/network allowlists, 24h expiry, approval_threshold $1. authorize() order: frozen, provenance, expiry, network, payee, host, per_
- [partial] Freeze / unfreeze: apps/server/src/sockets.ts:184-193: mandate.freeze sets frozen and, when true, runs.abortAll(); frozen:false on the same message unfreezes. Only plan steps 1-2 of freeze(user, reason): no signer removal, no pocket key wipe/sweep, no Telegram, no reason recorded. Any app-socket client can send it (no
- [working] Mandate editing via socket: apps/server/src/sockets.ts:195-198 -> session.updateMandate (session.ts:158-161) forces sessionId and preserves frozen; but replaces rules wholesale with no auth, so any socket client can raise caps or add payees
- [stubbed] Approval (ask) flow: authorize() returns _tag "ask" over $1 (policy.ts:166-175); tools.ts:239-241 and :283-285 turn it into a text tool result; sockets.ts:200-206 only echoes approval.resolve as approval.resolved; no approval.request is ever emitted by the server (grep approval in apps/server/src)
- [working] Browser socket: screencast, drain/backpressure, human take, ping/pong: apps/server/src/sockets.ts:106-119 drain resends latest frame under 512KB buffer; :146-151 browser.take aborts runs first; :214-255 subscribe/unsubscribe per socket
- [partial] WebSocket origin gate: index.ts:116-123 rejects untrusted Origin, but sockets.ts:52-62 isTrustedOrigin returns true when Origin header is absent, so any non-browser client (curl/websocat) can open /ws/app and /ws/browser
- [missing] Authentication on HTTP routes and sockets: router.ts:86-145 and index.ts:105-128 have no auth check; livePrivyServer.verify exists (packages/wallet/src/privy.ts:102) but is never called; live GET /api/wallet and /api/receipts return 200 to anonymous curl
- [missing] Multi-tenancy / per-user sessions: index.ts:37-41 one SessionId per process; router.ts:110, :116, :124 use deps.sessionId for every request; chat/:id/stream ignores the id (:119-124 comment)
- [partial] Oracle route GET /oracle/snapshot (x402 seller): apps/server/src/oracle-route.ts:27-88: 402 challenge, verify+settle via gate, query Graph after settlement, x-payment-response header. Flat 0.05 HBAR (:19) not per-deployment; live payTo is "0.0.0" (stub) per curl; stub gate accepts any base64-JSON header (packages/payments/src/oracle.ts:214-232); n
- [broken] Persistence (Postgres ledger, receipts, migrations): migrate.ts:22 points at packages/database/drizzle which does not exist (ls fails); packages/database/src/schema.ts defines spends+receipts tables but only memoryLedger exists (packages/wallet/src/ledger.ts:62); services.ts:96 comment says Postgres is "one factory call" away but no such factory exist
- [working] Static SPA serving with index.html fallback: router.ts:70-84; STATIC_DIR set in Dockerfile:396; live root serves SPA per handover
- [working] Effect Schema -> AI SDK tool schema bridge: apps/server/src/std.ts:32-48 merges toStandardSchemaV1 and toStandardJSONSchemaV1
- [missing] Chrome profile isolation (fresh per session, no cookies): environment.ts:131-133 single persistent CHROME_PROFILE_DIR; Dockerfile:395 /data/chrome-profile on a Railway volume (.railway/railway.ts browserVolume); README.md:196 states the profile is persistent and shared
Plan gaps:
- (14h, Day 2 (per-user), Day 3 (guest path)) §2 Entry + §10 Day 2/3: per-user sessions, guest pool (IP cap, TTL, 30+10 slots), email sign-in; §4 users keyed by Privy DID: One SessionId per process (index.ts:41); every route and socket acts on that one session; no Privy token verification wired (privy.ts:102 verify unused); no guest provisioning, no IP cap, no TTL, no delete-my-data
- (6h, Day 2) §4 / §10 Day 2: auth on the app and browser sockets and on /api/chat so only the owner can drive the Chrome and spend: No auth anywhere; null Origin is trusted (sockets.ts:58); live /api/wallet and /api/receipts answer anonymous curl with 200; mandate.update / mandate.freeze accepted from any socket client
- (6h, Day 2) §10 Day 2: users table, nonces, persisted ledger; §6 restart drill (ledger and pockets survive): memoryLedger only (services.ts:96); receipts in an array (session.ts:110); packages/database/drizzle missing so migrate.ts:22 fails; no Postgres SpendLedger implementation exists; railway.ts has no preDeployCommand
- (10h, Day 2) §4 K_pocket: per-user Hedera ECDSA account, key encrypted under env key, host caps 0.5/2 tHBAR, payTo must be a server-issued directory id, fee payer read from /supported at boot, spends serialized per user: One env-key payer for the whole process (services.ts:67-73); caps are USD-denominated $2/$10 via parQuote (session.ts:189, mandate.ts:176-186); payee allowlist is just the server's own oracle payTo (index.ts:80-86); /supported is never read (blocky402 live feePayer 0.0.7162784 available)
- (4h, Day 2) §4 idempotency state machine (reserved, signed, settled; replay returns cached body; mismatch 409; facilitator timeout reconciles against mirror node): reserve/settle with idempotency key exists (ledger.ts:92-115, session.ts:238-268) but a duplicate returns a receipt with no body (tools.ts:242 'Paid, but the server returned no body'), there is no 'signed' state, no 409, no mirror-node reconcile
- (4h, Day 2) §4 freeze(user, reason): five steps, unfreeze creates a new pocket key and re-adds the signer: sockets.ts:184-193 does steps 1-2 (frozen flag, abortAll); no signer removal, no pocket wipe/sweep, no Telegram, no reason; unfreeze is the same message with frozen:false and no auth
- (12h, Day 2-3) §4 W_user Privy embedded wallet with agent additional signer under P_agent; wallet_send builds eth_signTransaction and is denied by Privy with policy id; provenance 'user' for typed addresses: services.privy never used; setAddresses never called; wallet_send provenance is always 'model' (tools.ts:264) and settle is a stub (tools.ts:267-277); no Privy policy id on any receipt
- (6h, Day 2) §5 The Graph: registry of four pinned deployments, parallel query, _meta block, freshness gate, deployment id + block on every row and receipt, x402 mainnet gateway path recorded on the receipt: Single GRAPH_SUBGRAPH_ID client (environment.ts:140-142); evidence carries only query/snapshotHash/source/stubbed (tools.ts:133-138); oracle-route.ts:69 one lendingMarkets call; live graph mode is stub
- (8h, Day 1 (real settlement), Day 2 (metering, unlock), Day 3 (HCS), Day 5 (card)) §4 Service side + §5 Hedera: real 0.0.x payTo, per-deployment metering 0.0125 tHBAR, HCS message per settlement, one-time receipt token for the unlock page, /.well-known/x402.json service card: Flat 5000000 tinybars (oracle-route.ts:19); live payTo 0.0.0; no HCS; no receipt token; router.ts has no /.well-known route
- (4h, Day 2) §2 Shared Chrome: exact-origin allowlist, data:/blob:/javascript:/file: denied, banking/email/exchange denylist, page-initiated navigations checked: browser_navigate forwards any string to agentNavigate (tools.ts:73-77); no allow/deny logic in apps/server or packages/browser
- (3h, Day 2) §2 Fresh profile per session, wiped at session end, no imported cookies: Single persistent profile dir (environment.ts:131-133) on a Railway volume (Dockerfile:395); README.md:196 documents the opposite behaviour
- (4h, Day 3) §2 Wallet pane: 'paste a 402 URL' probe before pay; directory of allowed sellers with paid-calls counter; §10 Day 3 402 probe and server-issued directory: No probe route, no directory, no counters in router.ts
- (8h, Day 2 (bot), Day 5 (digest)) §2 Telegram pager (pairing, Freeze button with nonce, digest); §10 Day 5 digest cron: Nothing in apps/server; handover.md:253 confirms it is a fresh day of work
- (3h, Day 4) §2 Receipts: policy id and rule label, HashScan link, HCS sequence, ledger line; public receipts page with counters and blocked board: Receipt has decision.ruleId, settlement.transactionId, evidence hash (session.ts:297-316) but no policy id, HashScan link, HCS sequence, ledger line; no public receipts route
- (0.5h, Day 2) §2 Telegram section: 'No approval cards in v1' vs defaultRules approval_threshold $1: Any spend between $1 and $2 returns 'needs the human' with no way to grant (policy.ts:166-175, tools.ts:239-241); either drop the rule or emit approval.request
- (0.5h, Day 1) §10 Day 1: model and runtime versions pinned: model.ts:29 hardcodes "claude-sonnet-5" with no verification; chat.ts:86 passes `instructions:` to streamText (ai 7.0.92) — both compile per CI but the model id is unverified against the API
Hygiene: Tracked local state: apps/server/.froggy/chrome-profile/** is committed (209 files, 2.8MB) including Default/Cookies, Login Data, Login Data For Account, History, Sessions/*, Local State, Preferences — .gitignore has no .froggy entry; DAY0_CHECKLIST_FABLE51.md:68 says `git ls-files | grep -iE 'chrome-profile|Cookies|Login Data'` must print nothing; Ported-from header: apps/server/src/runs.ts:15 'Ported from harness's `packages/kernel/src/agent/runs.ts`'; Prior-project references: Dockerfile:47 and .env.example (Privy section) cite 'humanhook'; docs/handover.md:233 says humanhook's PRIVY_APP_ID/SECRET are on the local box; docs/handover.md:249 and :253 reference 'invok' and '~/code/boter'; Repo identity: .railway/railway.ts binds the deploy to github('grmkris/agentic-wallet') — project is s
Verdicts:
- [partly] 3. `bun run migrate` in apps/server throws because packages/database/drizzle does not exist and no Postgres SpendLedger implementation exists anywhere ->  (`ls packages/database/drizzle` → 'No such file or directory'; `git check-ignore` shows it is not ignored, it simply was never generated (drizzle.config.ts out: './drizzle'). apps/server/src/migrate.ts)
- [partly] 6. On the live URL the x402 'paid round trip' settles against the stub gate: /health reports hedera=stub, the 402 body's payTo is 0.0.0, and stubOracleGate.settle accepts any base64-JSON X-PAYMENT ->  (Live `curl https://app-production-58dd.up.railway.app/health` → {"modes":{"graph":"stub","hedera":"stub","model":"stub","privy":"stub"}}. `curl -i /oracle/snapshot?symbol=USDC` → 402 with accepts[0] {)
Extra: SSRF via x402_fetch: apps/server/src/tools.ts:157-158 does `fetch(url)` for any model-supplied URL string BEFORE any policy runs; the host allowlist (policy.ts:124-135) is only consulted at session.spend after a 402 is received, so non-402 responses  | A Chrome profile is committed to git: `git ls-files apps/server/.froggy` → 209 files including apps/server/.froggy/chrome-profile/Default/Cookies, 'Login Data', History, Sessions/*, LocalStorage. DAY0_CHECKLIST_FABLE51.md hygiene item is not done at  | Unauthenticated JSON API: /api/receipts, /api/wallet, POST /api/chat, POST /api/chat/stop (router.ts:91-142) have no auth; anyone can drive the agent loop or abort another run. One process-global session (index.ts:36-41) means every visitor shares on | wallet_send always uses provenance 'model' (tools.ts:264) so it is unconditionally refused (policy.ts:87-92) and its settle is a stub (tools.ts:267-277); PRODUCT_FABLE51.md §4 'user' provenance path and Privy denial beat do not exist in code. No Priv | migrate.ts header says it runs as a Railway preDeployCommand; .railway/railway.ts has no preDeployCommand and DATABASE_URL is merely `preserve()`d — the Postgres service/volume declared there is unused by the running code. | The Graph 'evidence' on the live deployment is a fixture (`source: fixture:messari-lending`, capturedAt 1756000000000 = a hardcoded timestamp); packages/graph liveGraphClient exists but graph=stub live. Blocky402 /supported currently lists hedera:tes

### web
apps/web is a 16-file Vite/React 19 SPA (TanStack Router, one route) that renders the three panes side by side, paints a JPEG screencast onto a canvas outside React state, forwards pointer/keyboard/IME input to the browser socket, and reads mandate/wallet/receipt/decision state from the app socket. The screencast painter, input capture, freeze toggle, deny card and the append-only receipts list work and match what README/handover claim; the live URL serves the built bundle with every integration reported as stub (privy included, so Sign in is disabled there). Against PRODUCT_FABLE51.md section 2 it is roughly a third of the way: no entry doors (guest/Try it, email OTP flow, invite code, consent), no approval cards (protocol carries them, client ignores them, server never emits them), the arbitration badge is a chip rather than the specified amber/blue/grey canvas border, receipts lack every explorer/Graph/policy-id field, the wallet pane has no pocket balance, caps denominators, seller directory, paid-call counters or 402 probe box, and the two pre-typed stranger buttons are three different suggestion strings. Mobile is a stacked flex column, not the read-only clip door. Wallet address is always "—" because nothing on the server ever calls session.setAddresses and the client never sends the Privy token anywhere.

Features:
- [working] Three-pane layout (chat | browser | wallet) on one screen: apps/web/src/routes/workspace-page.tsx:27-43 renders ChatPane, BrowserPane, WalletPane in a flex row (lg:flex-row); app-shell.tsx:25-66 header + Outlet; live URL returns the built index (assets/index-B1x63-j2.js) and /health 200
- [working] Screencast rendering (binary frame -> canvas, frames outside React): apps/web/src/hooks/use-browser-socket.ts:43-95 createPainter decodes [u32 metaLen][JSON][JPEG] via decodeScreencastFrame, createImageBitmap, latest-frame-wins queue, resizes canvas only on dimension change; :143-147 binary messages go to paint, JSON to setState. handover.md says verified live on Rai
- [working] Human input capture (mouse, wheel, keys, IME text, pointer capture, blur release): apps/web/src/components/browser-pane.tsx:108-230 pointer/wheel/key handlers; hidden focus-proxy input at :305-321 sends input.text; lib/input-capture.ts:75-87 bitmap scaling, :116-117 wheel inversion; 14 unit tests pass (bun test)
- [partial] Arbitration UI (who is driving: amber agent / blue human / grey frozen border): apps/web/src/components/browser-pane.tsx:40-50 MODE_TONE/MODE_LABEL colour a Badge only (agent amber, human emerald not blue, idle grey); canvas wrapper at :279 is a fixed ring-white/10 that never changes with interaction mode; no frozen/grey state derived from mandate.frozen (BrowserPane receives n
- [working] Browser start/navigate controls and overlay states: apps/web/src/components/browser-pane.tsx:59-71 overlayMessage for unavailable/crashed/idle/connecting; :235-237 browser.start; :245-267 address form sends browser.navigate; :288-301 overlay hidden only when status === 'running'
- [partial] Wallet pane: policy card (rule labels + Privy policy id): apps/web/src/components/wallet-pane.tsx:30-57 ruleLabel renders the 7 mandate rule tags from the in-process Mandate; no Privy policy id anywhere (grep 'policy' finds only PolicyDecision); mandate.id not rendered
- [partial] Wallet pane: pocket balance and today's spend against caps: wallet-pane.tsx:116-120 renders only `${formatUsd(windowSpentUsdMicros)} spent in the window`; no denominator (cap) shown; WalletSummary.balanceLabel (packages/protocol/src/app.ts:113) is never referenced in apps/web (grep balanceLabel = 0 hits); server sets it to 'balance unavailable (stub)' or '—'
- [broken] Wallet pane: wallet address: wallet-pane.tsx:92-94 short(wallet?.address) -> '—' whenever null; apps/server/src/session.ts:111 addresses default {signer:null,smart:null} and session.setAddresses (:146) has zero callers (grep across apps/server/src); the client Identity (privy.tsx:31-42) is only read by app-shell.tsx:16 and its 
- [missing] Wallet pane: seller directory with paid-calls counter: no directory/seller/paid-calls markup or protocol message in apps/web/src or packages/protocol/src/app.ts:120-174
- [missing] Wallet pane: 'paste a 402 URL' probe box: no such input; only the browser address bar (browser-pane.tsx:255-263) which sends browser.navigate, not a probe
- [working] Freeze button (client message, not a tool): wallet-pane.tsx:82-90 toggles onFreeze(!frozen); workspace-page.tsx:22-24 sends mandate.freeze v1; server apps/server/src/sockets.ts:184-194 sets frozen, abortAll() when freezing, broadcasts mandate.state; handover says verified both ways on Railway
- [working] Stub chips (per-service live/stub marking): wallet-pane.tsx:70-75,95-107 derives chips from session.welcome modes; use-app-socket.ts:86-89 stores modes; live /health returns graph/hedera/model/privy all 'stub' so all four chips show on the deployed page
- [partial] Denial / blocked card in wallet pane: wallet-pane.tsx:128-152 renders deny (red) or ask (amber) with message/question and code · ruleId; driven by policy.decision pushed from apps/server/src/index.ts:73-75. Missing vs plan: no share button, no Privy raw error text, only the last decision is kept (use-app-socket.ts:102-105 overwrites), n
- [missing] Blocked card in chat pane (plan: every denial renders a blocked card with share button in chat): chat-pane.tsx:78-100 renders text parts and tool parts as '· toolname' only; no tool input/output, no denial card, no share button
- [partial] Receipts list: wallet-pane.tsx:156-205 shows usd amount, decision tag badge, purpose, evidence.snapshotHash prefix, settlement.transactionId as plain text, 'stubbed' chip. Domain Receipt (packages/domain/src/receipt.ts:44-58) has no fields for Graph deployment id/block number, Graph path, HCS sequence, HashScan li
- [missing] Public receipts page (per-endpoint counters, blocked board): apps/web/src/router.tsx:12-21 defines a single '/' route; no other page
- [missing] Approval cards (ask decision -> card -> approval.resolve): use-app-socket.ts:110-115 lists approval.request/approval.resolved as explicit no-ops; no component renders ApprovalRequest; server never emits approval.request (grep 'approval.request' in apps/server/src = 0 hits) and apps/server/src/tools.ts:239-241,283-285 turn 'ask' into a plain string returned 
- [working] Chat: agent tool calls stream, resume after reload, stop: chat-pane.tsx:23-26 useChat({resume:true, DefaultChatTransport '/api/chat'}); :38-43 abort POSTs /api/chat/stop then local stop(); server router.ts:115-133 implements stop and the /api/chat/:id/stream replay (204 when nothing to resume). Tool parts render name only (:89-98)
- [partial] Two pre-typed stranger buttons ('Tell it to pay 0xevil', 'Let it buy something (0.05 tHBAR brief)'): chat-pane.tsx:15-19 has three SUGGESTIONS ('cheapest USDC borrow', 'Buy the packed lending snapshot', 'Send 5 USDC to 0xdead…') shown only while messages.length === 0 (:48-67); texts and count differ from plan and they disappear after the first message
- [stubbed] Privy login config (email OTP door): privy.tsx:159-169 dynamic import gated on privyConfigured (environment.ts:22-27 placeholder REPLACE_ME_PRIVY_APP_ID); config at :204-211 sets loginMethods ['google','email','wallet'] and embeddedWallets.ethereum.createOnLogin 'all-users' — plan section 10 Day 1 says 'email OTP on, create-on-login of
- [missing] Entry doors: 'Try it' guest pool, invite code, consent screen, judge slots: router.tsx has one route; app-shell.tsx only has Sign in/Sign out; no guest/consent/invite code strings anywhere in apps/web/src
- [partial] Mobile layout (plan: phones get a clip + read-only judge screencast + 'send me the desktop link'): workspace-page.tsx:27 flex-col lg:flex-row stacks all three panes below 1024px; chat-pane.tsx:46 w-full lg:w-96, wallet-pane.tsx:78 w-full lg:w-80; app-shell.tsx:30 hides the tagline below sm. Nothing detects a phone, no read-only mode, no clip, no desktop-link prompt; chat and browser both flex-1 i
- [missing] Mandate editing in UI: protocol has mandate.update (packages/protocol/src/app.ts:97-101) and server honours it (sockets.ts:195-199); apps/web never sends it (grep mandate.update in apps/web/src = 0)
- [missing] Sign-in identity surfaced to the server (agent as additional signer on the user's wallet): privy.tsx:98-113 lifts address/signer/token into context; only consumer is app-shell.tsx:16 for the button label; no fetch/socket message carries the token; server has no Authorization handling (grep in apps/server/src = 0)
Plan gaps:
- (12h, Day 2-3) Section 2 Entry: 'Try it' guest door (pooled pre-provisioned identities, IP cap, TTL, judge slots) and 'Sign in with email' Privy OTP door; landing/consent behind an invite code (Day 2 Jonas, Day 3 A): No landing, consent, invite code, or guest flow in apps/web; single '/' route; Sign in is disabled whenever VITE_PRIVY_APP_ID is unset (it is unset on Railway); Privy config uses google/email/wallet + createOnLogin all-users, not email-OTP-only with server-side wallet creation
- (4h, Day 3) Section 2 Chat pane: two pre-typed stranger buttons ('Tell it to pay 0xevil' -> Privy denial card in ~10 s; 'Let it buy something (0.05 tHBAR brief)') and every denial renders a blocked card with a share button: Three different suggestion strings that vanish after the first message; tool parts render as bare names; no blocked card or share button in chat
- (1.5h, Day 2) Section 2 Shared Chrome pane: amber border while agent drives, blue when you grab it, grey when frozen: Only a text Badge changes colour (agent amber, human emerald, idle grey); the canvas ring is static; no frozen state reaches BrowserPane
- (10h, Day 3-4) Section 2 Wallet pane: policy card with Privy policy id, pocket balance and today's spend against the caps, directory of allowed sellers with paid-calls counter, receipts, paste-a-402-URL probe box, Freeze: Has rule labels, window spend (no cap denominator), receipts, Freeze. Missing policy id, pocket balance (balanceLabel not rendered and server has no real balance), directory, paid-call counters, probe box; wallet address always '—' because session.setAddresses is never called
- (8h, Day 3-4) Section 2 Receipts: Graph deployment ids and block numbers, Graph path (x402 vs Studio), policy id + rule, Hedera tx id with HashScan link, HCS sequence number, ledger line ('0.10 / 2 tHBAR today'); public receipts page with per-endpoint counters and blocked board: Receipt schema (packages/domain/src/receipt.ts) has none of the Graph/HCS/ledger fields; pane shows amount, decision tag, purpose, snapshot hash prefix, raw transactionId text; no links; receipts are lost on reload (client never fetches /api/receipts); no public page
- (6h, Not scheduled in section 10 (plan section 4 says 'no approval path' for the daily job; session job implies one)) Section 4/handover 'next' #2: approval cards for the 'ask' decision (four-kind vocabulary, deadline, resolve via approval.resolve): Client explicitly ignores approval.request/approval.resolved; no card component; server never emits approval.request and returns 'needs the human' as a string to the model, so an over-threshold spend has no human path at all
- (4h, Day 2 / Day 4) Section 2: desktop-only; phones get a clip, a read-only screencast of the judge worker and 'send me the desktop link' (Day 2 copy+clip door, Day 4 read-only judge screencast): Panes stack vertically under lg with no phone detection, no read-only mode, no clip, no desktop-link prompt; wallet pane falls off-screen on a phone
- (3h, Day 3) Section 4: wallet pane renders the policy id and rule label from the committed JSON and appends Privy's raw error text underneath: Deny card shows code · ruleId from the in-process mandate only; no committed policy JSON, no Privy error text field in PolicyDecision (packages/domain/src/mandate.ts:139-145)
- (3h, Not scheduled) Section 2 Wallet pane: 'Unfreeze is a human action in the web pane'; handover #4 mandate editing in the UI: Unfreeze exists (same button). Mandate editing: protocol + server honour mandate.update but the pane is read-only
- (2h, Day 1) Section 10 Day 1: 'measurement of the live URL committed (fps, click round-trip)': No fps/latency instrumentation in the client; frame meta.ts is documented as 'staleness display only' but nothing displays it
Hygiene: 209 files of a real Chrome profile are tracked in git under apps/server/.froggy/chrome-profile/ (added in commit fd4bdc9), including Default/Cookies (20480 bytes), Default/Login Data (40960 bytes), Default/History, Sessions/, Local State; .gitignore does not exclude .froggy/. Must be purged from history before the repo goes public (all tracks require a public repo).; Prior-project references in shipped source: apps/web/src/hooks/use-browser-socket.ts:10 'Ported in spirit from invok's use-browser-ws.ts'; apps/web/src/lib/privy.tsx:4 'Three moves lifted from humanhook'; docs/handover.md and .env.example reference humanhook's Privy app and dashboard traps; handover says PRIVY_APP_ID/SECRET on this box are humanhook's app credentials.; Single-tenant assumptions baked into the client/server con
Verdicts:
- [partly] 5. A real Chrome profile with Cookies and Login Data is committed: `git ls-files apps/server/.froggy | wc -l` = 209 and `git cat-file -s HEAD:'apps/server/.froggy/chrome-profile/Default/Login Data'` = -> An entire Chrome user-data directory (209 files, ~4.1 MB, incl. the Cookies, Login Data, History, LocalStorage SQLite files and machine-identifying Preferences/Local State) is committed since fd4bdc9 and is not git-ignored — but as of HEAD those credential stores contain zero rows (no cookies, no lo (`git ls-files apps/server/.froggy | wc -l` → 209 (4,153,290 bytes total). `git cat-file -s HEAD:'.../Default/Login Data'` → 40960; Cookies → 20480. `git log --diff-filter=A -- '.../Login Data'` → fd4b)
Extra: Tests could not be run: no node_modules at repo root or in any package (`ls node_modules` -> No such file or directory), so `bun test` was not executed; verdicts rest on source reading and live curls. | Live deployment is fully stubbed: `curl /health` -> {"modes":{"graph":"stub","hedera":"stub","model":"stub","privy":"stub"},"runtime":"bun","status":"ok"}. docs/handover.md concedes the same (three keys missing, Privy deliberately unset on Railway). | Plan vs code on the Chrome profile: PRODUCT_FABLE51.md section 2 requires 'Fresh profile per session, wiped at session end, no imported cookies'; the code and README do the opposite — README 'Known limits': 'The Chrome profile is persistent, so the a | wallet_send hard-codes provenance 'model' for every call (apps/server/src/tools.ts:264 `provenance: "model"`), and its allowed path is a no-op (tools.ts:286 'Allowed by policy, but transfers are not wired to a signer yet.'; settle stub at :270-279 re | approval.resolve from a client is accepted and merely re-broadcast as approval.resolved to every app socket (apps/server/src/sockets.ts:200-206); it does not resolve anything, and since nothing ever emits approval.request there is nothing to resolve. | Receipts and the spend ledger are per-process memory (apps/server/src/session.ts:110; packages/wallet/src/ledger.ts:63-66 Maps), so 'server retains history' in claim 4 is true only until the Railway container restarts; live /api/receipts currently re

### browser
packages/browser (12 source files, 2416 lines, 2 commits on 2026-09-04) is a single-tenant shared-Chrome layer over Bun.WebView: a per-view serialised CDP queue with deadlines, a JPEG screencast with unconditional frame acks and socket backpressure, input translation, a human/agent arbitrator (1.5 s quiet window, 15 s starvation cap), an accessibility-tree snapshot with @eN refs and a page-content fence, popup re-adoption, and stale SingletonLock clearing. The core mechanics the handover claims (screencast paints, you can click into it, agent drives over CDP) are implemented and coherent; the live URL answers /health with every mode stubbed. Against PRODUCT_FABLE51.md section 2 it is inverted on the safety properties: there is exactly one BrowserSession and one persistent shared profile per process (no per-user isolation, no fresh-profile-per-session), no URL allowlist/denylist or scheme denial on any navigation path (agent tool, human navigate, popup adoption), no frozen/grey state, no stealth argv. Hygiene is red on two DAY0 items: 209 files of a real Chrome profile (Cookies, Login Data, History) are committed under apps/server/.froggy/chrome-profile, and three files carry undisclosed "Ported from invok" headers. Tests exist only for arbitration, chrome-detect and profile (26 pass); cdp, screencast, snapshot, tabs, popups and session are untested.

Features:
- [working] Bun.WebView Chrome backend (spawn, argv, profile dir, no auto-attach): session.ts:51-68 spawnWebView passes backend {argv, path, type:'chrome', url:false}, dataStore.directory, width/height; types/webview.d.ts:20-66 hand-written declaration (absent from @types/bun@1.4.0); Dockerfile:63-72 installs chromium and sets FROGGY_CHROME; docs/handover.md says verified renderin
- [working] Chrome detection (FROGGY_CHROME > system paths > Playwright cache, full build before headless shell): chrome-detect.ts:140-170 detectChrome; :78-84 rankChromiumDirs numeric revision ordering; 11 tests in chrome-detect.test.ts pass.
- [working] CDP seam (per-view command queue, per-command deadline, event subscription): cdp.ts:86-108 chains every send behind the previous settlement with bestEffort so a rejection does not poison the chain; :70-84 withDeadline 30 s default; :110-133 on() wraps EventTarget MessageEvent. Only one implementation exists (Bun.WebView); cdp.ts:5-8 names the raw-DevTools-WebSocket implement
- [working] Screencast (Page.startScreencast JPEG, unconditional ack, latest-frame resend, backpressure): screencast.ts:144-150 startScreencast everyNthFrame:1 quality 60 bounded to viewport; :164-170 ack before any send decision with 2 s deadline; :85-89 resendLatest on subscribe/drain; apps/server/src/sockets.ts:245-252 subscriber returns false above MAX_BUFFERED_BYTES; :103-111 serialised sync. No fp
- [working] Human input (mouse/key/text -> Input.* with 5 s deadline, human credited first): input.ts:75-119 inputCommand translation, rawKeyDown/keyUp, insertText for printable; :123-131 dispatchInput timeoutMs 5000; session.ts:183-195 noteHumanInput before dispatch. inputCommand is exported (index.ts:11) but has no test.
- [working] Arbitration (human wins instantly, 1.5 s quiet window, 15 s starvation cap, ref-counted agent holders): arbitration.ts:20 HUMAN_ACTIVE_MS=1500, :25 AGENT_WAIT_MS=15_000 match PRODUCT section 2; :103-117 withAgentControl; :64-88 noteHumanInput timer hands back to agent or idle; 8 tests pass. Take-page ordering enforced server-side: sockets.ts:146-150 runs.abortAll() before handleClientMessage('browser.
- [working] Accessibility snapshot with @eN refs and page-content fence: snapshot.ts:24-25 PAGE_CONTENT_FENCE constant; :111-143 renderTree rebuilds refs per capture (MAX_LINES 400, MAX_LABEL 120, 13 interactive roles); :152-188 capture via Accessibility.getFullAXTree with graceful fallback text. Main frame only; no iframe handling; no tests.
- [working] Agent click by ref (scrollIntoView, getBoxModel centroid, press/release with 3 s deadline): session.ts:264-278 agentClick resolves ref or returns a stale-snapshot note; :296-358 clickNode. Both dispatches are bestEffort so a timed-out press still reports ok:true (session.ts:337-357).
- [partial] Agent type: session.ts:280-288 only Input.insertText into whatever is focused; no element targeting, no clear, no Enter, no select/scroll/back tools exposed (apps/server/src/tools.ts:70-125 exposes navigate/snapshot/click/type only).
- [working] Tabs registry (open, activate, close, polled title/url): tabs.ts:98-113 openTab navigates first because Bun.WebView has no cdp() before a load, Page.enable + domContentEventFired debounce 300 ms; :124-142 closeTab tolerates a dead window. No agent tab tools; PRODUCT section 2 says one tab.
- [partial] Popup adoption (window.open re-opened as an owned tab): popups.ts:39-42 adopts only type page with openerId and http(s) url; :71-76 adopt then Target.closeTarget; popups.ts:10-15 documents lost opener/postMessage context. Adoption goes through session.ts:409-411 tabs.openTab with no arbitration or URL check, and tabs.ts:109 makes the popup the active (ca
- [working] Stale profile lock clearing (SingletonLock/Cookie/Socket, hostname-pid liveness): profile.ts:70-110 readlink of the dangling symlink, holderIsAlive via kill(pid,0) with EPERM as alive; session.ts:397-403 refuses start when held; 7 tests pass. Commit 8bed8b0.
- [partial] Crash detection and pane status (idle/starting/running/unavailable/crashed): errors.ts:47-58 regex on error message (closed the pipe|view is closed|not connected|chrome exited) because Bun gives no process handle; session.ts:432-446 guard flips status to crashed and disposes arbiter/screencast. Only wraps navigate and human input; agent snapshot/click/type paths (session.ts:
- [missing] URL allowlist / scheme denial / domain denylist on agent navigation: grep for allowlist|denylist|new URL( in packages/browser returns nothing; session.ts:236-243 navigate passes the string straight to view.navigate; apps/server/src/tools.ts:70-85 browser_navigate passes url to agentNavigate unchecked (schema only annotates 'An absolute http(s) URL'). The only host_al
- [missing] Allowlist on page-initiated navigations (redirects, location.href, window.open): No Fetch/Page interception anywhere; cdp.ts:16-18 notes Bun consumes Page.frameNavigated so navigations are only observed via domContentEventFired polling (tabs.ts:106-108). popups.ts adopts any http(s) popup unconditionally.
- [missing] Fresh profile per session, wiped at session end, no imported cookies: session.ts:40 'Persistent, so the agent shops as you'; apps/server/src/services.ts:83-86 one profileDirectory from CHROME_PROFILE_DIR for the process lifetime; .railway/railway.ts declares a persistent browser-profile volume; README 'Known limits' states the opposite design to PRODUCT section 2.
- [missing] Per-user isolation (one Chrome/context per user or worker-per-user): services.ts:83-86 constructs exactly one BrowserSession; index.ts:58 createServices called once; sockets.ts:70,235,245 every browser socket joins the same set and subscribes to the same screencast; sockets.ts:150 browser.take from any socket calls runs.abortAll(). DAY0_CHECKLIST line 14 records the 
- [missing] Frozen (grey) browser state and freeze reaching the browser: packages/protocol/src/browser.ts:22 InteractionMode = agent|human|idle only; apps/web/src/components/browser-pane.tsx:39-43 amber/emerald/grey(idle), no frozen class and human is green not blue; sockets.ts:184-190 mandate.freeze calls setFrozen and abortAll but never touches services.browser.
- [partial] Stealth argv and viewport (Day 2 A item): chrome-detect.ts:179-184 argv is only --no-sandbox/--disable-dev-shm-usage on linux plus --hide-scrollbars; session.ts:37 default viewport 1280x800 exists; no stealth flags, no user-agent, no window.ethereum test.
- [working] Browser/wallet import boundary: tools/graph.ts:65-70 forbids @froggy/browser <-> @froggy/wallet; packages/browser/package.json depends only on @froggy/domain and @froggy/protocol.
Plan gaps:
- (18h, Day 2, Sun 6) Section 2 / Day 2: per-user isolation (worker-per-user or puppeteer-core contexts), at least 5 fps at 1280 wide and input latency under 300 ms, else Sun 6 12:00 CEST cut line: One BrowserSession, one Chrome, one profile per process (services.ts:83-86); every browser socket shares the screencast and browser.take aborts all runs (sockets.ts:150,235,245). Needs a second CdpTab implementation (raw DevTools WebSocket or puppeteer-core), a session registry keyed by user, socket
- (3h, Day 2, Sun 6) Section 2 / Day 2: fresh profile per session, wiped at session end, no imported cookies, no window.ethereum: Profile is persistent and shared by design (session.ts:40, README Known limits, railway.ts browser-profile volume). Needs per-session temp profile dir, wipe on close, and a test that window.ethereum is undefined.
- (3h, Day 2, Sun 6) Section 2 / Day 2: exact-origin allowlist with a real URL parser; data:, blob:, javascript:, file: denied; banking/email/exchange denylist; applied to the agent's navigate tool: No URL check exists in packages/browser or in tools.ts browser_navigate. Needs a pure checker (new URL, scheme set, origin allowlist, denylist) called in BrowserSession.navigate/agentNavigate and the human browser.navigate path, with a denied-navigation result surfaced to the pane and the model.
- (5h, Day 2, Sun 6) Section 2 / Day 2: page-initiated navigations (redirects, location.href, window.open) pass the same check: Nothing intercepts navigations; Bun consumes Page.frameNavigated (cdp.ts:16-18). Needs Fetch.enable document-request interception (or Page.setInterceptFileChooser-style gating) on every tab plus the allowlist check inside popups.ts adopt before tabs.openTab.
- (2h, Day 2-3) Section 2: grey border when frozen; blue when the human grabs; freeze stops the browser too: InteractionMode has no frozen value (protocol browser.ts:22); mandate.freeze never calls the browser (sockets.ts:184-190); human colour is emerald not blue (browser-pane.tsx:42). Needs a frozen flag in BrowserState, Page.stopLoading and agent-tool refusal while frozen, and the pane class.
- (1h, Day 1, Sat 5) Day 1 A: 30-minute measurement of fps and click round-trip on the live URL committed to docs/measurements.md: No measurement exists; docs/measurements.md absent; screencast has no frame counter or timing hook.
- (1.5h, Day 1, Sat 5) Day 1 A: hygiene commit for anything that must not ship (DAY0 E.68 no browser profile tracked; E.69 no undisclosed prior-project references): 209 profile files tracked under apps/server/.froggy/chrome-profile (Cookies, Login Data, History, Sessions) since fd4bdc9; .gitignore has no .froggy entry. arbitration.ts:11, input.ts:6, chrome-detect.ts:9 say 'Ported from invok'; docs/architecture.md:116-132 names Invok/Harness with a local path; n
- (1h, Day 2, Sun 6) Day 2 A: stealth argv, viewport: argv is sandbox/shm/scrollbars only (chrome-detect.ts:179-184); viewport 1280x800 exists. Needs the stealth flag set and a test.
- (8h, Day 3-4) Day 3 A: guest path with pooled pre-provisioned seats, reserved judge worker; Day 4 A: read-only judge-worker screencast behind the phone door: Depends on isolation; nothing keyed per user, no read-only (input-less) subscriber mode on the browser socket.
- (6h, Day 4-6) Day 4 A: load test of six sessions with memory graph; Day 5 A: freeze-aborts-mid-step test; Day 6 A: restart drill (profiles ephemeral): No load script, no mid-step freeze test (the ordering is only a comment at sockets.ts:146-150), no restart drill. Blocked until isolation and per-session profiles exist.
- (3h, Day 2, Sun 6) Day 2 A: the receipt-token unlock page rendered in the shared Chrome: Out of packages/browser but on the Chrome owner's lane; no unlock page or token path found in this reading (not audited in depth here).
Hygiene: Tracked local state: `git ls-files apps/server/.froggy` lists 209 files (2.8 MB) of a real Chromium profile including Default/Cookies, Default/Login Data, Default/History, Default/Sessions, SingletonLock, added in commit fd4bdc9 (2026-09-04). sqlite read-only check: cookies table 0 rows, logins 0 rows, history 1 url (https://example.com/), so no secrets today, but DAY0 E.68 requires it untracked and ignored; .gitignore has no `.froggy` entry so the next `bun dev` re-dirties the tree.; Ported-from headers without disclosure: packages/browser/src/arbitration.ts:11 'Ported from invok, which ported it from harness', input.ts:6 'Ported from invok', chrome-detect.ts:9 'Ported from invok'. DAY0 E.69 requires either a README/AI-USE.md disclosure with a public link or header removal; neither README
Verdicts:
- [partly] 3. A real Chrome profile is committed: `git ls-files apps/server/.froggy | wc -l` prints 209 and includes Default/Cookies and Default/Login Data, first added in fd4bdc9; .gitignore contains no .froggy -> A genuine Chrome 151 user-data-dir (209 files, Cookies, Login Data, Web Data, Secure Preferences, Local State) is committed in fd4bdc9 and .gitignore has no .froggy line — but the committed databases are empty: cookies table 0 rows, logins 0 rows, autofill 0, credit_cards 0, History has no urls tabl (`git ls-files apps/server/.froggy | wc -l` -> 209; listing includes apps/server/.froggy/chrome-profile/Default/Cookies, Default/Login Data, Default/Login Data For Account, Default/Web Data, Default/Se)
- [partly] 6. Popup adoption bypasses arbitration and steals the cast: popups.ts:71-76 calls deps.adopt -> session.ts:409-411 tabs.openTab, and tabs.ts:109 sets the new tab active, so window.open('https://attack -> The adoption path does bypass arbitration and any host/origin allowlist and does make the popup the cast target, but it is not 'without any URL check': popups.ts:39-42 isAdoptable requires type==='page', an openerId, and a /^https?:/ URL, so javascript:/file:/data: popups are not adopted. https://at (packages/browser/src/popups.ts:59-77 handle() -> isAdoptable (lines 39-42: type 'page', openerId defined, /^https?:/u.test(url)) -> claim() -> `await bestEffort(deps.adopt(info.url))` then Target.clos)
- [partly] 5. Only 3 of 12 source files are tested: bun test in packages/browser runs arbitration.test.ts, chrome-detect.test.ts, profile.test.ts (26 pass); no test imports cdp.ts, screencast.ts, snapshot.ts, ta -> 3 of 13 non-test source files have a test file (3 of 12 only if index.ts is excluded). Everything else in the claim holds. (`bun test` in packages/browser (no local node_modules; root workspace resolution): bun test v1.3.14, 26 pass, 0 fail, 34 expect() calls, 3 files. `ls packages/browser/src/*.ts | grep -v .test.ts | wc )
- [partly] 6. Popup adoption bypasses arbitration and steals the cast: popups.ts:71-76 calls deps.adopt, which is session.ts:409-411 tabs.openTab, and tabs.ts:109 sets the new tab active, so a page calling windo -> Correct on arbitration bypass and cast steal; wrong on 'any URL check' — popups.ts:39-42 isAdoptable requires type==='page', an openerId, and /^https?:/ on the URL, so javascript:/file:/data: popups are not adopted. Any http(s) origin is, with no allowlist/denylist. (packages/browser/src/popups.ts:39-42 isAdoptable: `info.type === "page" && info.openerId !== undefined && /^https?:/u.test(info.url)`. popups.ts:59-77 handle -> claim -> `await bestEffort(deps.adopt(i)
Extra: Both WebSockets accept clients with no Origin header: apps/server/src/sockets.ts:56-61 isTrustedOrigin returns true when origin is null ('curl, a test'), and index.ts:116-127 is the only gate; there is no auth token on /ws/browser, so any non-browser | Plan §2 isolation items absent from packages/browser: no fresh-profile-per-session (profile is persistent by design, session.ts:40-41, README 'Known limits'), no exact-origin allowlist, no scheme denylist for agent/human navigate, no banking/email de | Freeze does not grab the page: mandate.freeze aborts runs (sockets.ts:189-191) but never calls browser.takePage(); the browser interaction mode is untouched, and after abort the arbitration state falls to 'idle' via releaseAgent (arbitration.ts:182-1 | Plan §4 freeze(user, reason) steps 3-5 (remove agent signer, wipe pocket key, Telegram) have no code; setFrozen is a flag flip (session.ts:163-165) consumed only by policy.ts:78. | The committed .froggy profile is not what runs in production: .dockerignore excludes .froggy and Dockerfile:86 sets CHROME_PROFILE_DIR=/data/chrome-profile (a Railway volume per docs/handover.md). DAY0_CHECKLIST_FABLE51.md:68 already lists 'No browse | Popup watcher is attached only to the first tab's CDP session (session.ts:406-414, inside doStart); it relies on Target.setDiscoverTargets being browser-wide. Adopted tabs are re-opened as bare GETs with no opener (popups.ts:10-15), so OAuth-style po

### wallet (packages/wallet: Privy adapter, policy engine, spend ledger) plus its only consumers in apps/server
packages/wallet is 798 lines in one commit (61ef247, 4 Sep 22:08): a pure policy engine `authorize()` (policy.ts), an in-memory idempotent spend ledger (ledger.ts), and a Privy server adapter (privy.ts) whose live half makes exactly two SDK calls (verifyAccessToken, users().get by identity token) and whose stub derives a sha256 pseudo-DID. The policy engine and ledger are real and unit-tested (20 cases) and are wired into the one spend choke point `WorkspaceSession.spend` (apps/server/src/session.ts); frozen, provenance, expiry, network/payee/host allowlists, per-tx cap, rolling window cap and approval threshold all evaluate outside the model. The Privy adapter is dead code: `services.privy.verify/addresses` and `session.setAddresses` have zero callers, the web never sends a token to the server, and there is no wallet creation, no authorization key, no P_agent policy JSON, no `eth_signTransaction`, no raw-sign, no per-user pocket. Against PRODUCT_FABLE51.md section 4 the custody split (W_user under Privy policy + K_pocket per user) is entirely absent: caps are host-side USD caps with HBAR priced at par $1, the payer is one env-level HEDERA_PRIVATE_KEY shared by all sessions, and the live deploy reports privy=hedera=graph=model=stub. Two correctness gaps matter for the plan's Day 2 "paid twice, charged once" test: an in-flight duplicate key pays twice, and a settled replay returns no cached body.

Features:
- [stubbed] Privy SDK: access-token verification (who is this): privy.ts:102-114 calls client.utils().auth().verifyAccessToken and returns claims.user_id; but grep shows no caller of `.verify(` anywhere in apps/ or packages/ outside privy.ts, and apps/web/src never sends a token (grep token()/Authorization in apps/web/src: 0 hits). Live mode is selected in apps/
- [stubbed] Privy SDK: address lookup from identity token (signer vs smart): privy.ts:86-98 calls client.users().get({ id_token }) and pickAddresses (L56-70) splits wallet/smart_wallet; the only consumer `WorkspaceSession.setAddresses` (apps/server/src/session.ts:146) has zero callers, so walletSummary().address (session.ts:176) is always null. Setting PRIVY_APP_ID live chan
- [missing] Privy wallet creation with user owner + agent additional signer under P_agent (plan 4, W_user): Only PrivyClient constructor (L80), users().get (L91), verifyAccessToken (L104-107) exist; no wallets().create, no policies, no authorization key, no eth_signTransaction/eth_signTypedData_v4/_rawSign anywhere (grep 'wallets()\|policy\|_rawSign\|signTransaction' in packages/wallet: 0 hits). No commit
- [working] Privy stub identity (pseudo-DID from any non-empty token, obviously-hashed address): privy.ts:134-151: `did:stub:` + sha256 prefix, address = 0x + 40 hex of the sha256; empty token -> null / NO_ADDRESSES. Marked mode:'stub' and surfaced to the pane via environment.modes.
- [working] authorize(): kill switch (frozen) evaluated first, not rule-shaped: policy.ts:78-83 returns deny('frozen') before any rule; test policy.test.ts:69-74 covers empty rule list + frozen. Mandate.frozen is preserved across mandate.update (apps/server/src/session.ts:159).
- [working] authorize(): provenance gate (page/model never payable): policy.ts:87-92 uses isPayable (packages/domain/src/address.ts:54-57, TRUSTED = mandate|server); tests policy.test.ts:76-96. Gap vs plan: the Provenance enum (address.ts:46-51) has no 'user' value, so the plan's 'user-typed address passes host gate and is denied by Privy' beat cannot be expressed; w
- [working] authorize(): expiry rule: policy.ts:94-99 denies when now > notAfter; default notAfter = now + 24h (packages/domain/src/mandate.ts:194-198). Plan wants a 7-day Privy timestamp condition plus a session TTL; only a host rule exists.
- [working] authorize(): network, payee and host allowlists (host matched on label boundary): policy.ts:101-135; hostAllowed (L64-70) is exact-or-dot-suffix, tested against `oracle.example.com.attacker.test` at policy.test.ts:143-151; payee ids normalised via normalizePayeeId (address.ts:76). Host rule is skipped entirely when intent.host is undefined (policy.ts:124), which is the case for w
- [working] authorize(): per-tx cap and rolling window cap: policy.ts:137-161; window sums `recent` entries with at >= now - windowMs; tests policy.test.ts:98-141. Defaults $2 per tx, $10 per 24h (mandate.ts:176-186). Caps are in UsdMicros only; the plan's K_pocket caps are in tHBAR (0.5/call, 2/day).
- [working] authorize(): approval threshold evaluated last, deny beats ask: policy.ts:166-175; test policy.test.ts:173-184. The 'ask' outcome is produced but nothing parks the turn — tools.ts:239-241 and :283-285 just return a string to the model (handover.md:50 admits this).
- [partial] Pricing an intent to USD (quote recorded on receipt): session.ts:186-190 always uses parQuote (1 unit = $1) for every asset including HBAR (x402_fetch builds an 8-decimal HBAR amount at tools.ts:183-191). The 'unpriceable' DenialCode (mandate.ts:130) is never emitted. So a 0.05 tHBAR spend is judged as $0.05.
- [working] Spend ledger: reserve-before-pay, per-session serialisation: ledger.ts:62-107 writes status 'reserved' under a per-session promise chain (serialize L68-89); session.ts:238-244 reserves only after an allow and before request.settle(). Tests ledger.test.ts:19-28, 90-101.
- [partial] Spend ledger: idempotency (same key -> same row): ledger.ts:95-102 returns the existing row for (sessionId, idempotencyKey). But session.ts:246 short-circuits only when row.status === 'settled'; a duplicate arriving while the first is still 'reserved' (in flight) or after a 'failed' proceeds to request.settle() again (session.ts:261). Also on a set
- [working] Spend ledger: window query excludes refused, includes reserved/settled/failed: ledger.ts:117-127; tests ledger.test.ts:50-88. Note session.spend never writes a 'refused' row (deny returns before reserve, session.ts:226-236), so 'refused' is only reachable via a direct settle() call.
- [missing] Persisted ledger (Postgres, unique (session_id, idempotency_key)): schema.ts:30-55 declares spends/receipts/mandates tables with the unique index, but apps/server/src/services.ts:96 wires memoryLedger() unconditionally; no code reads or writes packages/database from the server (only migrate.ts). Plan Day 2 wants users table + nonces + ledger in the database; schema
- [partial] Freeze: flag + abort active run: sockets.ts (case 'mandate.freeze', L185-194 in file) calls session.setFrozen and runs.abortAll() when frozen; unfreeze is the same message with frozen:false from any app socket. Plan freeze(user, reason) steps 3-5 (remove agent signer, wipe pocket key + sweep, Telegram) do not exist; there is no sig
- [working] Mandate editing without unfreeze side channel: session.ts:158-161 forces sessionId and preserves frozen on mandate.update. Not authenticated: any client on an allowed origin can post mandate.update / mandate.freeze (sockets.ts handles them with no identity check; index.ts:37-41 is one session per process).
- [missing] wallet_send tool -> raw Privy denial with policy id (plan video beat): tools.ts:247-301: provenance is hardcoded 'model' so every call is refused by the host provenance gate; settle() is a stub returning ok:false; tools.ts:286 literally returns 'Allowed by policy, but transfers are not wired to a signer yet.' No Privy call is ever reached.
- [partial] x402 payee provenance and directory rule (plan: payTo must be a server-issued directory id): tools.ts:196-201 labels the 402 body's payTo as provenance 'server'; the payee_allowlist rule (default payeeIds = the server's own oracle payTo, session.ts:128-133) is what actually stops a foreign payTo, not provenance. If a client widens payeeIds via mandate.update, any 402's payTo becomes payable
- [missing] K_pocket: per-user Hedera account, encrypted key, tHBAR caps: services.ts:67-73 builds one liveHederaPayer from environment.hederaAccountId/hederaPrivateKey for the whole process; no per-user key, no encryption, no tHBAR cap. Live /health at 2026-09-05 07:58 UTC shows hedera:'stub'.
- [missing] Per-user serialisation of W_user signatures / $1-per-day typed-data ledger (plan rule a): Serialisation is per SessionId only (ledger.ts:66-89); there is no typed-data path, no Graph mainnet x402 payer, no Base USDC rule.
Plan gaps:
- (4h, Day 1, Sat 5) Day 1 (B): fresh Privy app, agent P-256 authorization key, P_agent v0 JSON committed, 40-line denial script into PRIVY.md: Nothing exists: no policy JSON in the repo, no authorization key handling, no wallets()/policies calls in packages/wallet/src/privy.ts (only verifyAccessToken and users().get). handover.md:34 says the only Privy credentials on the box are an inherited app, which the checklist forbids reusing.
- (6h, Day 2, Sun 6) Day 2 (B): per-user W_user created server-side with user owner + agent additional signer under P_agent: No wallet creation path; Privy adapter is identity-only and uncalled. Requires new SDK calls (wallets().create with owner + additional_signers + policy_ids), storage of wallet id per DID, and the web sending the access token to the server (currently never sent).
- (6h, Day 2, Sun 6) Day 2 (B): per-user K_pocket created from treasury, key encrypted under one env key, never in model context: services.ts:67-73 uses one env HEDERA_ACCOUNT_ID/HEDERA_PRIVATE_KEY as the payer for every session; no account creation, no encryption, no per-user key. Live deploy is hedera=stub.
- (4h, Day 2, Sun 6) Day 2 (B): host caps for K_pocket (0.5 tHBAR/call, 2 tHBAR/24h), payTo must be a server-issued directory id, accepts[] must match hedera:testnet with feePayer read from Blocky402 /supported at boot: Caps exist only in USD with HBAR priced at par $1 (session.ts:186-190); x402_fetch trusts the 402 body's payTo as provenance 'server' (tools.ts:201) and relies on the payee allowlist; nothing reads /supported (which returns feePayer 0.0.7162784 today) and the live 402 carries extra:{}.
- (4h, Day 2, Sun 6) Day 2 (B): idempotency state machine (reserved, signed, settled) keyed on a client-minted payment id; replay returns the cached body; mismatch returns 409; facilitator timeout reconciles against mirror node — 'paid twice, charged once' test green: Ledger has reserved/settled/failed/refused but session.ts:246 only short-circuits on 'settled', so a concurrent duplicate (both see 'reserved') settles twice; a settled replay returns 'Paid, but the server returned no body.' (tools.ts:242); no 409 path; no mirror-node reconciliation; key is server-d
- (5h, Day 2, Sun 6) Day 2 (B): users table, nonces, ledger persisted in the database: packages/database/src/schema.ts has spends/receipts/mandates only, explicitly no users table (schema.ts:5-7, contradicting the plan), and the server never touches it — memoryLedger() is hard-wired at services.ts:96. Restart loses every spend and mandate.
- (4h, Day 2, Sun 6) Day 2 (B): freeze(user, reason) end to end — frozen_at checked before every Privy call and pocket signature, abort run, remove agent signer, wipe pocket key + sweep to treasury; unfreeze is a human web action that mints a new pocket key and re-adds the signer: Only steps 1-2 exist (session.setFrozen + runs.abortAll in sockets.ts); there is no signer to remove or pocket to wipe; unfreeze is the same unauthenticated socket message with frozen:false; freeze is per-process session, not per user.
- (4h, Day 3, Mon 7) Day 3 (B): wallet_send wired to a raw Privy denial with policy id; a user-typed address carries provenance 'user', passes the host gate, is built as eth_signTransaction and denied by Privy; pane renders policy id + rule label + Privy's raw error text: Provenance enum has no 'user' (address.ts:46-51); wallet_send hardcodes 'model' (tools.ts:264) so it never leaves the host; settle() is a stub; no eth_signTransaction call; no policy id in any receipt/decision type (Deny has code/message/ruleId only, mandate.ts:139-145).
- (6h, Day 3, Mon 7) Day 3 (B): Graph mainnet x402 through rule (a) eth_signTypedData_v4 from the demo wallet; $1-per-day host ledger for typed data; receipt records the path: No typed-data payer, no Base mainnet USDC asset (Network literal is base-sepolia|hedera:testnet only, money.ts:18), no per-wallet daily typed-data ledger.
- (5h, Day 4, Tue 8) Day 4 (B): wallet_topup under rule (b) with the 24-hour aggregation shown in the pane and on the receipt: No such tool; no Privy signing path; WalletSummary has no aggregation fields beyond windowSpentUsdMicros (session.ts:175-181).
- (1h, Day 2-3) Section 4: host serialises W_user signatures per user (Privy's rolling cap updates after signing): Serialisation is per SessionId in memoryLedger (ledger.ts:66-89) and covers only the reserve step, not the signature; there is no per-user key at all yet.
- (1h, Day 2, Sun 6) Section 4 rule (c) / entry: expiry = creation + 7 days in Privy plus host session TTL (48h guest): Only a host 'expiry' rule at now+24h (mandate.ts:194-198); no Privy timestamp condition; no session TTL or guest TTL handling.
Hygiene: Tracked browser profile with credentials: `git ls-files apps/server/.froggy` lists 209 files including Default/Cookies, Login Data, Login Data For Account, History, Web Data, Account Web Data (commit fd4bdc9, 2026-09-04 22:14 +0200). `.gitignore` has no `.froggy/` entry. DAY0_CHECKLIST section E row 1 says this must print nothing; it currently fails.; Prior-project references inside the wallet slice and its neighbours: packages/wallet/src/privy.ts:7 ('the same choice humanhook made'), apps/web/src/lib/privy.tsx:4 ('Three moves lifted from humanhook'), apps/server/src/runs.ts:15 ('Ported from harness's packages/kernel/src/agent/runs.ts'), packages/browser/src/{chrome-detect,input,arbitration}.ts ('Ported from invok'), packages/protocol/src/app.ts:43 ('taken from invok'). Checklist E row 2 r
Verdicts:
- [partly] 1. Privy is never actually consulted at runtime: the grep for `.verify(|.addresses(|setAddresses` returns only the definitions in privy.ts:40-43,86,102 and session.ts:146 with no call sites, and `grep -> The substantive claim holds: there are zero call sites of `privy.verify`, `privy.addresses` or `session.setAddresses`, and the web client never calls `identity.token()` nor sends an Authorization header, so no Privy token reaches the server. The grep description is inaccurate in detail: because the  (grep output: only `apps/server/src/session.ts:146`. `grep -rn 'services\.privy\|\.privy\.\|privy\.verify\|privy\.addresses\|\.verify(\|\.addresses(' apps/server/src packages --include='*.ts'` -> rc=1 )
- [partly] 6. The SDK calls in packages/wallet/src/privy.ts:91 (`client.users().get({ id_token })`) and :104-107 (`client.utils().auth().verifyAccessToken()`) match @privy-io/node@0.34.0's exported surface — not -> The surface match is verifiable and verified: I fetched the @privy-io/node 0.34.0 tarball (registry integrity sha512-oAcn4K… identical to bun.lock:676) into the scratchpad, not the repo. `PrivyClient.users(): PrivyUsersService` (public-api/PrivyClient.d.ts:88), `PrivyUsersService.get({ id_token }: { (ls node_modules packages/wallet/node_modules -> 'No such file or directory'; ~/.bun/install/cache has no @privy-io; packages/wallet/package.json pins "@privy-io/node": "0.34.0"; packages/wallet/tsconf)
- [partly] 1. Privy is never consulted at runtime: grep for .verify(/.addresses(/setAddresses returns only definitions (privy.ts:40-43,86,102 and session.ts:146), and no token()/Authorization in apps/web/src, so -> Conclusion holds, but the quoted grep output is wrong: `grep -rn '\.verify(\|\.addresses(\|setAddresses' apps packages --include='*.ts' --include='*.tsx'` prints exactly ONE line, apps/server/src/session.ts:146 (the privy.ts lines are `readonly verify:` / `verify: async (...)` and do not match a `\. (grep output: only apps/server/src/session.ts:146; `grep -rn 'token()\|Authorization' apps/web/src` exit=1; apps/web/src/lib/privy.tsx:111 `token: getAccessToken` (no caller); apps/web/src/components/c)
- [unverifiable] 6. The SDK calls in privy.ts:91 (client.users().get({ id_token })) and :104-107 (client.utils().auth().verifyAccessToken()) match @privy-io/node@0.34.0's exported surface — not verifiable on this chec ->  (No node_modules at root, packages/wallet or apps/server; `ls ~/.bun/install/cache | grep -i privy` → nothing. Pin: packages/wallet/package.json "@privy-io/node": "0.34.0"; bun.lock:676 resolves @privy)
Extra: No test exercises WorkspaceSession.spend at all: `grep -rn 'spend\|WorkspaceSession' apps/server/src/*.test.ts` returns nothing; test files are apps/server/src/{runs,tools}.test.ts and packages/wallet/src/{ledger,policy}.test.ts. tools.test.ts has no | bun test could not be run in any package: no node_modules anywhere in the checkout (workspace deps like `effect` and `@froggy/domain` unresolved). The ledger repro worked only because ledger.ts imports `@froggy/domain` type-only. | wallet_send is a fixed refusal, not a Privy denial: tools.ts:264 hard-codes `provenance: "model"`, so authorize denies on provenance before any signer; tools.ts:286 returns 'Allowed by policy, but transfers are not wired to a signer yet.' if it ever  | Ledger is per-session and in-memory (ledger.ts:62-66, services.ts:96) with no persistence; session.ts:146 setAddresses is never called so walletSummary().address is always null; walletSummary balanceLabel is '—' in live mode (session.ts:177-178), i.e | liveOracleGate emits `extra: {}` unconditionally (oracle.ts:55-65) — even with real Hedera keys the 402 would not carry the Blocky402 feePayer; whether @x402/hedera needs it in `extra` was not checked (no node_modules). | Plan §4 items with no code at this HEAD: K_pocket per-user Hedera account, encrypted key at rest, payTo must be server-issued directory id, `accepts[]` fee-payer check, reserved/signed/settled state machine keyed on a client-minted payment id, 409 on

### payments (packages/payments: x402 oracle gate, Hedera payer, facilitator wiring)
packages/payments is a 4-file, ~500-line package (types.ts, oracle.ts, payer.ts, index.ts + one test file) providing both halves of x402 on hedera:testnet: an OracleGate (challenge + verify/settle through a hand-rolled POST to the facilitator) and a Payer (ExactHederaScheme from @x402/hedera 2.25.0 with an env-key signer), each with a loud stub twin selected in apps/server/src/environment.ts when HEDERA_ACCOUNT_ID/HEDERA_PRIVATE_KEY still hold placeholders. The stub path is what actually runs everywhere today: the live URL returns a real 402 with payTo "0.0.0" and extra {}. The live path is broken as written: the oracle emits requirements with `extra: {}` and nothing ever reads Blocky402's /supported, but both the SDK client (throws "feePayer is required in paymentRequirements.extra for Hedera exact") and Blocky402 itself (verify -> isValid:false invalid_exact_hedera_payload_missing_fee_payer; settle -> success:false same reason, both confirmed by curl) require `extra.feePayer` = 0.0.7162784. So with real keys the payer would fail before signing and the gate would reject every payment; no real settlement can succeed without a code change (small: ~1-2h). Tests exist (6 cases, stub-only) but cannot run on this checkout because node_modules is absent (bun test fails: Cannot find package 'effect'); @x402/hedera is also absent from the local bun cache, suggesting it was never installed on this machine. Against PRODUCT_FABLE51.md section 4 the package covers the wire format only: no per-user pocket, no host caps in tinybars (the wallet ledger prices HBAR at par USD), no payTo directory check, no payment-id state machine (reserved/signed/settled) with 409 on mismatch or mirror-node reconciliation, no HCS receipt, no receipt token, no service card, no HashScan link.

Features:
- [working] 402 challenge (PaymentRequired body, x402 v2, hedera:testnet exact, 0.05 HBAR): oracle.ts:54-81 builds accepts[] + resource; apps/server/src/oracle-route.ts:39-46 returns it with status 402; live curl of https://app-production-58dd.up.railway.app/oracle/snapshot returned HTTP 402 with {accepts:[{amount:"5000000",asset:"0.0.0",extra:{},network:"hedera:testnet",payTo:"0.0.0",sche
- [missing] feePayer in requirements.extra (required by Blocky402 and by @x402/hedera client): oracle.ts:60 `extra: {}`; no code reads /supported anywhere (grep 'supported' in packages/payments and apps/server: none). SDK chunk-WP6D5IUW.mjs:33-34 throws without extra.feePayer; Blocky402 /verify and /settle answered invalid_exact_hedera_payload_missing_fee_payer for a payload with extra:{}; /s
- [broken] Live oracle settle via facilitator (/verify then /settle): oracle.ts:136-159 hand-rolled fetch with body {paymentPayload, paymentRequirements, x402Version} — matches @x402/core HTTPFacilitatorClient body shape (core chunk-RAWLCYSQ.mjs:474-483, 522-531); response decoding at :39-52 reads isValid/invalidReason/success/errorReason/transaction which match Block
- [broken] Live Hedera payer (ExactHederaScheme + createClientHederaSigner env key): payer.ts:70-76 constructs signer with PrivateKey.fromStringECDSA and {network:"hedera:testnet"} (valid per SDK signer.d.mts createClientHederaSigner(accountId, PrivateKey, {network})); payer.ts:96-99 calls scheme.createPaymentPayload(2, requirements); requirements built at payer.ts:50-58 pass throug
- [working] Requirement selection (first hedera:testnet exact entry, not accepts[0]): payer.ts:37-59; tested by oracle.test.ts:82-97 (EVM-only challenge refused)
- [partial] X-PAYMENT header encode/decode (base64 JSON envelope {accepted,payload,x402Version}): payer.ts:61-62 encodes; oracle.ts:91-111 decodes and validates envelope only. Uses v1 header names `x-payment` / `x-payment-response` (tools.ts:215, oracle-route.ts:74); @x402/core 2.25 sends PAYMENT-SIGNATURE and reads PAYMENT-RESPONSE first (core chunk-RAWLCYSQ.mjs:2137-2141, 2173-2177) with X-PAY
- [working] Stub oracle gate: oracle.ts:211-237 returns a real 402 and a visibly fake transactionId `stub-not-a-real-hedera-transaction-<ts>` with stubbed:true; oracle-route.ts:84 propagates stubbed onto the body
- [working] Stub payer: payer.ts:138-164 builds a structurally valid envelope with payload.transaction = "stub-unsigned-no-hedera-key-configured"
- [partial] Facilitator config: environment.ts:154-156 HEDERA_FACILITATOR_URL defaults to https://api.testnet.blocky402.com; .env.example:46; .railway/railway.ts:78 preserve(). Only the base URL is configurable; no auth headers, no timeout, no /supported read at boot (plan section 4 requires the fee payer read from /supported at b
- [partial] payTo: services.ts:61-64 payTo = HEDERA_ACCOUNT_ID (same account is payer and payee — the demo pays itself); apps/server/src/index.ts:85 adds services.oracle.payTo to the mandate allowlist; stub payTo "0.0.0" (oracle.ts:212) is what the live URL currently advertises. Plan: payTo must be a real 0.0.x from a
- [partial] Idempotency / reserve-before-pay: tools.ts:195 key `x402:${url}:${amount}`; session.ts:238-259 ledger.reserve then short-circuits only when status === "settled"; wallet/src/ledger.ts:92-107 returns the existing row for any status, so a row still `reserved` (in flight) or `failed` falls through to settle() again at session.ts:261 — c
- [partial] HBAR amount / cap accounting: session.ts:186-190 parQuote prices tinybars as if 1 HBAR = 1 USD (decimals 8 at tools.ts:185); plan section 4 caps K_pocket at 0.5 tHBAR per call / 2 per day in native units — no tinybar cap exists
- [missing] HCS receipt per settlement, receipt token unlock, /.well-known/x402.json service card, HashScan link: grep -rn 'hcs\|TopicMessage\|well-known\|hashscan' apps packages: no hits (only receipt.transactionId at packages/domain/src/receipt.ts:40 and wallet-pane.tsx:190)
- [missing] Privy-signed Hedera payment (ClientHederaSigner backed by Privy raw sign): payer.ts:1-14 header describes it as future work; only env-key signer exists
Plan gaps:
- (2h, Day 1, Sat 5) Section 4 / Day 1: fee payer read from Blocky402 /supported at boot; accepts[] carries extra.feePayer; first real settlement on HashScan: oracle.ts:60 hardcodes extra:{}; nothing calls /supported; SDK and facilitator both reject without feePayer. Needs a boot-time GET /supported (or env fallback 0.0.7162784), feePayer merged into requirementsFor, and a live test with a funded testnet account
- (4h, Day 1-3) Section 4: payTo is a real 0.0.x service account distinct from the payer, aliases rejected; payTo must be a server-issued directory id: payTo === HEDERA_ACCOUNT_ID (services.ts:63) so payer and payee are the same account; no directory, no alias rejection; tools.ts:196 takes payTo straight from the 402 body with provenance 'server'
- (6h, Day 2, Sun 6) Section 4: idempotency state machine (reserved, signed, settled) keyed on a client-minted payment id stored with (session, URL, amount); replay returns cached body; mismatch 409; facilitator timeout reconciles against the mirror node: Only reserved/settled/failed row status in an in-memory map; in-flight duplicate falls through and can pay twice (session.ts:246-261); cached replay returns no body (tools.ts:242); no 409 path; no mirror-node reconciliation
- (8h, Day 2, Sun 6) Section 4: K_pocket per user, host caps 0.5 tHBAR/call and 2 tHBAR/24h in native units, spends serialized per user, key encrypted at rest: One global env key; HBAR priced at par USD (session.ts:189) so caps are USD-denominated approximations; no per-user pocket
- (6h, Day 2 (unlock), Day 3 (HCS, 2h), Day 5 (service card, 1h)) Section 4/5: one-time receipt token so the shared Chrome can open the unlocked page; /.well-known/x402.json service card; HCS message per settlement; HashScan link on receipt: None of these exist (grep confirms); oracle-route returns JSON only
- (3h, Day 2, Sun 6) Section 4: oracle body becomes the four-deployment brief metered per deployment at 0.0125 tHBAR each (0.05 for the brief): Flat 5000000 tinybars per request (oracle-route.ts:19); no per-deployment metering
- (3h, Day 3, Mon 7) Section 2/3: pay peer endpoints from the directory (fare402 etc.) under the per-call cap; 'paste a 402 URL' probe box: x402_fetch only speaks v1 header names (x-payment/x-payment-response) while @x402/core 2.25 servers send/read PAYMENT-SIGNATURE/PAYMENT-RESPONSE first; no probe endpoint; payer would work against a peer 402 that carries extra.feePayer
- (2h, Day 1) Section 4: Hedera pocket with mode notice; tests runnable in CI: packages/payments tests cannot run on this checkout (no node_modules); no live-path unit test with a mocked facilitator asserting the request body carries feePayer
Hygiene: Package is named @froggy/payments and the 402 advertises serviceName 'Froggy lending oracle' with tags ['defi','lending','the-graph'] (oracle.ts:76-78) — prior-project identity baked into the public 402 body served at the live URL.; payer.ts:1-14 and docs/handover.md:53 reference @privy-io/node wallets._rawSign / humanhook / invok as prior art; README/handover mention humanhook's Privy app secrets being on this box (~/.config/secrets.env) — none in the repo, but the docs point at them.; Single-tenant custody: one HEDERA_PRIVATE_KEY in env serves as both the paying pocket and the payTo for every user (services.ts:61-73); a freeze cannot wipe a per-user key because there is none.; Payer and payee are the same Hedera account in live mode, so a 'real settlement' on HashScan would be a self-tra
Verdicts:
- [partly] 3. A non-2xx facilitator response makes liveOracleGate.settle throw (oracle.ts:145-149) and apps/server/src/oracle-route.ts:56 has no catch, so a malformed X-PAYMENT in live mode yields a 500 from the -> The throw-with-no-catch path is real and reachable, but not every malformed X-PAYMENT hits it. Three outcomes in live mode: (1) header that is not base64 JSON, or JSON missing accepted/payload/x402Version → 402 via oracle.ts:103-111,166-174; (2) base64 JSON whose `accepted`/`payload` have the wrong  (oracle.ts:140-149 `if (!response.ok) throw new Error(...)`; oracle.ts:154-157 also throws on an unrecognised body; oracle.ts:180,192 `await post(...)` with no try; oracle-route.ts:56 `const settled = )
- [partly] 2. Blocky402 rejects any /verify or /settle whose paymentRequirements.extra lacks feePayer with invalid_exact_hedera_payload_missing_fee_payer; the fix is merging /supported's hedera:testnet extra {fe -> /verify was re-observed on 2026-09-05: POST https://api.testnet.blocky402.com/verify with the stub payload and extra:{} -> HTTP 200 {"isValid":false,"invalidReason":"invalid_exact_hedera_payload_missing_fee_payer","payer":""}. With extra:{feePayer:"0.0.7162784"} the same stub payload -> {"isValid":f (curl output 2026-09-05 08:08 UTC: /verify without feePayer -> invalid_exact_hedera_payload_missing_fee_payer; with feePayer -> invalid_exact_hedera_payload_transaction_could_not_be_decoded; /supported)
- [partly] 3. A non-2xx facilitator response makes liveOracleGate.settle throw (oracle.ts:145-149) and apps/server/src/oracle-route.ts:56 has no catch, so a malformed X-PAYMENT in live mode yields a 500 from the -> The throw-with-no-catch chain is real: oracle.ts:145-149 throws on !response.ok, oracle.ts:155-157 throws on an unrecognised body, oracle-route.ts:56 `await deps.gate.settle(...)` has no try/catch, router.ts:161-170 and index.ts:104-131 (Bun.serve fetch with no `error` handler) add none either, so a (packages/payments/src/oracle.ts:140-158, :166-174; apps/server/src/oracle-route.ts:48-65; apps/server/src/router.ts:161-170; apps/server/src/index.ts:104-131 (no error handler passed to Bun.serve); cu)
Extra: No code reads Blocky402 /supported at boot, contrary to PRODUCT_FABLE51.md §4 ('the fee payer read from Blocky402's /supported at boot') — grep for 'supported'/'feePayer' across packages/payments/src and apps/server/src returns nothing. The feePayer  | Even after feePayer is fixed, the live oracle would still advertise payTo = HEDERA_ACCOUNT_ID (services.ts:63), i.e. the payer account pays itself; plan §4's separate treasury/payTo/pocket accounts, HCS receipts, /.well-known/x402.json service card,  | tools.ts:195 idempotencyKey `x402:${url}:${amount}` carries no run id, unlike wallet_send (tools.ts:256 includes deps.run.id). A second, intentional purchase of the same URL later in the same process returns the cached 'settled' row (session.ts:246-2 | oracle.ts:91-95 PaymentEnvelope validates only that accepted/payload exist (Schema.Unknown), so structurally wrong envelopes reach the facilitator and become a 500 via the uncaught throw at oracle.ts:145-149; the stub gate (oracle.ts:223) has the sam | Live probe sequence against Blocky402 with the repo's own stub payload shows the ladder of blockers once feePayer is added: requirements-with-feePayer but accepted-without → accepted_payment_requirements_mismatch; both with feePayer → invalid_exact_h | PRICE_TINYBARS = 5000000 (oracle-route.ts:19) = 0.05 HBAR, which matches plan §4's 0.05 tHBAR brief price; the plan's per-deployment 0.0125 metering is not implemented (one flat price, symbol query param only).

### graph
packages/graph (@froggy/graph, 216-line client + 48-line fixture + 39-line types) is a single-deployment Messari-lending client with two implementations behind one GraphClient interface: liveGraphClient (POST to gateway.thegraph.com/api/subgraphs/id/<one id> with Bearer key, Effect Schema decode of the response) and stubGraphClient (3 hard-coded USDC markets, fixed timestamp, source "fixture:messari-lending", stubbed:true). It is wired in apps/server/src/services.ts:50-57 by environment mode, consumed by the graph_query tool (tools.ts:126-150) and the paid GET /oracle/snapshot route (oracle-route.ts:69-85), and provenance (query, snapshotHash, source, stubbed) is attached to receipts. Everything the plan adds on top is absent: no registry of the four pinned deployments, no parallel query/merge, no _meta block, no freshness gate, no deployment id or block number on rows or receipts, no Studio-vs-x402 provider path, no SKILL.md, no GRAPH.md. The live Railway deployment reports graph:"stub" as of 2026-09-05 07:58 UTC. tools/graph.ts and tools/check-graph.ts are the dependency-boundary declaration/check (unrelated to The Graph) and are complete for what they do.

Features:
- [working] Single GraphClient interface with live + stub implementations selected by env mode: client.ts:107-120 stubGraphClient, client.ts:133-180 liveGraphClient; apps/server/src/services.ts:50-57 picks by environment.modes.graph; environment.ts:105-108 mode is live only when GRAPH_API_KEY and GRAPH_SUBGRAPH_ID are both non-placeholder
- [partial] Live gateway query (Messari standardized lending schema, one deployment): client.ts:24-39 LENDING_QUERY selects markets{name inputToken{symbol} totalBorrowBalanceUSD totalDepositBalanceUSD rates{rate side type}} filtered inputToken_.symbol + isActive, first:25; client.ts:135 url = `${gateway}/subgraphs/id/${options.subgraphId}` (exactly one id); client.ts:144-146 Bearer a
- [missing] Registry of four pinned deployments queried in parallel (plan sec 5): LiveGraphOptions.subgraphId is a single string (client.ts:127); .env.example:37 has one GRAPH_SUBGRAPH_ID; grep for 'registry' across apps/packages finds only ChatRunRegistry; none of the ids JCNWRypm…, D7mapex…, AwoxEZ…, GbKdmBe… appear anywhere in the repo
- [missing] Freshness gate (_meta block older than 2h returns unavailable, fail closed): LENDING_QUERY (client.ts:24-39) has no _meta selection; GatewayResponse schema (client.ts:63-70) has no block field; capturedAt is Date.now() at client.ts:170, i.e. wall-clock of the fetch, not indexed block time
- [missing] Deployment id and block number on every row and receipt: Evidence schema receipt.ts:28-34 = {query, snapshotHash, source, stubbed}; LendingMarket type types.ts:11-19 has no deploymentId/block; the only trace of the id is the `source` URL string (client.ts:175)
- [working] Response decoding at the boundary (schema drift fails loudly): client.ts:49-72 Schema.decodeUnknownResult(GatewayResponse); client.ts:150-167 throws on !response.ok, decode failure, and GraphQL errors[]
- [working] Cheapest-first sort and VARIABLE-rate preference: client.ts:77-81 rateOf prefers side+type VARIABLE, falls back to any rate for the side, defaults 0; client.ts:104-105 byCheapestBorrow applied in both stub (114) and live (173)
- [working] Stub/fixture with visible provenance: fixture.ts:18 FIXTURE_CAPTURED_AT fixed; fixture.ts:20-48 three USDC markets (Aave V3, Compound V3, Morpho Blue); client.ts:116-117 source 'fixture:messari-lending', stubbed:true; tools.ts:139-141 appends '[STUB: recorded fixture…]' to model-visible result; session.ts:308 receipt.stubbed ||= evidenc
- [partial] Provenance on receipts (snapshotHash, query, source, stubbed): client.ts:189-197 snapshotHash sha256 over {capturedAt,markets,query,source}; tools.ts:67 module-level `let lastEvidence`, set at 131-136 by graph_query, attached at 226-229 to the next x402_fetch spend; never cleared, so evidence can be stale/unrelated; wallet-pane.tsx:183-187 shows only the first 
- [missing] Two providers behind one interface: Studio key (default) + mainnet x402 gateway paid from Privy wallet; receipt records which: Only the Studio/Bearer path exists (client.ts:144-146); no x402 client for gateway.thegraph.com; Evidence has no provider/path field (receipt.ts:28-34); grep for 'thegraph' outside packages/graph hits only environment.ts:144 default URL
- [working] Paid oracle endpoint selling the Graph snapshot (GET /oracle/snapshot): oracle-route.ts:39-46 returns 402 challenge without x-payment; 56-65 settles; 69 queries graph after settlement; 77-85 returns answer, markets, snapshotHash, source, stubbed. Live: curl https://app-production-58dd.up.railway.app/oracle/snapshot?symbol=USDC -> HTTP 402 with accepts[0].network hedera:
- [missing] Subgraph MCP SKILL.md for discovery; GRAPH.md with freshness curls: git ls-files shows only froggy-browser, froggy-leash, froggy-verification skills; no GRAPH.md anywhere; docs/ has PLAN.md, architecture.md, decisions, handover.md, prizes.md, summary.md
- [working] Dependency-graph declaration (tools/graph.ts) and whole-repo check (tools/check-graph.ts): tools/graph.ts:117-123 declares packages/graph layer adapter, mayImport [], mayUse [effect]; apps/server mayImport includes @froggy/graph (graph.ts:67) and apps/server/package.json:21 depends on it; check-graph.ts:42-129 verifies workspace/declaration parity, manifest edges, reachability/seam; not r
- [partial] Unit tests for the Graph client: 8 tests, all against stubGraphClient/snapshotHash/describeCheapestBorrow (client.test.ts:9-80); liveGraphClient has no test (no fetch mock, no decode/error-path test)
Plan gaps:
- (4h, Day 2, Sun 6) Sec 5 / Day 2: registry JSON with four pinned deployments (Aave v3 Ethereum, Aave v3 Base, Compound v3 Ethereum, Spark Lend), one Messari query run in parallel across all four and merged, cheapest with provenance: Client takes exactly one subgraphId (client.ts:127,135); env has one GRAPH_SUBGRAPH_ID; none of the four ids appear in the repo; a live run answers 'cheapest across protocols' from a single protocol's deployment
- (1.5h, Day 2, Sun 6) Sec 5 / Day 2: freshness gate — _meta block older than two hours returns 'unavailable', fail closed: No _meta in LENDING_QUERY, no block fields in the decode schema, capturedAt is fetch wall-clock (client.ts:24-39,63-70,170)
- (2.5h, Day 2, Sun 6) Sec 2 Receipts / Sec 5: deployment id and block number on every row and receipt: LendingMarket (types.ts:11-19) and Evidence (receipt.ts:28-34) have neither; only the gateway URL string carries the id; wallet pane shows a 12-char hash only (wallet-pane.tsx:185)
- (8h, Day 3, Mon 7) Sec 5 / Day 3: second provider — mainnet x402 gateway at $0.01/query paid from demo Privy wallet under rule (a); receipt records which path (x402 or Studio key): Only the Bearer Studio path exists; no x402 client for The Graph, no EIP-3009 typed-data signing hook, no provider field on Evidence
- (1h, Day 1, Sat 5) Day 1: Studio key and the four freshness curls into GRAPH.md; all four deployments' block ages known: No GRAPH.md; live deployment still graph:"stub" (no GRAPH_API_KEY/GRAPH_SUBGRAPH_ID set on Railway per /health)
- (1h, Day 2, Sun 6) Sec 5: Subgraph MCP for discovery documented in a SKILL.md: .agents/skills has no Graph skill
- (1.5h, Day 2, Sun 6) Sec 3: oracle body becomes the four-deployment brief metered per deployment (0.0125 tHBAR each, 0.05 for four): oracle-route.ts:19 flat PRICE_TINYBARS=5000000 (0.05 HBAR) for one single-deployment snapshot; no per-deployment metering
- (1h, Day 3, Mon 7) Sec 3: 'cheapest variable USDC borrow' with provenance the receipt can cite for that spend: lastEvidence (tools.ts:67) is a never-cleared closure variable; the receipt cites whatever the most recent graph_query returned, even if unrelated to the purchase or from a previous symbol
- (1.5h, Day 1, Sat 5) Live client verified (Day 1 'first real query'): liveGraphClient has no test and has never run against a real gateway in this repo; nested filter `inputToken_: { symbol }` and the rates shape are unverified against the four Messari deployments
Hygiene: Live Railway deployment answers graph:"stub" (/health, 2026-09-05 07:58 UTC) and the paid /oracle/snapshot sells fixture data with payTo 0.0.0; .env.example:34-35 itself states a stubbed Graph query qualifies for nothing — the deployed artifact is currently non-qualifying for the Graph track; Fixture markets (fixture.ts:20-48) include 'Morpho Blue USDC', which is not one of the plan's four deployments (Aave v3 x2, Compound v3, Spark); a stubbed demo answers with a protocol the live path will never return; tools.ts:67 `let lastEvidence` is module-scoped per tool set and never reset: a stale or unrelated snapshot hash can be written onto a receipt as 'the evidence it acted on', undermining the provenance claim; Evidence.source is the full gateway URL (client.ts:175); safe today because the k
Verdicts:
- [partly] packages/graph/src/client.ts:135 — liveGraphClient builds one URL from one subgraphId and has no code path that queries more than one deployment; the plan's four deployment ids appear nowhere in the r -> The single-deployment part is confirmed: LiveGraphOptions has one `subgraphId: string` (client.ts:127), one URL is built (client.ts:135), one fetch per lendingMarkets call (client.ts:139), and services.ts:52-55 passes exactly one `environment.graphSubgraphId`. There is no registry, no array of ids,  (grep -rn 'JCNWRypm|D7mapexM|AwoxEZbi|GbKdmBe4' . (excluding research_FABLE51/) hits only DAY0_CHECKLIST_FABLE51.md:42,43; PRODUCT_FABLE51.md:94; RESEARCH_FABLE51.md:129. grep subgraphId across .ts/.md)
- [partly] 1. packages/graph/src/client.ts:135 — liveGraphClient builds one URL from one subgraphId and has no code path that queries more than one deployment; the plan's four deployment ids appear nowhere in th -> Code half is correct: client.ts:133-135 builds exactly one URL `${gateway}/subgraphs/id/${options.subgraphId}`; lendingMarkets (137-183) does one fetch against it; LiveGraphOptions (122-128) takes a single `subgraphId`; services.ts:50-57 constructs one liveGraphClient from the single env var GRAPH_S (packages/graph/src/client.ts:122-128,133-135,139; apps/server/src/services.ts:50-57; apps/server/src/environment.ts:140-142; .env.example:36-38; grep output: DAY0_CHECKLIST_FABLE51.md:42-43, PRODUCT_F)
Extra: Graph code has not changed since bootstrap: `git log -- packages/graph apps/server/src/oracle-route.ts apps/server/src/tools.ts` shows only fd4bdc9 and 61ef247 (both 2026-09-04 22:0x +0200). HEAD 0b076fe (2026-09-05 07:21 +0300) touched none of these | Plan-vs-code gap for section 5 'The Graph', itemised: registry JSON (absent), four deployments (one env var), parallel query (single fetch), _meta block (absent), two-hour freshness gate (absent), deployment id + block on rows/receipts (absent; Lendi | protocolOf (client.ts:91-92) lowercases the first whitespace-delimited token of market.name. Messari market names like 'Aave V3 USDC' yield 'aave' but Compound v3 markets are typically named e.g. 'Compound V3 USDC' -> 'compound', and Spark -> 'spark' | liveGraphClient.lendingMarkets fetch (client.ts:139) has no timeout/AbortSignal; a hung gateway hangs the paid oracle response indefinitely after settlement, compounding claim 6. | MARKET_LIMIT=25 (client.ts:131) and orderBy totalBorrowBalanceUSD desc: when one deployment has more than 25 markets for a symbol, the cheapest market can be excluded before the client-side sort — 'cheapest borrow first' is then only cheapest among t | stubbed flag on the oracle response is `snapshot.stubbed || settled.stubbed` (oracle-route.ts:84) — a live Graph answer behind a stub Hedera gate is reported stubbed, and vice versa; the receipt cannot distinguish which half was fake.

### domain-protocol-ui
packages/domain (Effect Schema for ids, money, mandates, decisions, receipts; 1 test file), packages/protocol (two versioned WebSocket wire schemas plus the binary screencast frame envelope; 1 test file) and packages/ui (four shadcn/Base UI components, cn re-export, one 792-line globals.css with a dark-only oklch token set) were all written in a single bootstrap commit on 4 Sep by grmkris and have not changed since. The schemas are coherent and consistently consumed by apps/server and apps/web for the single-wallet, single-session, USD-denominated mandate model the bootstrap built, and the live URL confirms the spine (200 /health with all four modes "stub", a real 402 with payTo "0.0.0"). Against PRODUCT_FABLE51.md the contract layer is a different product: no user identity, no two-pocket (W_user + K_pocket) model, no tHBAR-denominated caps or HBAR quote source, no "user" provenance, no Base mainnet network, no Privy policy id, no Graph deployment/block or HCS fields on receipts, no directory/probe/pocket-balance messages, and the approval-card vocabulary exists on the wire but is never emitted by the server or rendered by the client. Tests exist only for TypeIDs and frame framing and could not be run (no node_modules, install forbidden).

Features:
- [working] TypeID identifiers (SessionId, RunId, TabId, MandateId, RuleId, SpendId, ReceiptId) with generate/is/fromUuid/toUuid and prefix-collision guard: id.ts:57-121 makeIdSchema; id.ts:128-152 seven registered prefixes; id.test.ts:7-51 covers generation, decoding, cross-prefix rejection, base32 alphabet, 128-bit overflow, UUID round trip, duplicate prefix. Consumed by apps/server/src/session.ts, packages/wallet, packages/database/src/schema.ts.
- [partial] Money: UsdMicros brand, usd()/usdMicros() constructors, formatUsd, Amount (decimal-string units), Asset, Network literals, Quote, parQuote, priceInUsdMicros: money.ts:18 Network = ["base-sepolia","hedera:testnet"] only; money.ts:88-107 KNOWN_ASSETS has Base Sepolia USDC, Hedera HBAR, Hedera USDC, no Base mainnet USDC; money.ts:131-134 priceInUsdMicros does Number(BigInt(units)) (precision loss > 2^53, contradicting the comment at 67-69); money.ts:137-141
- [partial] Payee provenance (mandate/server/model/page), isPayable, normalizePayeeId, EvmAddress and HederaEntityId validators: address.ts:46-51 Provenance literals lack the plan's "user" value; address.ts:57-65 Payee.id is a bare Schema.String; EvmAddress (13-21) and HederaEntityId (24-32) are exported but have zero consumers outside packages/domain (grep). isPayable/normalizePayeeId consumed by packages/wallet/src/policy.t
- [working] Mandate rules union (per_tx_cap, window_cap, payee_allowlist, host_allowlist, network_allowlist, expiry, approval_threshold), Mandate with frozen flag, defaultRules: mandate.ts:21-84 seven TaggedStructs; mandate.ts:86-97 Mandate{createdAt,frozen,id,rules,sessionId}; mandate.ts:170-204 defaultRules ($2/tx, $10/24h, ask above $1, 24h expiry). Evaluated in packages/wallet/src/policy.ts, rendered in apps/web/src/components/wallet-pane.tsx:30-57. No policyId field, n
- [working] SpendIntent with idempotencyKey, host, payee, usdMicros: mandate.ts:100-118; built in apps/server/src/tools.ts (x402_fetch provenance "server" at 201, wallet_send provenance "model" at 264) and priced in apps/server/src/session.ts:213.
- [partial] PolicyDecision union Allow/Ask/Deny with DenialCode: mandate.ts:121-160. Deny is rendered (wallet-pane.tsx:128-152 shows code and ruleId). Ask is produced by policy.ts:166-173 and rendered as "Waiting on you" (wallet-pane.tsx:139) but nothing can resolve it: apps/server/src/tools.ts:239-241 and 283-285 turn ask into a text string returned to the model
- [partial] Receipt (decision, intent, quote, evidence, settlement, stubbed), Evidence, Settlement, SpendStatus: receipt.ts:28-58; built by apps/server/src/session.ts:286-308 and rendered by wallet-pane.tsx:164-202 (amount, decision tag, purpose, evidence hash prefix, transactionId, stubbed badge; quote, runId, denial code and ruleId on the receipt are never rendered). Settlement.network is Schema.String not N
- [working] PROTOCOL_VERSION / ProtocolVersion envelope: index.ts:61-63 PROTOCOL_VERSION = 1, ProtocolVersion = Literals([1]); used as Envelope in protocol/src/app.ts:20 and browser.ts:13. The PROTOCOL_VERSION constant itself has no consumer: apps hard-code `v: 1` (apps/server/src/sockets.ts:192,197,204; apps/web/src/hooks/use-app-socket.ts:74).
- [partial] App socket client messages: approval.resolve, mandate.freeze, mandate.update, ping: app.ts:81-108. mandate.freeze handled (sockets.ts:184-194, aborts runs, broadcasts). mandate.update handled (sockets.ts:195-199 -> session.ts:158-161 keeps only frozen and sessionId, replaces all rules; no auth) but no UI sends it (grep "mandate.update" in apps/web: none). approval.resolve is echoed
- [partial] App socket server messages: session.welcome (ServiceModes), mandate.state, wallet.state (WalletSummary), receipt.appended, policy.decision, approval.request, approval.resolved, pong, protocol.error: app.ts:120-174. welcome/mandate/wallet/receipt/decision all set React state in use-app-socket.ts:86-104 and render. approval.request and approval.resolved are explicitly dropped at use-app-socket.ts:106-115 ("not state the pane renders yet"); the server never constructs an ApprovalRequest (grep Appr
- [stubbed] ApprovalRequest / ApprovalOption / ApprovalKind (deny_stop, deny, allow_session, allow_once) / APPROVAL_KIND_ORDER: app.ts:48-79 defined and exported from index.ts:2-5; zero consumers outside packages/protocol (grep). Schema-only, nothing produces or renders one.
- [working] WalletSummary (address, balanceLabel, signerAddress, windowSpentUsdMicros): app.ts:110-118; produced by session.ts:168-180, rendered by wallet-pane.tsx:93,119. Single wallet only: no second pocket, no per-cap remaining, windowSpentUsdMicros is Schema.Int not UsdMicros.
- [working] Browser socket client messages (input.mouse/key/text, browser.start/navigate/activate-tab/close-tab/take, ping) and server messages (browser.state, pong): browser.ts:76-119 and 149-161; every type has a producer in apps/web (input-capture.ts, browser-pane.tsx, use-browser-socket.ts) and a consumer in packages/browser/src/session.ts and apps/server/src/sockets.ts (grep table). BrowserStatus has no frozen state and BrowserState.error is only for crashed
- [working] Screencast frame envelope encode/decode ([u32 LE metaLen][JSON meta][JPEG]): frames.ts:38-90; frames.test.ts:9-65 covers round trip, zero-copy view, large payload, truncated header, truncated meta, non-JSON meta, non-number dimensions. Used by packages/browser/src/screencast.ts and apps/web/src/hooks/use-browser-socket.ts. FrameMeta uses Schema.Finite so negative or zero w/h
- [working] UI components Badge, Button, Separator, Tooltip and cn re-export: badge.tsx:30-50, button.tsx:43-56, separator.tsx:4-20, tooltip.tsx:6-63; consumed by apps/web (main.tsx:2, wallet-pane.tsx:17-19, app-shell.tsx:9-10, browser-pane.tsx:16-17, chat-pane.tsx:11). Lockfile pins @base-ui/react 1.8.0, react 19.2.8, tailwindcss 4.3.3, cva 0.7.1, cn 0.2.5 (verified in bun.l
- [partial] Design tokens and fonts (globals.css: Bricolage Grotesque + IBM Plex Mono, dark-only oklch palette, radius, custom data-* variants): globals.css:652-676 @theme tokens, 678-708 :root with color-scheme: dark and no .dark/light block (grep "^.dark" none); 700-708 eight --scene-* tokens have no consumer in apps/web/src (grep "scene-" none); 6-30 accordion keyframes reference --radix-accordion-content-height in a Base UI project; wall
- [missing] Plan §2 wallet pane surfaces: policy card with Privy policy id, pocket balance vs caps, seller directory with paid-call counters, paste-a-402-URL probe box: No message type or schema for directory, probe, pocket balance or policy id anywhere in app.ts:81-174 or domain; WalletSummary (app.ts:110-118) is the only wallet state on the wire.
- [missing] Plan §2 blocked/denial card with share button and Privy raw error text: Deny (mandate.ts:139-145) carries code/message/ruleId only; wallet-pane.tsx:128-152 shows text, no share action; chat-pane.tsx renders no cards (no @froggy/domain import, grep).
Plan gaps:
- (2h, Day 3, Mon 7 (wallet_send wired to a raw Privy denial)) §4: user-typed wallet_send address carries provenance "user", passes the host gate and is denied by Privy with the policy id; page/model provenance refused before Privy: Provenance (address.ts:46-51) has no "user" literal and TRUSTED_PROVENANCE (54) is [mandate, server]; wallet_send hard-codes "model" (apps/server/src/tools.ts:264). Deny (mandate.ts:139-145) has no privyPolicyId/rawError fields for the denial card.
- (4h, Day 2, Sun 6 (host caps)) §4 K_pocket: 0.5 tHBAR per call, 2 tHBAR per rolling 24h, tHBAR-denominated; §5 price 0.0125 tHBAR per deployment: All caps are UsdMicros (money.ts:27, mandate.ts:21-37) and the only quote is parQuote (stablecoin par, money.ts:137-141); no HBAR quote source, no per-asset/native-unit cap rule. Need either asset-denominated cap rules or an HBAR Quote provider recorded on the receipt.
- (4h, Day 2, Sun 6 (per-user W_user and K_pocket, users table)) §4 two pockets (W_user Privy embedded wallet + K_pocket Hedera account) per user; §2 guest vs email identities with TTL: No UserId TypeID (id.ts:128-152 has session/run/tab/mandate/rule/spend/receipt only); Mandate keyed by sessionId (mandate.ts:95); WalletSummary (app.ts:110-118) is one address + one balance label; no envelope auth or identity on either socket.
- (1h, Day 3, Mon 7 (Graph mainnet x402 from the demo wallet)) §4 rule (a) chainId 8453 Base mainnet USDC to The Graph's payTo; §5 Graph mainnet x402 gateway: Network literals (money.ts:18) are base-sepolia and hedera:testnet only; KNOWN_ASSETS (88-107) has no Base mainnet USDC; NetworkAllowlist can therefore not express mainnet.
- (3h, Day 2-3 (Graph registry, HCS message per settlement)) §2 Receipts: Graph deployment ids and block numbers, which Graph path (x402 or Studio key), Privy policy id and rule, Hedera tx id and HashScan link, HCS sequence number, ledger line: Evidence (receipt.ts:28-34) is query/snapshotHash/source/stubbed; Settlement (37-41) is network/transactionId; no deploymentIds, blockNumbers, graphPath, policyId, hcsSequence, explorerUrl, or window-total fields. wallet-pane.tsx:164-202 renders none of these.
- (6h, Day 3, Mon 7 (402 probe, server-issued directory)) §2 Wallet pane: policy card with Privy policy id, pocket balance and today's spend against the caps, seller directory with paid-calls counter, paste-a-402-URL probe box, Freeze: No protocol messages for directory, probe request/result, pocket balance or policy id (app.ts:81-174); WalletSummary has one windowSpentUsdMicros with no per-rule remaining. Mandate has no policyId field.
- (0.5h, Day 2, Sun 6) §4 idempotency state machine (reserved, signed, settled) keyed on client-minted payment id: SpendStatus (receipt.ts:61-66) is reserved/settled/failed/refused, no "signed"; SpendIntent.idempotencyKey exists (mandate.ts:112).
- (1h, Day 2, Sun 6 (freeze end to end)) §4 freeze(user, reason) with five steps; unfreeze is a human action that re-provisions: Protocol mandate.freeze (app.ts:92-96) is a boolean toggle with no reason and no user; Mandate.frozen (mandate.ts:92) is the only state. Server side is out of this area but the wire shape needs a reason and a one-way freeze/explicit unfreeze message.
- (4h, Not scheduled in §10 (handover next-steps item 2)) §2 approval semantics: threshold ask parks the turn; plan says no approval cards in v1 (Telegram) and Privy Intents only in the Wednesday box; handover item 2 says port parkInteraction: ApprovalRequest/ApprovalKind (app.ts:48-79) never emitted or rendered; approval.resolve just echoes approval.resolved (sockets.ts:200-207); Ask decision degrades to a text tool result (tools.ts:239-241, 283-285). Decide: either delete the vocabulary and drop approval_threshold from defaultRules, or 
- (2h, Day 2, Sun 6 (allowlists on every navigation path)) §2 Shared Chrome pane: one tab, grey when frozen, blue when grabbed, origin allowlist with denial on every navigation path, phone door read-only screencast: BrowserState (browser.ts:138-147) is multi-tab with activate/close-tab messages; no frozen status; BrowserState.error only for crashed/unavailable, so a denied navigation has no wire shape; no read-only viewer role.
- (3h, Day 3-4 (pre-typed buttons, public receipts page)) §2 chat pane blocked card with share button; two pre-typed buttons: chat-pane.tsx imports nothing from @froggy/domain and renders no decision cards; no share-payload schema (public receipts page / blocked board) in domain or protocol.
Hygiene: Prior-project references in source: packages/protocol/src/app.ts:43 "taken from invok"; packages/domain/src/id.ts:3 and :82 use a `rom_` example prefix that no registered id here uses (looks ported from another codebase). docs/handover.md also names humanhook, invok and boter.; packages/ui/src/styles/globals.css:700-708 defines eight --scene-* tokens (void, key-light, grid, local/remote emissive) that nothing in apps/web references, and lines 6-30 keep Radix accordion keyframes (--radix-accordion-content-height) in a Base UI project; both look carried over from another product.; mandate.update (protocol app.ts:97-101, handled at apps/server/src/sockets.ts:195-199 -> session.ts:158-161) lets any connected app-socket client replace every rule (clear the payee allowlist, raise caps) with no a
Verdicts:
- [partly] 1. ApprovalRequest is never constructed; use-app-socket discards approval.request/approval.resolved; a threshold `ask` can never be answered by a human in the running product -> Substance holds: no code path constructs an ApprovalRequest, the pane only shows the `ask` question as text (wallet-pane.tsx:128-150) and the server's `approval.resolve` handler is an echo (sockets.ts:200-207), so a human cannot resolve an `ask`. The file:line citation is wrong: ApprovalRequest live (packages/protocol/src/app.ts is 182 lines; ApprovalRequest is defined at app.ts:71-79 and the approval.request server message at app.ts:152-156 (claimed lines 235-243 do not exist). `grep -rn 'approva)
- [partly] 2. `mandate.update` over wss://app-production-58dd.up.railway.app/ws/app from any browser replaces every rule while keeping frozen and sessionId; no auth check between decode and updateMandate -> A browser page on a foreign origin is refused at upgrade (403, verified live), so 'from any browser' overstates it. What is true: any non-browser client (no Origin header) or anyone who simply opens the app's own origin can connect with no authentication and send mandate.update; sockets.ts:195-199 p (apps/server/src/sockets.ts:168-178 decode, :195-199 `deps.session.updateMandate(message.mandate)` with no auth between; apps/server/src/session.ts:158-161 `this.mandate = { ...next, frozen: this.manda)
- [partly] 4. priceInUsdMicros({units:'9007199254740993', asset:{decimals:0}}, parQuote(0)) returns the value for 9007199254740992 because Number(BigInt(units)) rounds above 2^53, contradicting money.ts:67-69 -> The precision loss is real (Number(BigInt(units)) at money.ts:132 discards the bigint before any arithmetic, contradicting the comment at money.ts:67-69), but for the exact input claimed the function throws a Schema Int (isSafeInteger) error from usdMicros(), it does not return the value for 9007199 (packages/domain/src/money.ts:131-134 `const whole = Number(BigInt(amount.units)) / 10 ** amount.asset.decimals; return usdMicros(Math.ceil(whole * quote.usdMicrosPerUnit));` and money.ts:30-31 usdMicr)
- [partly] 1. packages/protocol/src/app.ts:235-243 ApprovalRequest is never constructed anywhere; use-app-socket.ts:110-115 discards approval.request/approval.resolved, so a threshold `ask` can never be answered -> Substance confirmed, line reference wrong. packages/protocol/src/app.ts is 182 lines; ApprovalRequest is defined at app.ts:71-79 and the `approval.request` server message at app.ts:152-156. Nothing constructs either: grep over apps/server, packages/wallet, packages/protocol/src, apps/web/src for `ap (packages/protocol/src/app.ts:71-79,152-161 (wc -l = 182); apps/server/src/sockets.ts:200-206; apps/server/src/tools.ts:239-241,283-285; packages/wallet/src/policy.ts:166-173; packages/domain/src/manda)
Extra: Tests could not be executed: no node_modules at repo root, packages/domain, packages/wallet or apps/server (bun install not run in this checkout); all verdicts on money.ts rely on a standalone `bun -e` reproduction plus the effect@4.0.0-rc.112 source | Live server state (probe 2026-09-05 via wss://app-production-58dd.up.railway.app/ws/app, ping only): session.welcome modes = {graph:'stub', hedera:'stub', model:'stub', privy:'stub'}; wallet.state address=null, signerAddress=null, balanceLabel 'balan | apps/server/src/sockets.ts:52-62 isTrustedOrigin returns true for a missing Origin header by design (comment: 'A missing Origin is a non-browser client'), so any script can connect to /ws/app and /ws/browser on production without credentials; the bro | apps/server/src/sockets.ts:200-207: the server's only handling of client `approval.resolve` is to broadcast `approval.resolved` with the requestId; there is no parked request registry, no deadline, no effect on any run — the three-way race described  | Blocky402 live: https://api.testnet.blocky402.com/supported returns hedera:testnet with extra.feePayer 0.0.7162784 (x402Version 2, scheme exact) alongside eip155:80002 and solana; matches PRODUCT_FABLE51.md §4 'fee payer read from /supported' but the | README.md:16/handover.md:16 claim 'The leash | per-tx cap, rolling cap, allowlists, provenance, expiry, freeze' — consistent with packages/wallet/src/policy.ts:72-178; but README.md:100 'The approval channel is not a tool' describes an absence, and t

### database-deploy-ci
packages/database is a complete but unwired Drizzle/Postgres seam: a three-table schema (spends, receipts, mandates) with a typeid column helper and a unit test, an Effect `Database` service exposing raw `sql` + `health`, and a `migrate.ts` in apps/server that points at a `packages/database/drizzle` migrations folder that does not exist. No server code imports `@froggy/database` (tools/graph.ts:145-149 declares it as a seam; apps/server uses `memoryLedger()` at services.ts:96); the server reads `DATABASE_URL` into `environment.databaseUrl` and never uses it. Deploy is a single Railway service from a root Dockerfile (Bun 1.4.0 + apt chromium, no Xvfb), one replica, a `/data` browser-profile volume, and a Postgres resource with a 50 GB volume, but no `preDeployCommand` to run migrations and `DATABASE_URL` is `preserve()` rather than linked to the Postgres resource. CI is one GitHub Actions job (`bun run check` -> build -> Playwright e2e with 3 smoke tests); no Docker build gate, no live-URL check, no DB service. The live URL answers `/health` with all four modes `stub` and a well-formed Hedera x402 402 with `payTo: "0.0.0"`. The biggest hygiene problem: 209 files of a real Chrome profile (`apps/server/.froggy/chrome-profile`, including `Cookies`, `Login Data`, `History`, `Local State`) are tracked in git since commit fd4bdc9, are not gitignored, are not excluded by `.dockerignore` (its `.froggy` pattern is root-anchored), and the working tree is already dirty from a local Chrome run.

Features:
- [stubbed] Drizzle schema: spends / receipts / mandates tables: schema.ts:30-88 defines three pgTable()s with unique index spends_session_idempotency (49-52); no runtime code imports them — `grep -rn '@froggy/database' apps packages` returns nothing; only tools/graph.ts:140 and tools/spikes/verify-unused-surface.ts:49 reference the package
- [working] TypeID uuid column helper (typeIdColumn / typeIdPrimaryKey): columns.ts:15-42 customType over native `uuid` with fromDriver/toDriver via IdSchema; covered by columns.test.ts:13-45 (4 cases: sql type, default generator, round-trip, wrong-prefix rejection)
- [stubbed] Effect `Database` service (postgres client, health): index.ts:16-52 Layer reads Config.redacted('DATABASE_URL'), builds postgres pool (max 10, idle 20), exposes `sql` and `health` (`select 1`); no drizzle `db` instance is constructed and nothing consumes the service
- [missing] Spend ledger persisted in Postgres: services.ts:92-96 `ledger: memoryLedger()` with comment that the Postgres version is a future swap; packages/wallet/src/ledger.ts:50-56 says in-memory is only correct for one process; .railway/railway.ts:88-92 pins replicas to 1 because of it
- [missing] Receipts / mandates persisted: schema.ts:57-88 tables exist; no writer anywhere (grep 'receipts'/'mandates' outside packages/database finds no Drizzle usage); handover.md:51 lists 'Persist the ledger' as next work
- [broken] Migrations (drizzle-kit generate + migrator): migrate.ts:22 `migrationsFolder: "packages/database/drizzle"`; `ls packages/database/drizzle` -> No such file or directory; drizzle.config.ts:10 `out: "./drizzle"` but `bun run db:generate` has never been run (no tracked SQL); running `bun run db:migrate` with DATABASE_URL set would fail on a missin
- [stubbed] Server DATABASE_URL config: environment.ts:126-129 loads DATABASE_URL with default postgres://postgres:postgres@localhost:5432/froggy and returns it at :203; `grep -rn databaseUrl apps packages` shows no consumer besides environment.ts; note drizzle.config.ts:7 defaults to db name `agent_native`, environment.ts:128 to `froggy`
- [working] Dockerfile (single image: SPA build + Bun server + Chromium): Dockerfile:22-52 builder installs with --frozen-lockfile and `bun run --filter=@froggy/web build`; :55-95 runtime apt-installs chromium + fonts, FROGGY_CHROME=/usr/bin/chromium (:72), CHROME_PROFILE_DIR=/data/chrome-profile (:86), STATIC_DIR=/app/apps/web/dist (:87), HEALTHCHECK on /health (:92-93),
- [partial] Dockerfile build-arg for VITE_PRIVY_APP_ID: Dockerfile:49-50 declares ARG/ENV VITE_PRIVY_APP_ID=""; .railway/railway.ts:70-83 env block does not list VITE_PRIVY_APP_ID, so the SPA is built with an empty Privy app id unless Railway injects it as a build arg out of band; handover.md:34 confirms Privy is deliberately not set on Railway
- [partial] Railway IaC: app service, Postgres, two volumes, checkSuites gating: railway.ts:31 github(...,{checkSuites:true}); :37 postgres('Postgres'); :41-45 postgres-volume 50 GB; :54 browser-profile 2 GB mounted at /data (:94); :92 replicas 1; :84-87 healthcheck /health timeout 120. Gaps: DATABASE_URL is `preserve()` at :73 (not a reference to `db`), no preDeployCommand for 
- [working] Chrome profile persistence via Railway volume: railway.ts:54 volume('browser-profile',{sizeMB:2000}) and :94 volumeMounts {'/data': browserVolume}; Dockerfile:86 CHROME_PROFILE_DIR=/data/chrome-profile; handover.md:5 says the browser socket brings Chromium up live (not re-verified here beyond /health)
- [working] CI: deterministic gates (format, type-aware lint, typecheck, graph, agents, tests, knip): ci.yml:36-39 `bun install --frozen-lockfile` then `bun run check`; package.json:17 defines check as format:check && lint:types && typecheck && graph && agents:check && test && knip; runs on pull_request and push to main (ci.yml:3-9), concurrency cancel-in-progress (:14-16)
- [working] CI: build + Playwright e2e: ci.yml:41-48 `bun run build`, `bunx playwright install --with-deps chromium`, `bun run e2e`; playwright.config.ts:23-44 boots server on 3101 and Vite on 3100 with reuseExistingServer=false in CI, retries 2, github reporter
- [missing] CI: Docker image build gate: ci.yml:18-48 contains a single `typescript` job; no `docker build` step, so the Dockerfile is only exercised by Railway's deploy after CI passes
- [missing] CI: database tests against a real Postgres: no `services:` block and no DATABASE_URL in ci.yml; the only DB test (columns.test.ts) is pure and needs no connection
- [working] e2e smoke: workspace boots with three panes and no console errors: runtime.spec.ts:3-25 asserts 'Froggy' text, Message/Address textboxes, Wallet heading, a `canvas`, and zero console/page errors; does NOT assert a screencast frame arrives
- [partial] e2e smoke: mandate visible + Freeze button present: runtime.spec.ts:27-36 checks /per transaction/ text and a Freeze button are visible; never clicks Freeze, so freeze-aborts-run is untested end to end
- [working] e2e smoke: /oracle/snapshot answers a Hedera x402 402: runtime.spec.ts:38-58 asserts status 402, x402Version 2, accepts[0].network 'hedera:testnet', scheme 'exact'; live URL matches: HTTP 402 with the same shape (payTo '0.0.0', amount '5000000' — stub mode)
- [missing] e2e: paid round trip, chat run, grab, receipts: only three tests exist in e2e/; no test posts a chat message, pays the 402, grabs the page, or reads a receipt
- [working] Git hooks (lefthook): lefthook.yml:1-16 pre-commit runs oxfmt on staged files, `bun run lint`, `bun run graph`; pre-push runs `bun run check`; installation is manual via `bun run hooks:install` (package.json:28)
- [working] Architecture graph check + oxlint boundary plugin: check-graph.ts:42-129 verifies every workspace is declared, mayImport matches package.json deps, and unreachable packages declare a seam; graph.ts:139-150 registers packages/database with `seam` (unreachable from apps); boundaries.ts enforces per-file imports
- [working] Agent skill file check: check-agent-files.ts:1-53 validates .agents/skills/*/SKILL.md frontmatter name/description and rejects TODOs; wired as `agents:check` in package.json:27
- [working] knip unused-surface gate: knip.json:3-4 ignores tools/spikes/** and .railway/railway.ts and the `railway` dep; part of `bun run check`
- [partial] Spike scripts (frontend toolchain, unused surface): verify-frontend-toolchain.ts:1-16 writes to hard-coded /tmp/field01-toolchain and spawns three servers; verify-unused-surface.ts scans imports; both knip-ignored, never run in CI, tracked as exploratory only
- [broken] Tracked local Chrome profile (apps/server/.froggy): `git ls-files apps/server/.froggy | wc -l` = 209 files, 2.8 MB, added in commit fd4bdc9 (Fri 4 Sep 22:14 +0200); includes Default/Cookies (20 KB), Default/Login Data (40 KB), Default/History (160 KB), Local State, Preferences, Secure Preferences; .gitignore has no `.froggy` entry (only `test-results
Plan gaps:
- (0.5h, Day 1, Sat 5) Day 1 hygiene commit: `git ls-files | grep -iE 'chrome-profile|Cookies|Login Data'` prints nothing (DAY0 section E; PRODUCT s10 Day 1 A-column): 209 Chrome profile files including Cookies, Login Data, History, Local State are tracked (apps/server/.froggy/chrome-profile/**); .gitignore lacks `.froggy`; .dockerignore's `.froggy` pattern is root-anchored so the profile also ships in the image; working tree already dirty from local Chrome
- (8h, Day 2, Sun 6) Day 2: users table, nonces, ledger in the database; 'paid twice with the same payment id, charged once' green against the DB: schema.ts:4 deliberately has no users table; no nonces/pocket/guest tables; no migrations generated (packages/database/drizzle missing, migrate.ts:22 points at it); Database service unwired (graph.ts:145 seam); server still uses memoryLedger() (services.ts:96)
- (3h, Day 2, Sun 6) Day 2: fresh profile per session, wiped at session end, no imported cookies (PRODUCT s2 pane 1); Day 6 restart drill 'profiles ephemeral, ledger and pockets survive': Deploy does the inverse: Dockerfile:86 + railway.ts:54,94 persist one shared profile on a 2 GB volume and the ledger is in-memory; README.md:112 and handover.md:65 describe persistence as the product
- (4h, Day 2, Sun 6) Day 2: idempotency state machine (reserved, signed, settled) keyed on client-minted payment id stored with (session, URL, amount); replay returns cached body, mismatch 409, facilitator timeout reconciles via mirror node (PRODUCT s4 K_pocket): SpendStatus (receipt.ts:61-66) is reserved|settled|failed|refused with no `signed`; spends table (schema.ts:30-55) has no URL, amount-check, cached-body or payment-id columns; no reconciliation path
- (1h, Day 2, Sun 6) Migrations as a Railway pre-deploy step (migrate.ts:2-7 describes itself as a `preDeployCommand`): .railway/railway.ts app service has no preDeployCommand and DATABASE_URL is preserve() (railway.ts:73) instead of a reference to the `db` resource; postgres-volume (railway.ts:41-45) is declared but not visibly attached to the db service
- (6h, Day 3, Mon 7) Day 3: guest path (pooled pre-provisioned identities, IP cap 3/day, 30+10 slots, 48h TTL) and one-click delete-my-data (profile, receipts, rows): no guest/identity/pocket tables in schema, no TTL or IP-cap storage; nothing to delete because nothing is persisted
- (2h, Day 3, Mon 7) Day 3: HCS message per settlement, receipts carry Hedera tx id, HashScan link, HCS sequence number, Graph deployment ids and block numbers: receipts table stores an opaque jsonb `document` (schema.ts:70) plus `stubbed`; no columns or indexes for tx id / HCS sequence / deployment id, and no writer exists
- (1h, Day 1 / Day 7) Day 1: measurement of the live URL committed; Day 7-8: hourly curl alarm on the live URL and the 402: no scheduled workflow or cron in .github/workflows (only ci.yml on push/PR); no post-deploy smoke against the Railway URL
- (4h, Day 5, Wed 9) Day 5: freeze-aborts-mid-step test; Day 5 ten consecutive four-minute takes without a dropped socket: e2e only asserts the Freeze button is visible (runtime.spec.ts:35); no e2e drives a chat run, pays the 402, or exercises freeze/grab/socket stability
- (0.5h, Day 1, Sat 5) DAY0 section E: Bun 1.4.1 everywhere it is pinned: repo pins 1.4.0 in package.json:52 (packageManager/engines), Dockerfile:22,55, ci.yml:31; local bun is 1.3.14
- (1h, Day 2, Sun 6) CI validates what deploys (checkSuites:true gates the Railway deploy on this workflow): ci.yml never builds the Dockerfile, so a broken image passes CI and fails only at Railway; also no CI Postgres service for a future DB-backed ledger test
Hygiene: Tracked browser profile with credential stores: apps/server/.froggy/chrome-profile/Default/{Cookies,Login Data,Login Data For Account,History,Web Data,Local State,Preferences,Secure Preferences,Sessions/*} — 209 files, 2.8 MB, committed in fd4bdc9; directly fails DAY0_CHECKLIST_FABLE51.md section E check 1. Fix must be a normal commit (untrack + .gitignore `.froggy/`) plus a README disclosure line; the checklist forbids history rewrite.; Working tree churn: `git status --porcelain apps/server/.froggy` already shows History, History-shm, History-wal modified — every local `bun dev` run dirties tracked files and risks committing fresh browsing/cookie state.; Docker context leak: .dockerignore:15 lists `.froggy` (root-anchored under Go filepath.Match semantics) but the profile lives at apps/s
Verdicts:
- [partly] `DATABASE_URL=... bun run db:migrate` fails with a missing-folder error because apps/server/src/migrate.ts:22 references packages/database/drizzle, which does not exist, and no .sql migration is track -> The folder is missing and no migration SQL is tracked (confirmed by inspection), but the run was not reproducible here and the failure mode is not a raw missing-folder error: drizzle-orm's migrator reads `<migrationsFolder>/meta/_journal.json` and throws `Can't find meta/_journal.json file` when the (apps/server/src/migrate.ts:22 `await migrate(drizzle(sql), { migrationsFolder: "packages/database/drizzle" });`. `ls packages/database/drizzle` => No such file or directory. `git ls-files packages/dat)
Extra: No node_modules anywhere in the checkout, so no `bun test` could be run in any package; all code claims verified by reading only. | CHROME_PROFILE_DIR defaults to `.froggy/chrome-profile` (apps/server/src/environment.ts:123-125), relative to the server cwd — this is how a real local Chrome profile (Cookies, Login Data, ~4.08 MB, 209 files) ended up committed in fd4bdc9. In the im | Railway config provisions a Postgres service + 50 GB volume and passes DATABASE_URL (.railway/railway.ts:37-45,73) that nothing in the running code reads; migrate.ts is the only consumer and it is not wired as a preDeployCommand and has no migrations | apps/server/package.json depends directly on drizzle-orm 0.45.2 and postgres 3.4.9 solely for migrate.ts, while packages/database (the package that owns the schema) is not a dependency of any app. | Plan vs code: PRODUCT_FABLE51.md §4 requires payTo to be a real 0.0.x account with aliases rejected, an HCS topic, fee payer read from Blocky402 /supported at boot, and a users table + persisted ledger by Day 2; the live deployment has payTo 0.0.0, a | docs/handover.md:5 says 'Verified live: ... a real 402 and its paid round trip'; with hedera mode `stub` on the live /health, that round trip was against the stub payer/facilitator (packages/payments/src/payer.ts:128 STUB_ACCOUNT_ID), not Blocky402.

### docs-skills
The docs layer is three generations deep and they disagree: docs/summary.md (Telegram-first Mini App), docs/PLAN.md + docs/architecture.md (round 2: web workspace, "steal from ~/code/humanhook, invok, boter, harness, starters", persistent profile, Privy-as-leash-on-Hedera), and docs/handover.md + README.md + AGENTS.md + three .agents/skills (written 4 Sep against the Froggy bootstrap). PRODUCT_FABLE51.md §14 explicitly reverses several of the round-2 and handover decisions (custody split by chain, raw-sign Hedera wrapper struck, persistent profile cut, Telegram as pager, single subgraph id replaced by a four-deployment registry, users table required, no approval cards in v1). The handover's "what works" table is mostly accurate for the local spine but overstates the live deployment: https://app-production-58dd.up.railway.app/health reports all four modes `stub`, the live 402 carries `payTo: "0.0.0"` and `extra: {}` (no Blocky402 feePayer), and the "paid round trip" is a stub settlement id. The skills are consistent with the code they describe (pure authorize, receipt-on-refusal, no approval tools, PAGE_CONTENT_FENCE) but describe the pre-FABLE51 single-leash model. Hygiene is the largest risk: a 209-file Chrome profile (Cookies, Login Data, History) is tracked and not gitignored, nine source/doc files carry "ported from invok/harness/humanhook" headers, docs/decisions/0001-0002 describe a different starter project (simulation, game-core, `rom_` ids), and docs/handover.md names a prior project's Privy app as the one on the box. No tests were run: node_modules is absent at the root and in every package.

Features:
- [working] AGENTS.md operating contract (stubs selected only in environment.ts, browser/wallet boundary, TypeIDs, run owned by server): apps/server/src/environment.ts:30-38 PLACEHOLDER table and modeOf(); tools/graph.ts:100-117 browser mayImport excludes @froggy/wallet and wallet excludes browser; packages/domain/src/id.ts exists; apps/server/src/chat.ts:82 abortSignal: run.signal. AGENTS.md:75 'Testnets only' conflicts with PRODUCT
- [working] Repository skills froggy-browser / froggy-leash / froggy-verification (validated by `bun run agents:check`): tools/check-agent-files.ts:1-56 validates SKILL.md frontmatter name/description/TODO only (openai.yaml not checked). Claims match code: policy.ts:72-178 pure authorize with frozen->provenance->caps->threshold order; session.ts:226-236 receipt written on refusal; tools.ts:64-324 has no raise_limit/un
- [working] README 'Run it' / 'Verify it' instructions: package.json scripts dev/check/e2e/e2e:install at lines 10-32 match README; .env.example placeholders match environment.ts:30-38. Cannot be executed here: no node_modules anywhere (find . -maxdepth 3 -name node_modules returned nothing).
- [working] README 'stub is loud' claim (wallet pane chip, receipt stubbed:true): apps/web/src/components/wallet-pane.tsx:193-198 renders 'stubbed' badge; apps/web/src/components/app-shell.tsx:35-53 identity.stubbed chip; packages/domain/src/receipt.ts:33,56 stubbed: Schema.Boolean; session.ts:308 receipt stubbed if evidence or settlement stubbed; live /health returns {graph,hede
- [stubbed] handover: 'real 402 and its paid round trip verified live': docs/handover.md:5-6 claims it; curl https://app-production-58dd.up.railway.app/oracle/snapshot -> 402 with payTo '0.0.0', extra {} (packages/payments/src/oracle.ts:60 `extra: {}`, :212 STUB_PAY_TO='0.0.0'); settlement in stub mode returns `stub-not-a-real-hedera-transaction-<ts>` (oracle.ts:234). B
- [stubbed] handover: 'approval cards: protocol carries ApprovalRequest, nothing renders it yet': packages/protocol/src/app.ts:71-79,152-156 define ApprovalRequest/approval.request; grep shows no server code constructs an approval.request message; apps/server/src/sockets.ts:200-207 approval.resolve only re-broadcasts approval.resolved; apps/web/src/hooks/use-app-socket.ts:110-111 ignores approva
- [partial] handover: 'mandate.update honoured by server; pane only reads': apps/server/src/sockets.ts:195-198 handles mandate.update; apps/server/src/session.ts:158-161 preserves frozen and sessionId; apps/web/src/components/wallet-pane.tsx has only the Freeze/unfreeze button at :89 and no editor.
- [stubbed] handover: 'persist the ledger; packages/database holds the schema': packages/database/src/schema.ts:30-88 spends/receipts/mandates tables exist; packages/wallet/src/ledger.ts:62 memoryLedger in use (apps/server/src/services.ts:96); tools/graph.ts:157-161 marks database an unreachable seam; apps/server/src/migrate.ts:23 targets packages/database/drizzle which is not 
- [missing] handover: 'Privy-signed Hedera payment (wallets._rawSign secp256k1)': packages/payments/src/payer.ts:70-74 signs with PrivateKey.fromStringECDSA(env key); no _rawSign call anywhere. PRODUCT_FABLE51.md:120,203 strikes this item ('zero policy coverage; it would make the video sentence false').
- [missing] handover/architecture: Telegram onboarding or pager: grep -rniE 'grammy|telegram' apps packages returns nothing; docs/handover.md:54 admits ~/code/boter has no pairing flow; PRODUCT_FABLE51.md:39 requires a grammY pager bot with /start <code>, Freeze button, digest.
- [working] Freeze aborts the run first, then takes the page (README rule): apps/server/src/sockets.ts:146-151 browser.take -> runs.abortAll() before handleClientMessage; :184-193 mandate.freeze -> setFrozen then abortAll; policy.ts:78-83 frozen checked first. Plan's freeze (PRODUCT_FABLE51.md:85: remove signer, wipe pocket key, Telegram message) is not implemented.
- [working] Payee provenance gate (README rule 'payees have provenance'): packages/domain/src/address.ts:46-57 Provenance mandate|server|model|page, TRUSTED = mandate,server; policy.ts:87-92 denies untrusted_provenance; tools.ts:264 wallet_send always provenance 'model'. No 'user' provenance exists, which PRODUCT_FABLE51.md:67 requires so a user-typed address reaches Priv
- [broken] docs/decisions/0001 and 0002: 0001:5 'separate packages for domain, protocol, simulation, rendering, UI, chain, and database'; 0002:5 example id `rom_…`, :13 'packages/game-core may not import the domain, so createSimulation<Id>()'. No packages/game-core, simulation or rendering exist (ls packages: browser database domain graph 
- [missing] docs/decisions/0003: git ls-files docs/decisions lists 0001,0002,0004,0005; git log --all -- 'docs/decisions/0003*' is empty; 0004 and 0005 are numbered as if 0003 existed.
- [broken] docs/PLAN.md link to ARCHITECTURE.md: docs/PLAN.md:3,40,215,427 link `ARCHITECTURE.md`; the tracked file is docs/architecture.md (git ls-files | grep -i architecture). Resolves on macOS only; 404 on GitHub/Linux.
- [broken] docs/summary.md (round-1 meeting notes): Says 'Telegram-first', 'Telegram Mini App', 'copy-trade', 'streaming ads'; superseded by docs/PLAN.md:7-18 and PRODUCT_FABLE51.md:137 (cut list). No superseded banner.
- [working] docs/prizes.md (scraped ETHGlobal prize page): 372 lines of raw prize-page text for all 11 sponsors; Hedera AI & Agentic Payments requires a hosted live x402 service on Hedera (line ~99-105); Graph tracks disqualify mocked data; used as the source for PRIZE_AUDIT_FABLE51.md.
- [broken] README 'Known limits: Bun.WebView wants a real window, so a container needs a display': README.md Known limits bullet 2 vs Dockerfile:15-19 ('It needs no display server… DISPLAY unset and no Xvfb') and docs/handover.md:19 ('with no display server and no Xvfb'). The README sentence is stale.
- [missing] x402 service card /.well-known/x402.json and Blocky402 feePayer read at boot (PRODUCT_FABLE51.md:71,73): router.ts routes at :91,:115,:136,:140,:153,:161 only (/api/chat, /api/chat/stop, /api/receipts, /api/wallet, /health, oracle path); live GET /.well-known/x402.json returns the SPA index.html (HTTP 200 text/html); oracle.ts:60 hardcodes extra: {}.
- [missing] HCS receipt per settlement (PLAN.md:285 stretch; PRODUCT_FABLE51.md:93 required): grep -rniE 'hcs|TopicMessage' apps packages returns nothing.
- [partial] Graph client as four-deployment Messari registry with freshness gate (PRODUCT_FABLE51.md:94): client.ts:126-135 one `subgraphId` per client; environment.ts:32 single GRAPH_SUBGRAPH_ID; LENDING_QUERY :24-39 is Messari-standardized but has no `_meta { block }` (grep '_meta' empty), so no freshness gate; no deployment ids on receipts (Evidence has source URL + snapshotHash only, tools.ts:133-13
- [partial] Model and runtime pins per DAY0 checklist (claude-opus-5, Bun 1.4.1): model.ts:29 ANTHROPIC_MODEL = 'claude-sonnet-5'; package.json:58-60 engines bun >=1.4.0, packageManager bun@1.4.0; Dockerfile:22,55 oven/bun:1.4.0; ci.yml:29 bun-version 1.4.0. DAY0_CHECKLIST_FABLE51.md:72 asks for 1.4.1 and claude-opus-5.
Plan gaps:
- (3h, Day 1, Sat 5) DAY0_CHECKLIST §E / PRODUCT §11 'Start Fresh': untrack browser profile, disclose 'ported from' headers or rewrite, AI-USE.md: apps/server/.froggy/chrome-profile (209 files incl. Cookies, Login Data, History) is tracked since fd4bdc9 and not in .gitignore; 9 files carry 'Ported from invok/harness' or 'lifted from humanhook' headers (packages/browser/src/input.ts:6, arbitration.ts:11, chrome-detect.ts:9, apps/server/src/runs
- (4h, Day 1, Sat 5) PRODUCT §14 / README_FABLE51 'twelve changes': reconcile README, AGENTS.md, handover, architecture, skills with the FABLE51 decisions: README/architecture/railway.ts/.env.example still sell the persistent 'agent shops as you' profile (cut, PRODUCT:137); handover #5 still recommends the Privy raw-sign Hedera wrapper (struck, PRODUCT:120); handover #2 recommends approval cards (cut in v1, PRODUCT:39); AGENTS.md:75 'testnets only' vs 
- (2h, Day 1, Sat 5) PRODUCT §7 evidence-file skeletons (README winner shape, HEDERA.md, PRIVY.md, GRAPH.md, VALIDATION.md, FEEDBACK.md, ACQUISITION.md, AI-USE.md) with TODO(tx) markers: none of the eight exist (git ls-files shows only README.md and the *_FABLE51.md planning docs)
- (3h, Day 2, Sun 6) PRODUCT §5 Graph: 'the Subgraph MCP for discovery documented in a SKILL.md'; froggy-leash/browser/verification updated for the two-pocket custody split: no Graph skill under .agents/skills; froggy-leash describes one mandate/one leash and does not mention K_pocket host caps, 'user' provenance, or Privy policy ids; froggy-verification lists no feePayer, four-deployment freshness, or HCS checks
- (2h, Day 1-2) PRODUCT §4 service side: 402 carries extra.feePayer read from Blocky402 /supported at boot; /.well-known/x402.json service card; curl-able 402 in README: oracle.ts:60 extra: {}; no /supported fetch anywhere; router.ts has no /.well-known route (live returns SPA HTML); README has no curl example
- (8h, Day 2, Sun 6) PRODUCT §5 Graph: registry of four pinned Messari deployments, parallel query, _meta block, freshness gate, deployment id + block on every receipt, Studio key with mainnet x402 fallback recorded on the receipt: client.ts:126-135 one subgraphId; no _meta in LENDING_QUERY :24-39; Evidence type carries source+hash only (tools.ts:133-138); no x402 Graph path
- (12h, Day 1-3) PRODUCT §4 W_user: server-created Privy embedded wallet with agent P-256 authorization key as additional signer under committed default-deny policy JSON; raw Privy denial with policy id rendered in the pane: packages/wallet/src/privy.ts only verifies tokens and reads addresses; no wallet creation, no policy JSON, no signer; wallet_send (tools.ts:247-301) never reaches Privy and returns 'transfers are not wired to a signer yet' (:286)
- (10h, Day 2, Sun 6) PRODUCT §4 K_pocket: per-user Hedera ECDSA pocket, key encrypted under env key, host caps 0.5/2 tHBAR, payTo must be a server-issued directory id, idempotency state machine (reserved/signed/settled), mirror-node reconciliation: payer.ts:70-74 one env private key for the whole service; policy caps are USD-micro par quotes (session.ts:189); no directory; ledger states are reserved/settled/failed/refused in memory only
- (7h, Day 2, Sun 6) PRODUCT §7 users table + persisted ledger + Telegram nonces so restart does not reset caps or frozen flag: schema.ts:4 'no users table'; services.ts:96 memoryLedger; packages/database/drizzle migrations folder absent so migrate.ts:23 cannot run; railway.ts has no preDeployCommand
- (4h, Day 2, Sun 6) PRODUCT §4 freeze(user, reason): flag + abort + remove signer + wipe pocket key + sweep + Telegram; unfreeze creates new key and re-adds signer: sockets.ts:184-193 only sets mandate.frozen and abortAll; wallet-pane.tsx:89 exposes the same button as 'Frozen — unfreeze' with no re-provisioning
- (8h, Day 2, Sun 6) PRODUCT §2 Telegram pager: grammY bot, /start <code> pairing, Freeze inline button with nonce, morning digest, /bug: no telegram/grammy code (grep empty); handover.md:54 says it is 'still cut'
- (6h, Day 2, Sun 6) PRODUCT §2 Chrome pane: fresh profile per session wiped at end, exact-origin allowlist with real URL parser on every navigation path incl. redirects/window.open, denylisted schemes: CHROME_PROFILE_DIR persistent by design (environment.ts:126, railway.ts:50-55 volume, profile.ts stale-lock logic); browser_navigate (tools.ts:70-85) has no allowlist check visible in the tool layer
- (2h, Day 3, Mon 7) PRODUCT §5 Hedera: HCS message per settlement: no HCS code (grep hcs/TopicMessage empty)
- (4h, Day 3, Mon 7) PRODUCT §4 wallet_send: user-typed address carries provenance 'user', passes host gate, is built as eth_signTransaction and denied by Privy with policy id; page/model provenance refused by host: address.ts:46-51 has no 'user' provenance; tools.ts:264 hardcodes 'model' so the Privy-denial beat cannot happen
- (0.5h, Day 1, Sat 5) DAY0 §E pins: Bun 1.4.1, model id claude-opus-5: model.ts:29 claude-sonnet-5; package.json/Dockerfile/ci.yml pin 1.4.0
- (8h, Day 3, Mon 7) PRODUCT §2 guest path ('Try it': pooled pre-provisioned identities, IP cap, TTL, delete-my-data): apps/web/src/lib/privy.tsx:47-56 LOCAL identity stub only; no guest pool, IP cap, or delete action
Hygiene: Tracked Chrome profile: 209 files / 2.8 MB under apps/server/.froggy/chrome-profile (Cookies, Login Data, History, Local State, Sessions/*) committed in fd4bdc9, still tracked at HEAD, not matched by .gitignore (git check-ignore -> NOT IGNORED). Read-only sqlite counts: cookies 0, logins 0, history urls 1 — so the disclosure line can truthfully say it held no cookies or logins, but it must be untracked and ignored (DAY0_CHECKLIST_FABLE51.md:68).; Prior-project 'ported from' headers in code: packages/browser/src/input.ts:6 ('Ported from invok'), arbitration.ts:11 ('Ported from invok, which ported it from harness'), chrome-detect.ts:9, apps/server/src/runs.ts:15 ('Ported from harness's packages/kernel/src/agent/runs.ts'), apps/web/src/hooks/use-browser-socket.ts:10, apps/web/src/lib/privy.ts
Verdicts:
- [partly] 2. No server code path ever emits an approval.request; an ask decision surfaces solely as tool text at tools.ts:239-241. -> No producer of approval.request exists (confirmed), but an `ask` decision does NOT surface solely as tool text: every decision incl. ask is pushed to the pane as `policy.decision` (apps/server/src/session.ts:223 `this.deps.onPolicyDecision(decision)` runs before the allow check at :226; apps/server/ (`grep -rn 'approval.request' --include='*.ts' --include='*.tsx' --include='*.md' .` (excluding node_modules, research_FABLE51) -> only packages/protocol/src/app.ts:155 and apps/web/src/hooks/use-app-s)
- [refuted] 4. The reconnect/replay endpoint GET /api/chat/:id/stream does not exist; router.ts only matches /api/chat and /api/chat/stop, so 'Reloading the tab mid-run resumes rather than restarts' is unsupporte -> apps/server/src/router.ts:119-134 has a regex route `if (/^\/api\/chat\/[^/]+\/stream$/u.test(pathname))` that returns 204 when `deps.runs.get(sessionId)?.replay()` is null, else streams the recorded SSE replay as text/event-stream. The client uses `useChat({ resume: true, transport: new DefaultChat (router.ts:119-134 (regex + 204/replay branch); chat.ts:92 comment matches; runs.ts:100-110 `replay()` returns null once `replayable` flips false (unbounded-buffer cap, runs.ts:23,68-71); apps/server/s)
- [partly] 2. No server code path ever emits an approval.request; an ask decision surfaces solely as tool text at tools.ts:239-241. -> The core claim holds: nothing constructs `approval.request`. The 'solely as tool text' part is overstated: an `ask` also reaches the client as a `policy.decision` (index.ts:74) and the wallet pane renders the question as a read-only card (wallet-pane.tsx:128-144). There is still no way for a human t (grep -rn 'approval.request|ApprovalRequest' apps packages --include=*.ts,*.tsx: only packages/protocol/src/app.ts:71,79,154-155, packages/protocol/src/index.ts:5, apps/web/src/hooks/use-app-socket.ts:)
- [refuted] 4. The reconnect/replay endpoint does not exist: router.ts matches only /api/chat (:91) and /api/chat/stop (:115), so 'Reloading the tab mid-run resumes rather than restarts' is unsupported. -> The stream route exists via a regex at router.ts:123 (the grep for `pathname ===` missed it) and the client is wired to it; reload-resume is supported for the single-session build. The real defect under the multi-user lens: index.ts:41 creates ONE `sessionId` per process and router.ts:116/:124 abort (apps/server/src/router.ts:123-134: `if (/^\/api\/chat\/[^/]+\/stream$/u.test(pathname)) { const replay = deps.runs.get(deps.sessionId)?.replay() ?? null; if (replay === null) return new Response(null,)
Extra: Live /health reports every integration stubbed: {"modes":{"graph":"stub","hedera":"stub","model":"stub","privy":"stub"}}; /api/wallet -> {"address":null,"balanceLabel":"balance unavailable (stub)","signerAddress":null,"windowSpentUsdMicros":0}. Nothi | docs/handover.md:5 'Verified live: ... a real 402 and its paid round trip' — the round trip is the stub gate: the live endpoint returned HTTP 200 to a fabricated X-PAYMENT whose payload was {"fake":true}. The snapshot body is hardcoded (capturedAt 17 | Plan §2 requires 'Fresh profile per session, wiped at session end, no imported cookies'; the implementation is the opposite: a persistent shared profile on a Railway volume (.railway/railway.ts browserVolume comment 'Persistent so logins survive a re | wallet_send never reaches a signer or Privy: apps/server/src/tools.ts:287 returns 'Allowed by policy, but transfers are not wired to a signer yet.' when the mandate allows. Plan §4's Privy rule (a)/(b) denial-with-policy-id beat and the Hedera pocket | apps/server/src/migrate.ts:3-7 comment says it runs as a Railway preDeployCommand; .railway/railway.ts declares none. Nothing in apps/server/src imports @froggy/database at runtime (grep: only environment.ts:127 reads DATABASE_URL); the ledger is in- | No node_modules anywhere in the checkout (root, apps/server, packages/payments), so no package tests could be run for this audit; migrate.ts ran only because its imports are external and bun auto-installed from ~/.bun/install/cache.