---
category: Actions
---

The primary control. Six variants and nine sizes, including four square icon-only sizes; pass `data-icon="inline-start"` / `"inline-end"` on a child icon so the padding tightens on that side.
